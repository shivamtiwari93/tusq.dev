# Acceptance Matrix — tusq.dev v0.1.0

**turn_d6b242db408a1312 M43 verification (run_3df735753a5adcb3, 2026-04-27, HEAD 0ec18b8):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (34 scenarios)`. `node -e "require('./src/cli.js')"` → exit 0, module loads cleanly (`_guardInputSchemaPrimaryParameterSourceBucketKey` and `_guardInputSchemaPrimaryParameterSourceAggregationKey` guards pass). `node bin/tusq.js help` → exit 0, 27-command CLI surface (init, scan, manifest, compile, serve, review, docs, approve, auth, confidence, diff, domain, effect, examples, input, method, output, path, pii, policy, redaction, **request**, response, sensitivity, surface, version, help) with `request` between `redaction` and `response`; `node bin/tusq.js help | grep -c '^  [a-z]'` → 27. `node bin/tusq.js request index --help` → exit 0; planning-aid framing (`This is a planning aid, not a runtime request executor, request-payload validator, input-contract conformance detector, request generator, or input-contract certifier`), source rule (closed four-value property-source set path/request_body/query/header; path = all properties source === "path"; request_body = all === "request_body"; query = all === "query"; header = all === "header"; mixed = two or more distinct closed-set values; none = 0 properties; unknown = malformed/cookie/file/multipart/form-data/array-of-sources), bucket order `path → request_body → query → header → mixed → none → unknown` confirmed. `node bin/tusq.js request index --source REQUEST_BODY --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown input schema primary parameter source: REQUEST_BODY`, empty stdout (case-sensitive uppercase enforcement confirmed). `node bin/tusq.js request index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `sources[]` (`path` bucket: get_users_api_v1_users_id, capability_count 1; `request_body` bucket: post_users_users, capability_count 1; `none` bucket: get_users_users, capability_count 1) and `warnings: []`. `node bin/tusq.js request index --source path --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, single `path` bucket. `node bin/tusq.js request index --source request_body --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, single `request_body` bucket. `git diff --quiet HEAD -- package.json package-lock.json` → exit 0 (zero dependency drift). `git diff 9702941..0ec18b8 --name-only` → exactly 10 dev-owned files changed: `src/cli.js`, `tests/smoke.mjs`, `tests/evals/governed-cli-scenarios.json`, `tests/eval-regression.mjs`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`, `.planning/IMPLEMENTATION_NOTES.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`. Zero reserved state, QA-owned, or launch-owned files modified. All 18 M43 ROADMAP checkboxes `[x]`. `classifyInputSchemaPrimaryParameterSource` thresholds (null/undefined → unknown; non-object/Array → unknown; no `properties` key → unknown; properties null/undefined/non-object/Array → unknown; propKeys.length === 0 → none; any property null/undefined/non-object/Array → unknown; any property missing `source` → unknown; any property source non-string or outside closed four-value set → unknown; seenSources.size === 1 → that source value; seenSources.size > 1 → mixed) verified from `src/cli.js:5204–5247`. Challenged dev turn (turn_3f3d861b475bc439, HEAD 0ec18b8) — all five dev decisions upheld; no objections. Added REQ-440–REQ-464 (25 new M43 criteria). OBJ-001/OBJ-002/OBJ-003 carried forward. All 464 acceptance criteria (REQ-001–REQ-464) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_bb74bbc5f03f0d87 M42 verification (run_f33f485bb7998de9, 2026-04-27, HEAD 5583b8d):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (33 scenarios)`. `node -e "require('./src/cli.js')"` → exit 0, module loads cleanly (`_guardOutputSchemaTopLevelTypeBucketKey` and `_guardOutputSchemaTopLevelTypeAggregationKey` guards pass). `node bin/tusq.js help` → exit 0, 26-command CLI surface (init, scan, manifest, compile, serve, review, docs, approve, auth, confidence, diff, domain, effect, examples, input, method, output, path, pii, policy, redaction, **response**, sensitivity, surface, version, help) with `response` between `redaction` and `sensitivity`; `node bin/tusq.js help | grep -c '^  [a-z]'` → 26. `node bin/tusq.js response index --help` → exit 0; planning-aid framing (`This is a planning aid, not a runtime response executor, response-payload validator, data-contract conformance detector, response generator, or data-contract certifier; types are deterministic stable-output ordering only (NOT data-contract-completeness-ranked)`), type rule (literal exact-string match against six JSON Schema 2020-12 spec primitives; `'integer'` bucketed as unknown with reason `output_schema_type_field_value_not_in_json_schema_primitive_set`; compositional schemas without top-level type → unknown; array-of-types → unknown), and bucket order `object → array → string → number → boolean → null → unknown` confirmed. `node bin/tusq.js response index --type OBJECT --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown output schema top-level type: OBJECT`, empty stdout (case-sensitive uppercase enforcement confirmed). `node bin/tusq.js response index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `types[]` (`object` bucket: get_users_api_v1_users_id + post_users_users, capability_count 2; `array` bucket: get_users_users, capability_count 1) and `warnings: []`. `node bin/tusq.js response index --type object --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, single `object` bucket, exit 0. `git diff --quiet HEAD -- package.json package-lock.json` → exit 0 (zero dependency drift). `git diff 2890573..5583b8d --name-only` → exactly 10 dev-owned files changed: `src/cli.js`, `tests/smoke.mjs`, `tests/evals/governed-cli-scenarios.json`, `tests/eval-regression.mjs`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`, `.planning/IMPLEMENTATION_NOTES.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`. Zero reserved state, QA-owned, or launch-owned files modified. All 18 M42 ROADMAP checkboxes `[x]`. `classifyOutputSchemaTopLevelType` thresholds (null/undefined/non-object/array output_schema → unknown; missing `type` property → unknown; null/non-string/array type value → unknown; type string not in closed six-value spec primitive set → unknown; `'integer'` → unknown; each of `'object'/'array'/'string'/'number'/'boolean'/'null'` → respective bucket) verified from `src/cli.js:5174–5193`. Challenged dev turn (turn_8f2c26df7726bc2e, HEAD 5583b8d) — all five dev decisions upheld; no objections. Added REQ-415–REQ-439 (25 new M42 criteria). OBJ-001/OBJ-002/OBJ-003 carried forward. All 439 acceptance criteria (REQ-001–REQ-439) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_17fc87e4651eb033 M41 verification (run_7bad406d9ea95ce5, 2026-04-27, HEAD f7009bb):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (32 scenarios)`. `node -e "require('./src/cli.js')"` → exit 0, module loads cleanly (`_guardPathSegmentCountTierBucketKey` and `_guardPathSegmentCountTierAggregationKey` guards pass). `node bin/tusq.js help` → exit 0, 25-command CLI surface (init, scan, manifest, compile, serve, review, docs, approve, auth, confidence, diff, domain, effect, examples, input, method, output, **path**, pii, policy, redaction, sensitivity, surface, version, help) with `path` between `output` and `pii`; `node bin/tusq.js help | grep -c '^  [a-z]'` → 25. `node bin/tusq.js path index --help` → exit 0; planning-aid framing (`This is a planning aid, not a runtime URL router, path validator, route registry, artifact sprawl executor, or path-depth certifier; tiers are deterministic stable-output ordering only (NOT sprawl-risk-ranked)`), tier function (`none if path is "/" (0 segments); low if 1-2; medium if 3-4; high if >= 5; unknown if path missing or malformed`), path-parameter-counts-as-one-segment note, and bucket order `none → low → medium → high → unknown` confirmed. `node bin/tusq.js path index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 1, stderr `Unknown path segment count tier: HIGH`, empty stdout (case-sensitive uppercase enforcement confirmed). `node bin/tusq.js path index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `tiers[]` (`low` bucket: get_users_users + post_users_users, capability_count 2; `medium` bucket: get_users_api_v1_users_id, capability_count 1) and `warnings: []`. `node bin/tusq.js path index --tier low --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, single `low` bucket. `git diff --quiet HEAD -- package.json package-lock.json` → exit 0 (zero dependency drift). `git diff b29bf30..f7009bb --name-only` → exactly 10 dev-owned files changed: `src/cli.js`, `tests/smoke.mjs`, `tests/evals/governed-cli-scenarios.json`, `tests/eval-regression.mjs`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`, `.planning/IMPLEMENTATION_NOTES.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`. Zero reserved state, QA-owned, or launch-owned files modified. All 18 M41 ROADMAP checkboxes `[x]`. `classifyPathSegmentCountTier` thresholds (null/undefined → unknown; non-string → unknown; empty string → unknown; no leading '/' → unknown; empty interior segment → unknown; count=0 → none; count≤2 → low; count≤4 → medium; else → high) verified from `src/cli.js:4811–4834`. Challenged dev turn (turn_a58d22a53169262b, HEAD f7009bb) — all five dev decisions upheld; no objections. Added REQ-390–REQ-414 (25 new M41 criteria). OBJ-001/OBJ-002/OBJ-003 carried forward. All 414 acceptance criteria (REQ-001–REQ-414) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_63c7cc83d4a3e120 M40 verification (run_0ce75469bde80380, 2026-04-27, HEAD aa81869):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (31 scenarios)`. `node -e "require('./src/cli.js')"` → exit 0, module loads cleanly (`_guardOutputSchemaPropertyCountTierBucketKey` and `_guardOutputSchemaPropertyCountTierAggregationKey` guards pass). `node bin/tusq.js help` → exit 0, 24-command CLI surface (init, scan, manifest, compile, serve, review, docs, approve, auth, confidence, diff, domain, effect, examples, input, method, **output**, pii, policy, redaction, sensitivity, surface, version, help) with `output` between `method` and `pii`; `node bin/tusq.js help | grep -c '^  [a-z]'` → 24. `node bin/tusq.js output index --help` → exit 0; planning-aid framing (`This is a planning aid, not a runtime output executor, output-schema validator, doc-contradiction detector, output generator, or doc-accuracy certifier`), tier function (`none if Object.keys(output_schema.properties).length === 0; low if 1-2; medium if 3-5; high if >= 6; unknown if output_schema/properties missing or malformed`), type:array informative note, and bucket order `none → low → medium → high → unknown` confirmed. `node bin/tusq.js output index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown output schema property count tier: HIGH`, empty stdout (case-sensitive uppercase enforcement confirmed). `node bin/tusq.js output index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `tiers[]` (`low` bucket: get_users_api_v1_users_id + post_users_users; `unknown` bucket: get_users_users with reason `output_schema_properties_field_missing`) and `warnings[]` with one entry. `node bin/tusq.js output index --tier low --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, single `low` bucket. `git diff --quiet HEAD -- package.json package-lock.json` → exit 0 (zero dependency drift). `git diff 5a67550..aa81869 --name-only` → exactly 10 dev-owned files changed: `src/cli.js`, `tests/smoke.mjs`, `tests/evals/governed-cli-scenarios.json`, `tests/eval-regression.mjs`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`, `.planning/IMPLEMENTATION_NOTES.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`. Zero reserved state, QA-owned, or launch-owned files modified. All 18 M40 ROADMAP checkboxes `[x]`. `classifyOutputSchemaPropertyCountTier` thresholds (null/undefined/non-object/array output_schema → unknown; null/non-object/array/missing properties → unknown; properties containing null/primitive/array/function value → unknown; length 0 → none; 1-2 → low; 3-5 → medium; >= 6 → high) verified from `src/cli.js:4444–4468`. Challenged dev turn (turn_5dfd6a1036bcf940, HEAD aa81869) — all five dev decisions upheld; no objections. Added REQ-365–REQ-389 (25 new M40 criteria). OBJ-001/OBJ-002/OBJ-003 carried forward. All 389 acceptance criteria (REQ-001–REQ-389) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_b7f84e0d69dcabf6 M39 verification (run_533b2f8c47cc0bf0, 2026-04-27, HEAD 52c5e56):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (30 scenarios)`. `node -e "require('./src/cli.js')"` → exit 0, module loads cleanly (`_guardRequiredInputFieldCountTierBucketKey` and `_guardRequiredInputFieldCountTierAggregationKey` guards pass). `node bin/tusq.js help` → exit 0, 23-command CLI surface (init, scan, manifest, compile, serve, review, docs, approve, auth, confidence, diff, domain, effect, examples, **input**, method, pii, policy, redaction, sensitivity, surface, version, help) with `input` between `examples` and `method`; `node bin/tusq.js help | grep -c '^  [a-z]'` → 23. `node bin/tusq.js input index --help` → exit 0; planning-aid framing (`This is a planning aid, not a runtime input executor, input-schema validator, input generator, or exposure-safety certifier`), tier function (`none if required.length === 0; low if 1-2; medium if 3-5; high if >= 6; unknown if input_schema/required missing or malformed`), and bucket order `none → low → medium → high → unknown` confirmed. `node bin/tusq.js input index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown required input field count tier: HIGH`, empty stdout (case-sensitive uppercase enforcement confirmed). `node bin/tusq.js input index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `tiers[]` array (`none` and `low` buckets) and `warnings: []`. `git diff --quiet HEAD -- package.json package-lock.json` → exit 0 (zero dependency drift). `git diff 1ac33dc..52c5e56 --name-only` → exactly 10 dev-owned files changed: `src/cli.js`, `tests/smoke.mjs`, `tests/evals/governed-cli-scenarios.json`, `tests/eval-regression.mjs`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`, `.planning/IMPLEMENTATION_NOTES.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`. Zero reserved state, QA-owned, or launch-owned files modified. All 18 M39 ROADMAP checkboxes `[x]`. `classifyRequiredInputFieldCountTier` thresholds (null/undefined/not-plain-object/array → unknown; required null/missing/not-array → unknown; non-string/empty-string element → unknown; length 0 → none; 1-2 → low; 3-5 → medium; >= 6 → high) verified from `src/cli.js:4080-4107`. Challenged dev turn (turn_60ca77d51809c98f, HEAD 52c5e56) — all five dev decisions upheld; no objections. Added REQ-340–REQ-364 (25 new M39 criteria). OBJ-001/OBJ-002/OBJ-003 carried forward. All 364 acceptance criteria (REQ-001–REQ-364) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_6d4662cdbbeeb118 M38 verification (run_0c5145f830f5940e, 2026-04-27, HEAD 71d5474):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (29 scenarios)`. `node -e "require('./src/cli.js'); console.log('Module loaded OK');"` → exit 0, module loads cleanly (`_guardExamplesCountTierBucketKey` and `_guardExamplesCountTierAggregationKey` guards pass). `node bin/tusq.js help` → exit 0, 22-command CLI surface (init, scan, manifest, compile, serve, review, docs, approve, auth, confidence, diff, domain, effect, **examples**, method, pii, policy, redaction, sensitivity, surface, version, help) with `examples` between `effect` and `method`; `node bin/tusq.js help | grep -c '^  [a-z]'` → 22. `node bin/tusq.js examples index --help` → exit 0; planning-aid framing (`This is a planning aid, not a runtime example executor, schema validator, example generator, or eval-readiness certifier`), tier function (`none if length === 0; low if 1-2; medium if 3-5; high if >= 6; unknown if missing/non-array/null-element/non-object/array-element`), and bucket order `none → low → medium → high → unknown` confirmed. `node bin/tusq.js examples index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown examples count tier: HIGH`, empty stdout (case-sensitive uppercase enforcement confirmed). `node bin/tusq.js examples index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `tiers[]` array and `warnings: []`. `git diff --quiet HEAD -- package.json package-lock.json` → exit 0 (zero dependency drift). `git diff d03c73b..HEAD --name-only` → exactly 10 files: `src/cli.js`, `tests/smoke.mjs`, `tests/evals/governed-cli-scenarios.json`, `tests/eval-regression.mjs`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`, `.planning/IMPLEMENTATION_NOTES.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`. Zero reserved state, QA-owned, or launch-owned files modified. All 18 M38 ROADMAP checkboxes `[x]`. `classifyExamplesCountTier` thresholds (null/undefined/non-array → unknown; non-object/null/array element → unknown; length 0 → none; 1-2 → low; 3-5 → medium; >= 6 → high) independently verified from `src/cli.js` lines 3722–3735. Challenged dev turn (turn_0ec2cb6d0b868173, HEAD 71d5474) — all five dev decisions upheld; no objections. Added REQ-315–REQ-339 (25 new M38 criteria). OBJ-001/OBJ-002/OBJ-003 carried forward; OBJ-004 (dev machine evidence table transcription error for --tier HIGH, M37) noted as previously resolved (non-blocking). All 339 acceptance criteria (REQ-001–REQ-339) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_c47096d9b37000b3 M37 verification (run_0b366d58febc99be, 2026-04-27, HEAD 9a9118b):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (28 scenarios)`. `node -e "require('./src/cli.js'); console.log('Module loaded OK');"` → exit 0, module loads cleanly (`_guardPiiFieldCountTierBucketKey` and `_guardPiiFieldCountTierAggregationKey` guards pass). `node bin/tusq.js help` → exit 0, 21-command CLI surface (init, scan, manifest, compile, serve, review, docs, approve, auth, confidence, diff, domain, effect, method, **pii**, policy, redaction, sensitivity, surface, version, help) with `pii` between `method` and `policy`; `node bin/tusq.js help | grep -c '^  [a-z]'` → 21. `node bin/tusq.js pii index --help` → exit 0; planning-aid framing (`This is a planning aid, not a runtime PII detector, data-leakage prevention engine, runtime redaction enforcer, or compliance certifier`), tier function (`none if length === 0; low if 1-2; medium if 3-5; high if >= 6; unknown if missing/non-array/non-string-element`), and bucket order `none → low → medium → high → unknown` confirmed. `node bin/tusq.js pii index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown pii field count tier: HIGH`, empty stdout (case-sensitive uppercase enforcement confirmed). `node bin/tusq.js pii index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `tiers[]` array containing `pii_field_count_tier: 'none'` entry and `warnings: []`. `git diff --quiet HEAD -- package.json package-lock.json` → exit 0 (zero dependency drift). `git diff --quiet HEAD -- src/ bin/ tests/ website/ package.json package-lock.json` → exit 0 (zero uncommitted source drift). Challenged dev turn (turn_0031ce2764696475, HEAD 9a9118b) — `git diff e8e1837..HEAD --name-only` confirms 10 dev-owned files changed: `src/cli.js`, `tests/smoke.mjs`, `tests/evals/governed-cli-scenarios.json`, `tests/eval-regression.mjs`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`, `.planning/IMPLEMENTATION_NOTES.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`. Zero reserved state, QA-owned, or launch-owned files modified. All 20 M37 ROADMAP checkboxes `[x]`. **Dev turn machine evidence discrepancy noted (non-blocking):** dev machine evidence table listed exit_code 0 for the `--tier HIGH` command, but the evidence summary correctly described exit 1 and my independent verification confirms exit 1. The implementation is correct; the machine evidence table was incorrectly transcribed. Added REQ-290–REQ-314 (25 new M37 criteria). OBJ-001/OBJ-002/OBJ-003 carried forward. All 314 acceptance criteria (REQ-001–REQ-314) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_9fd0a8b165ae91e5 M36 verification (run_8580d828f0e1cc1e, 2026-04-27, HEAD 310c55a):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (27 scenarios)`. `node -e "require('./src/cli.js'); console.log('Module loaded OK');"` → exit 0, module loads cleanly. `node bin/tusq.js help` → exit 0, 20-command CLI surface (init, scan, manifest, compile, serve, review, docs, approve, auth, confidence, diff, domain, effect, method, policy, redaction, sensitivity, surface, version, help) with `confidence` between `auth` and `diff`. `node bin/tusq.js confidence index --help` → exit 0; planning-aid framing (`This is a planning aid, not a runtime confidence enforcement engine, evidence-quality scoring engine, or automated re-classifier.`), tier function (`high >= 0.85; medium if 0.6 <= confidence < 0.85; low < 0.6; unknown if null/missing/non-numeric/out-of-[0,1]`), and bucket order `high → medium → low → unknown` confirmed. `node bin/tusq.js confidence index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown confidence tier: HIGH`, empty stdout (case-sensitive lowercase enforcement confirmed). `node bin/tusq.js confidence index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `tiers[]` array and `warnings: []`. `git diff --quiet HEAD -- package.json package-lock.json` → exit 0 (zero dependency drift). `git diff --quiet HEAD -- src/ bin/ tests/ website/ package.json package-lock.json` → exit 0 (zero uncommitted source drift). Challenged dev turn (turn_c3e78ecd352330aa) — `git diff d8e960e..HEAD --name-only` confirms 10 dev-owned files changed: `src/cli.js`, `tests/smoke.mjs`, `tests/evals/governed-cli-scenarios.json`, `tests/eval-regression.mjs`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`, `.planning/IMPLEMENTATION_NOTES.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`. Zero reserved state, QA-owned, or launch-owned files modified. All 20 M36 ROADMAP checkboxes `[x]`. Added REQ-265–REQ-289 (25 new M36 criteria). OBJ-001/OBJ-002/OBJ-003 carried forward. All 289 acceptance criteria (REQ-001–REQ-289) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_b417afbe873a5777 M35 refinement re-verification (run_152b21c8bbaa78d9, 2026-04-26, HEAD d73daeb):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (26 scenarios)`. `node -e "require('./src/cli.js'); console.log('Module loaded OK');"` → exit 0, module loads cleanly under `_guardAuthSchemeBucketAlignment` IIFE (guard passes: `AUTH_SCHEME_INDEX_BUCKET_ORDER ∪ {unknown}` equals `AUTH_SCHEMES`). `node bin/tusq.js auth index --help` → exit 0; planning-aid framing and `bearer → api_key → session → basic → oauth → none → unknown` bucket order confirmed. `node bin/tusq.js auth index --scheme BEARER --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown auth scheme: BEARER`, empty stdout. `git diff --quiet HEAD -- src/ bin/ tests/ website/ package.json package-lock.json` → exit 0 (zero uncommitted source drift). Challenged dev turn (turn_bdc543168423c491) — `git diff 5d09895..HEAD --name-only` confirms 3 dev-owned files changed: `.planning/IMPLEMENTATION_NOTES.md`, `.planning/ROADMAP.md`, `src/cli.js` (PM turn 5d09895 changed 4 PM-owned files: `.planning/PM_SIGNOFF.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`). Zero reserved state, QA-owned, or launch-owned files modified. `src/cli.js` line 109: `AUTH_SCHEME_INDEX_BUCKET_ORDER = Object.freeze(AUTH_SCHEMES.filter((s) => s !== 'unknown'))` — correctly derived from M29 `AUTH_SCHEMES`; `_guardAuthSchemeBucketAlignment` IIFE at line 114 throws synchronously if the sets diverge. ROADMAP line 629 flipped `[ ]→[x]`; all 20 M35 checkboxes now `[x]`. This is the 9th recurrence of the vision_scan stale-checkbox false-positive pattern (M28→M30→M31→M32→M33→M33-again→M34-again→M34-again-again→M35-again). Added REQ-264 (1 new criterion for derivation + guard). OBJ-001/OBJ-002/OBJ-003 carried forward. All 264 acceptance criteria (REQ-001–REQ-264) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_1192bec565305f72 M35 verification (run_0b373a30d182816a, 2026-04-26, HEAD b9de3af):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (26 scenarios)`. `node bin/tusq.js help` → exit 0, 19 commands (init, scan, manifest, compile, serve, review, docs, approve, auth, diff, domain, effect, method, policy, redaction, sensitivity, surface, version, help) with `auth` between `approve` and `diff`. `node bin/tusq.js auth index --help` → exit 0; includes planning-aid framing (`This is a planning aid, not a runtime authentication enforcer or OAuth/OIDC/SAML/SOC2 compliance certifier.`) and bucket iteration order `bearer → api_key → session → basic → oauth → none → unknown`. `node bin/tusq.js auth index --scheme BEARER --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown auth scheme: BEARER`, empty stdout (case-sensitive enforcement confirmed). `node bin/tusq.js auth index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `schemes[]` array. `git diff --quiet HEAD -- package.json package-lock.json` → exit 0 (zero dependency drift). `git diff --quiet HEAD -- src/ bin/ tests/ website/ package.json package-lock.json` → exit 0 (zero uncommitted source drift). Challenged dev turn (turn_e2b7cb50cd77d1d5) — `git diff b129ca9..HEAD --name-only` confirms 12 files: `.planning/IMPLEMENTATION_NOTES.md`, `.planning/PM_SIGNOFF.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md`, `TALK.md`, `src/cli.js`, `tests/eval-regression.mjs`, `tests/evals/governed-cli-scenarios.json`, `tests/smoke.mjs`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`; the 4 PM-owned files from PM turn (turn_d7fc926d1c177c66, 455be4e); 9 dev-owned files from dev turn; TALK.md operational. Zero reserved state, QA-owned, or launch-owned files modified. M35 implemented: AUTH_SCHEME_INDEX_AGGREGATION_KEY_ENUM (scheme|unknown), AUTH_SCHEME_INDEX_BUCKET_ORDER (bearer→api_key→session→basic→oauth→none), cmdAuth, cmdAuthIndex, buildAuthIndex, formatAuthIndex; 19-command CLI surface; 26 eval scenarios (auth-scheme-index-determinism added). Added REQ-240–REQ-263 (24 new M35 criteria). OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation correct for manually-edited manifests. OBJ-002 (low, non-blocking) carried forward: surface-plan-determinism eval uses synthetic_capabilities. OBJ-003 (low, non-blocking) carried forward: M31 per-domain flag value assertions not independently smoke-asserted; M32/M33/M34/M35 close their own analogs. All 263 acceptance criteria (REQ-001–REQ-263) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_38cdeb85330f729b M34 re-verification (run_9b4197b36f01ca42, 2026-04-26, HEAD 035126b):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (25 scenarios)`. `node bin/tusq.js help` → exit 0, 18 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, domain, effect, method, policy, redaction, sensitivity, surface, version, help) with `method` between `effect` and `policy`. `node bin/tusq.js method index --help` → exit 0; includes planning-aid framing (`This is a planning aid, not a runtime HTTP-method router, REST-convention validator, or idempotency classifier.`) and bucket iteration order `GET → POST → PUT → PATCH → DELETE → unknown`. `git diff e63b515..HEAD --name-only` → `.planning/IMPLEMENTATION_NOTES.md` only (zero source drift). Challenged dev turn (turn_de5362f531cf37b9) — only `.planning/IMPLEMENTATION_NOTES.md` changed; M34 confirmed fully shipped at V1.15 in run_bf8efb6b9c733000 (8th recurrence of vision_scan stale-checkbox false-positive pattern: M28→M30→M31→M32→M33→M33-again→M34-again→M34-again-again). PM turn (turn_2ef90ba3bbd49667) correctly reconciled 20 M34 ROADMAP checkboxes as stale-checkbox false positive from vision_scan (intent_1777241694258_a438). No new acceptance criteria required — no new scope shipped; REQ-001–REQ-239 remain complete coverage for the V1.15 (M1–M34) shipped boundary. OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation is correct for manually-edited manifests. OBJ-002 (low, non-blocking) carried forward: surface-plan-determinism eval uses synthetic_capabilities. OBJ-003 (low, non-blocking) carried forward: M31 per-domain flag value assertions not independently smoke-asserted; M32/M33/M34 close their own analogs. All 239 acceptance criteria (REQ-001–REQ-239) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

| Req # | Requirement | Acceptance criteria | Test status | Last tested | Status |
|-------|-------------|---------------------|-------------|-------------|--------|
| REQ-001 | `tusq help` prints command list and exits 0 | stdout contains all 6 commands; exit code 0 | PASS | 2026-04-18 | PASS |
| REQ-002 | `tusq version` prints version and exits 0 | stdout is `0.1.0\n`; exit code 0 | PASS | 2026-04-18 | PASS |
| REQ-003 | Unknown command exits 1 with usage | exit code 1; stdout contains "Unknown command" | PASS | 2026-04-18 | PASS |
| REQ-004 | Invalid flag prints command usage and exits 1 | exit code 1; stdout contains "Usage: tusq scan" | PASS | 2026-04-18 | PASS |
| REQ-005 | `tusq init` creates tusq.config.json | file exists after init; idempotent on re-run | PASS | 2026-04-18 | PASS |
| REQ-006 | `tusq scan` without init exits 1 with message | "No tusq config found" message; exit code 1 | PASS | 2026-04-18 | PASS |
| REQ-007 | `tusq scan` detects Express routes | route_count ≥ 2; framework = express | PASS | 2026-04-18 | PASS |
| REQ-008 | `tusq scan` detects Fastify routes | scan completes exit 0; framework = fastify | PASS | 2026-04-18 | PASS |
| REQ-009 | `tusq scan` detects NestJS routes | scan completes exit 0; framework = nestjs | PASS | 2026-04-18 | PASS |
| REQ-010 | `tusq manifest` requires prior scan | "No scan data found" message; exit code 1 | PASS | 2026-04-18 | PASS |
| REQ-011 | `tusq manifest` generates tusq.manifest.json | file created with capabilities array | PASS | 2026-04-18 | PASS |
| REQ-012 | Approval persists across manifest regeneration | approved=true survives re-run of manifest | PASS | 2026-04-18 (smoke) | PASS |
| REQ-013 | `tusq compile --dry-run` exits 0 without writing files | exit code 0; no tusq-tools/ dir created | PASS | 2026-04-18 (smoke) | PASS |
| REQ-014 | `tusq compile` produces tool definition files | tusq-tools/ directory created | PASS | 2026-04-18 (smoke) | PASS |
| REQ-015 | `tusq review` requires manifest | "No manifest found" message; exit code 1 | PASS | 2026-04-18 | PASS |
| REQ-016 | `tusq review` prints capability summary | exit code 0 after manifest exists | PASS | 2026-04-18 (smoke) | PASS |
| REQ-017 | `tusq serve` starts MCP HTTP server | "server listening" on stdout; responds to RPC | PASS | 2026-04-18 (smoke) | PASS |
| REQ-018 | MCP `tools/list` returns capability array | result.tools is non-empty array | PASS | 2026-04-18 (smoke) | PASS |
| REQ-019 | MCP `tools/call` returns schema | result.schema present | PASS | 2026-04-18 (smoke) | PASS |
| REQ-020 | SIGINT shuts down serve cleanly | exit code 0 after SIGINT | PASS | 2026-04-18 (smoke) | PASS |
| REQ-021 | All commands accept `--verbose` flag | no error on --verbose; extra output produced | PASS | 2026-04-18 (smoke) | PASS |
| REQ-022 | Per-command `--help` prints usage | exit code 0; usage line for each sub-command | PASS | 2026-04-18 | PASS |
| REQ-023 | `website/` reflects the migrated live homepage from `websites/` | Docusaurus homepage preserves the legacy hero, three-card explanation band, workflow steps, and V1 surface grid in `website/src/pages/index.tsx` | PASS | 2026-04-19 | PASS |
| REQ-024 | Docusaurus preserves the legacy site's not-found recovery behavior and styling cues | `website/src/pages/404.tsx`, `website/src/pages/index.module.css`, and `website/src/css/custom.css` retain the warm gradient, Fraunces/Space Grotesk typography, paper-card styling, and a path back to home | PASS | 2026-04-19 | PASS |
| REQ-025 | Implementation notes explicitly record website consolidation and gate-unblocking changes | `.planning/IMPLEMENTATION_NOTES.md` contains a literal `## Changes` heading and states that `website/` is canonical while `websites/` is legacy-only cleanup residue | PASS | 2026-04-19 | PASS |
| REQ-026 | Scan output carries provenance back to source (file + line) for each discovered capability | Each route in `.tusq/scan.json` contains `provenance.file` and `provenance.line` fields pointing to the originating source file | PASS | 2026-04-19 | PASS |
| REQ-027 | Manifest preserves capability provenance from scan | Each capability in `tusq.manifest.json` retains `provenance.file` and `provenance.line` from the originating scan | PASS | 2026-04-19 | PASS |
| REQ-028 | Compiled tool definitions carry provenance to the canonical artifact | Each `tusq-tools/<name>.json` file includes `provenance.file` and `provenance.line` linking the tool back to its source route declaration | PASS | 2026-04-19 | PASS |
| REQ-029 | Post-v0.1.0 roadmap page exists and explicitly marks items as non-shipped | `website/docs/roadmap.md` exists, sidebar_position is 8, and file contains the literal string "None of the items below are shipped behavior in `v0.1.0`" | PASS | 2026-04-19 | PASS |
| REQ-030 | Manifest format doc exists, is sidebar-wired, and accurately documents V1 schema limitations | `website/docs/manifest-format.md` exists at sidebar_position 3, is listed in `website/sidebars.ts` items, and contains `## V1 schema limitations` section with the conservative schema shape | PASS | 2026-04-19 | PASS |
| REQ-031 | `sensitivity_class` field is emitted at every pipeline stage and defaults to `unknown` in V1 | `tusq.manifest.json` capabilities each have `sensitivity_class: "unknown"`; compiled `tusq-tools/*.json` files include `sensitivity_class: "unknown"`; MCP `tools/list` and `tools/call` responses include `sensitivity_class: "unknown"`; `website/docs/manifest-format.md` documents all 5 levels and V1 default | PASS | 2026-04-19 | PASS |
| REQ-032 | `auth_hints` field is emitted at every pipeline stage and MCP runtime responses include it as an array | `tusq.manifest.json` capabilities each have `auth_hints` (string[]); compiled `tusq-tools/*.json` files include `auth_hints`; MCP `tools/list` response includes `auth_hints` as an array; MCP `tools/call` response includes `auth_hints` as an array; smoke tests assert `Array.isArray(auth_hints)` for both MCP methods; `website/docs/mcp-server.md` lists `auth_hints` in governance metadata | PASS | 2026-04-20 | PASS |
| REQ-033 | `examples` and `constraints` fields are emitted at manifest, compiled tools, and MCP `tools/call` stages (not scan or `tools/list` — by design per SYSTEM_SPEC) | `tusq.manifest.json` capabilities each have `examples[]` (default describe-only placeholder) and `constraints` object with 5 null/empty defaults; compiled `tusq-tools/*.json` include both fields propagated from manifest; MCP `tools/call` response includes both fields; smoke tests assert default examples/constraints on manifest, assert custom examples/constraints survive manifest→compile propagation and manifest→tools/call propagation; `website/docs/manifest-format.md` documents both fields with V1 limitations; SYSTEM_SPEC.md lines 564-571 explicitly omit `examples` from `tools/list` and scan — confirmed intentional | PASS | 2026-04-19 | PASS |
| REQ-034 | `redaction` field propagates at manifest, compile, and MCP `tools/call` stages; `tools/list` intentionally excludes it; `approved_by`/`approved_at` are manifest-only audit fields that survive regeneration and are explicitly absent from compile output and MCP responses | `tusq.manifest.json` capabilities each have `redaction` with permissive V1 defaults (`pii_fields: []`, `pii_categories: []`, `log_level: "full"`, `mask_in_traces: false`, `retention_days: null`) and `approved_by`/`approved_at` (null by default); custom values survive manifest regeneration except M25/M26-owned `pii_fields`/`pii_categories`, which are re-derived from source-declared schema keys; compiled `tusq-tools/*.json` include `redaction` propagated from manifest but do NOT contain `approved_by`, `approved_at`, `approved`, or `review_needed`; MCP `tools/list` does NOT include `redaction`; MCP `tools/call` includes `redaction` but does NOT include approval metadata; smoke-test assertions cover all boundaries (`node tests/smoke.mjs` → exit 0); `website/docs/manifest-format.md` documents `redaction` and `approved_by`/`approved_at` (manifest-only audit trail); `website/docs/mcp-server.md` states tools/call includes `redaction` and approval metadata is manifest-only | PASS | 2026-04-22 (smoke + docs) | PASS |
| REQ-035 | Version-history fields are emitted in manifest only (`manifest_version`, `previous_manifest_hash`, `capability_digest`) with deterministic hashing and strict non-propagation downstream | `tusq.manifest.json` root includes `manifest_version` (integer counter) and `previous_manifest_hash` (SHA-256 of prior manifest bytes, or `null` when no prior file exists); each capability includes 64-char lowercase hex `capability_digest` computed from content fields only (excluding `approved`, `approved_by`, `approved_at`, `review_needed`, and `capability_digest`); smoke tests verify digest stability on approval-only edits, digest changes on content edits, and assert version-history fields are absent from compiled `tusq-tools/*.json` and MCP `tools/call`; docs updated in `website/docs/manifest-format.md` and `website/docs/mcp-server.md` to declare manifest-only boundary | PASS | 2026-04-20 | PASS |
| REQ-036 | Framework-specific deep extraction: Fastify correctly detects both 2-arg and 3-arg inline forms and deduplicates; NestJS composes controller prefix + method decorator paths and inherits class-level guards to all methods | Fastify scan returns 2 routes for fixture with `fastify.get(path, {schema}, handler)` (3-arg inline) and `fastify.route({method, url, handler})` (route-object); GET route `handler=listOrders` and `input_schema` reflects schema hints from options block; POST route carries `auth_hints` including `requireAuth`; NestJS scan returns 2 routes with composed paths (`/users`, `/users/:id/disable`); GET route `auth_hints` includes `AuthGuard` (inherited from class decorator); POST route `auth_hints` includes both `AuthGuard` (inherited) and `AdminGuard` (method-level); smoke tests assert all 7 framework-specific properties (`node tests/smoke.mjs` → exit 0) | PASS | 2026-04-20 | PASS |
| REQ-037 | First-pass manifest usability (M15): path parameter extraction into `input_schema`, prefix-aware domain inference, rich capability descriptions, and schema-miss confidence penalty so schema-less routes surface for review | Scan of Express fixture with `app.get('/api/v1/users/:id', requireAuth, getUser)` yields `domain="users"` (prefix segments `api`, `v1` skipped); `input_schema.properties.id={type:"string",source:"path"}` and `required=["id"]`; capability description equals `Retrieve user by id - read-only, requires requireAuth (handler: getUser)`; confidence drops below 0.8 for schema-less named-handler routes (-0.10 schema-miss penalty applied in `scoreConfidence`) → `review_needed=true`; smoke tests assert domain, path-parameter properties, rich description substrings, and sub-0.8 confidence (`node tests/smoke.mjs` → exit 0) | PASS | 2026-04-21 | PASS |
| REQ-038 | Manifest review governance improvements: `tusq review --strict` blocks ship on unapproved or low-confidence capabilities; review output summarizes inferred inputs, returns, and source/handler/framework provenance; input/output schemas are inferred (body params for writes, array/object shapes from handler returns); path parameters are tagged with `source: "path"` through manifest and compile | `tusq review --strict` against an unapproved fixture manifest exits 1 with `Review gate failed: …` on stderr; same command with all capabilities approved and not flagged exits 0; `tusq review --verbose` output includes `inputs=body:request_body`, `returns=array<object>`, `source=src/app.ts`, `handler=listUsers`, `framework=express`; manifest POST `/users` carries `input_schema.properties.body.source="request_body"`; manifest GET `/users` carries `output_schema.type="array"`; manifest POST `/users` carries `output_schema.properties.ok.type="boolean"`; manifest capabilities carry `provenance.framework` and `provenance.handler`; NestJS POST `/users/:id/disable` manifest `input_schema.properties.id.source="path"`; all assertions covered by `node tests/smoke.mjs` → exit 0 | PASS | 2026-04-21 | PASS |
| REQ-039 | `tusq diff` compares two explicit manifest files and reports capability change counts | Fixture manifests with added, removed, changed, and unchanged capabilities produce correct summary counts; invalid or missing `--from`/`--to` paths exit 1 with actionable errors | PASS | 2026-04-21 (smoke) | PASS |
| REQ-040 | `tusq diff --json` emits stable structured output for automation | Output parses as JSON and contains `from_version`, `to_version`, `summary`, and `changes[]`; no human prose is mixed into JSON mode | PASS | 2026-04-21 (smoke) | PASS |
| REQ-041 | Changed capabilities are detected by `capability_digest` and include field-level change names | Same-name capabilities with different digests are classified as `changed`; output includes `previous_digest`, `current_digest`, and `fields_changed[]` | PASS | 2026-04-21 (smoke) | PASS |
| REQ-042 | `tusq diff --review-queue` lists capabilities needing human review | Review queue includes added capabilities, changed capabilities, unapproved capabilities, and `review_needed=true` capabilities with governance and provenance metadata when available | PASS | 2026-04-21 (smoke) | PASS |
| REQ-043 | `tusq diff --fail-on-unapproved-changes` works as a CI gate | Command exits 1 when any added or changed capability is unapproved; exits 0 when added or changed capabilities are approved and no review-needed gate remains | PASS | 2026-04-21 (smoke) | PASS |
| REQ-044 | Help and docs expose the diff workflow honestly | `tusq help`, `tusq diff --help`, README, CLI reference, and manifest docs document `tusq diff` without claiming history-store, git integration, or automatic re-approval support | PASS | 2026-04-21 (smoke + file inspection) | PASS |
| REQ-045 | Governed CLI eval scenarios are versioned and repo-local | `tests/evals/governed-cli-scenarios.json` exists, declares schema_version `1.0`, and contains at least two scenarios covering governed workflow and manifest diff regression behavior | PASS | 2026-04-21 (eval) | PASS |
| REQ-046 | Eval runner validates review-gate behavior before compile | `tests/eval-regression.mjs` runs `tusq review --strict`, expects the low-confidence/unapproved sentinel to fail, then approves the manifest and verifies strict review passes before compile | PASS | 2026-04-21 (eval) | PASS |
| REQ-047 | Eval runner validates compiled tool-call metadata boundaries | Eval assertions verify compiled tools preserve auth hints, side-effect class, schema source markers, examples, constraints/redaction defaults, and do not leak manifest-only approval metadata | PASS | 2026-04-21 (eval) | PASS |
| REQ-048 | Eval runner validates manifest diff review-queue and CI gate behavior | Eval assertions create previous/current manifests, verify changed capability field reporting and review-queue inclusion, and require `--fail-on-unapproved-changes` to fail for the changed unapproved capability | PASS | 2026-04-21 (eval) | PASS |
| REQ-049 | Full project test command executes both smoke and eval suites | `npm test` runs `node tests/smoke.mjs && node tests/eval-regression.mjs` and exits 0 with both "Smoke tests passed" and "Eval regression harness passed (2 scenarios)" | PASS | 2026-04-21 (npm test) | PASS |
| REQ-050 | `tusq approve` is exposed in command help and per-command usage | `tusq help` includes `approve`; `tusq approve --help` prints capability-name, `--all`, `--reviewer`, `--manifest`, `--dry-run`, and `--json` usage | PASS | 2026-04-21 (smoke) | PASS |
| REQ-051 | `tusq approve <name>` approves exactly one capability with audit metadata | Smoke coverage selects one unapproved capability, writes `approved: true`, clears `review_needed`, sets `approved_by` from `--reviewer`, and records ISO-like `approved_at` | PASS | 2026-04-21 (smoke) | PASS |
| REQ-052 | `tusq approve --all` intentionally approves all unapproved or review-needed capabilities | Smoke coverage verifies every manifest capability is `approved: true` and `review_needed: false` after `tusq approve --all --reviewer <id>` | PASS | 2026-04-21 (smoke) | PASS |
| REQ-053 | `tusq approve --dry-run --json` is safe for review automation | Smoke coverage parses dry-run JSON and confirms the manifest approval metadata remains unchanged | PASS | 2026-04-21 (smoke) | PASS |
| REQ-054 | `tusq docs` is exposed in command help and per-command usage | `tusq help` includes `docs`; `tusq docs --help` prints `--manifest`, `--out`, and `--verbose` usage; exit code 0 | PASS | 2026-04-22 (smoke) | PASS |
| REQ-055 | `tusq docs` generates Markdown with required structural sections | stdout includes `# Capability Documentation`, `## Manifest`, `## Capabilities`, `Manifest version`, `Approved: yes`, `Side effect class`, `Sensitivity class`, `Auth hints`, `#### Examples`, `#### Constraints`, `#### Redaction`, `#### Provenance` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-056 | `tusq docs --out <path>` writes deterministic Markdown matching stdout | `--out` writes a file whose content is byte-identical to the stdout output from the same manifest; smoke asserts file content equals stdout | PASS | 2026-04-22 (smoke) | PASS |
| REQ-057 | `tusq docs` exits 1 with actionable message when manifest is not found | Missing or non-existent `--manifest` path exits 1 with `Manifest not found: <path>` | PASS | 2026-04-22 (CLI) | PASS |
| REQ-058 | `tusq serve --policy` startup failure UX | Missing policy file exits 1 with `Policy file not found: <path>`; invalid JSON exits 1 with `Invalid policy JSON at: <path>`; unsupported schema_version exits 1 with `Unsupported policy schema_version: <v>`; unknown mode exits 1 with `Unknown policy mode: <mode>`; invalid allowed_capabilities exits 1 with `Invalid allowed_capabilities in policy:` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-059 | `describe-only` policy mode is a no-op on `tools/call` | Starting serve with `mode: "describe-only"` produces same `tools/call` response shape as no policy; no `executed` field present in the response | PASS | 2026-04-22 (smoke) | PASS |
| REQ-060 | `dry-run` mode returns `executed: false` with plan shape and policy echo | `tools/call` under `mode: "dry-run"` returns `executed: false`, `dry_run_plan` with `method`, `path` (with path-param substitution), `path_params`, `plan_hash` (64-char hex), `evaluated_at`, and a `policy` echo with `mode`, `reviewer`, `approved_at` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-061 | `plan_hash` is deterministic across repeated calls | Same tool call arguments produce identical `plan_hash` on repeated invocations; different argument values produce a different hash | PASS | 2026-04-22 (smoke) | PASS |
| REQ-062 | Argument validation failure returns `-32602` with structured errors | Missing required argument returns JSON-RPC error code `-32602` with `data.validation_errors` array containing at least one entry with `path` (e.g. `/id`) and `reason` (e.g. `required field missing`) fields | PASS | 2026-04-22 (smoke) | PASS |
| REQ-063 | `allowed_capabilities` filter blocks non-listed tools under policy | A capability NOT in `allowed_capabilities` returns error code `-32602` with `data.reason: 'capability not permitted under current policy'`; a capability IN the list succeeds with `executed: false` dry-run response | PASS | 2026-04-22 (smoke) | PASS |
| REQ-064 | `tusq policy init` is exposed in command help and default generation produces a valid policy file | `tusq help` includes `policy`; `tusq policy init --help` prints full flag synopsis including `--mode`, `--reviewer`, `--allowed-capabilities`, `--out`, `--force`, `--dry-run`, `--json`, `--verbose`; default generation without flags produces a file with `schema_version:"1.0"`, `mode:"describe-only"`, non-empty reviewer string, ISO-8601 `approved_at`, and no `allowed_capabilities` field | PASS | 2026-04-22 (smoke) | PASS |
| REQ-065 | `tusq policy init` flag surface: `--mode`, `--allowed-capabilities`, and error exits | `--mode dry-run` sets `mode:"dry-run"` in output; `--allowed-capabilities a,b` produces exact trimmed/deduplicated `["a","b"]` array; unknown `--mode live-fire` exits 1 with `Unknown policy mode: live-fire`; empty `--allowed-capabilities ","` exits 1 with `Invalid allowed-capabilities:` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-066 | `tusq policy init` file-conflict protection and `--force` override | Running `tusq policy init --out <existing>` a second time exits 1 with `Policy file already exists:` and `--force` in stderr; `tusq policy init --out <existing> --force` overwrites cleanly and produces a valid `schema_version:"1.0"` file | PASS | 2026-04-22 (smoke) | PASS |
| REQ-067 | `tusq policy init --dry-run` prints valid JSON to stdout and does NOT write the target file | `--dry-run` output parses as JSON with `schema_version:"1.0"`; the `--out` target file is not created when `--dry-run` is set | PASS | 2026-04-22 (smoke) | PASS |
| REQ-068 | Generated policy file passes `loadAndValidatePolicy` round-trip | `tusq policy init --mode dry-run` followed by `tusq serve --policy <generated>` starts successfully (exits `waitForReady` → SIGINT → exit 0); confirms generator output cannot drift from the M20 validator | PASS | 2026-04-22 (smoke) | PASS |
| REQ-069 | Eval harness covers `policy-init-generator-round-trip` scenario | `tests/evals/governed-cli-scenarios.json` contains a `policy-init-generator-round-trip` scenario; `npm test` runs 5 scenarios and exits 0 with `Eval regression harness passed (5 scenarios)` | PASS | 2026-04-22 (eval) | PASS |
| REQ-070 | `tusq policy verify` is exposed in command help and round-trip init→verify succeeds | `tusq policy verify --help` exits 0 and stdout includes `tusq policy verify`; `tusq policy init --mode dry-run --out <path>` followed by `tusq policy verify --policy <path>` exits 0 with `Policy valid:` message including mode and reviewer | PASS | 2026-04-22 (smoke) | PASS |
| REQ-071 | `tusq policy verify --json` success shape | `--json` on a valid policy file exits 0 and stdout parses as JSON with `valid: true`, `path`, and `policy` object containing `schema_version`, `mode`, `reviewer`, `approved_at`, and `allowed_capabilities` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-072 | `tusq policy verify` exits 1 on missing file and `--json` failure shape | Missing policy path exits 1 with `Policy file not found: <path>`; `--json` on a missing file exits 1 and stdout parses as JSON with `valid: false`, `path`, and `error` fields | PASS | 2026-04-22 (smoke) | PASS |
| REQ-073 | `tusq policy verify` exits 1 on all four malformed-policy failure conditions | Malformed JSON exits 1 with `Invalid policy JSON at:`; unsupported `schema_version` exits 1 with `Unsupported policy schema_version:`; unknown `mode` exits 1 with `Unknown policy mode:`; non-array `allowed_capabilities` exits 1 with `Invalid allowed_capabilities in policy:` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-074 | `tusq policy verify` and `tusq serve --policy` produce identical stderr for all four malformed-policy fixtures | For each of the four bad fixtures (bad JSON, bad schema_version, bad mode, bad allowed_capabilities): both `tusq policy verify --policy <fixture>` and `tusq serve --policy <fixture>` exit 1; the error message produced by `verify` is a substring of the error message produced by `serve` (shared `loadAndValidatePolicy()` call site) | PASS | 2026-04-22 (smoke) | PASS |
| REQ-075 | `tusq policy verify --strict` is exposed in help and default (no-`--strict`) verify behavior is byte-for-byte identical to M22 | `tusq policy verify --help` exits 0 and stdout includes `[--strict [--manifest <path>]]`; running `tusq policy verify` without `--strict` on a valid policy exits 0 with `Policy valid:` message and emits no `strict` or `manifest` keywords — confirming M22 code path is unmodified even when a manifest file is present in the working directory | PASS | 2026-04-22 (smoke) | PASS |
| REQ-076 | `--strict` cross-references every `allowed_capabilities` entry against the manifest and exits 1 on not-in-manifest, not-approved, or requires-review | `tusq policy verify --strict --manifest <path>` exits 0 when all allowed capabilities are present, approved, and not `review_needed`; exits 1 with `Strict policy verify failed: allowed capability not found in manifest: <name>` for a missing cap; exits 1 with `Strict policy verify failed: allowed capability not approved: <name>` for an unapproved cap (`approved: false`); exits 1 with `Strict policy verify failed: allowed capability requires review: <name>` for a `review_needed: true` cap | PASS | 2026-04-22 (smoke) | PASS |
| REQ-077 | `--strict` exits 1 with actionable messages for missing manifest, malformed manifest JSON, and `--manifest` without `--strict` | `--strict --manifest <missing>` exits 1 with `Manifest not found: <path>`; `--strict --manifest <bad-json>` exits 1 with `Invalid manifest JSON at: <path>`; `--manifest <path>` without `--strict` exits 1 with `--manifest requires --strict` before any file read (Constraint 11 guard — manifests file is never opened) | PASS | 2026-04-22 (smoke) | PASS |
| REQ-078 | `--strict --json` success shape includes `valid:true`, `strict:true`, `manifest_path`, `manifest_version`, and `approved_allowed_capabilities`; failure shape includes `strict_errors` array | `--strict --json` on a passing policy exits 0 and stdout parses as JSON with `valid:true`, `strict:true`, `manifest_path`, `manifest_version`, and `approved_allowed_capabilities` count; `--strict --json` on a failing policy exits 1 and stdout parses as JSON with `valid:false`, `strict:true`, `strict_errors` array with `{name,reason}` entries; `reason` is one of `not_in_manifest`, `not_approved`, `requires_review` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-079 | `--strict` with unset or null `allowed_capabilities` passes trivially without reading any capability from the manifest | `tusq policy verify --strict --manifest <path>` on a policy with no `allowed_capabilities` field exits 0 with `Policy valid (strict):`; the trivial-pass path does not iterate the manifest capabilities array | PASS | 2026-04-22 (smoke) | PASS |
| REQ-080 | M22 parity is preserved under `--strict` for all four M22 failure fixtures, and `policy-strict-verify-determinism` eval scenario asserts deterministic `strict_errors` ordering across three repeat runs | For each of the four M22 bad-fixture files (bad JSON, bad schema_version, bad mode, bad allowed_capabilities): `tusq policy verify --policy <fixture>` and `tusq policy verify --policy <fixture> --strict` both exit 1 and their stderr messages are byte-identical; `tests/evals/governed-cli-scenarios.json` contains `policy-strict-verify-determinism`; `npm test` exits 0 with `Eval regression harness passed (6 scenarios)` | PASS | 2026-04-22 (smoke + eval) | PASS |
| REQ-081 | Fastify route with a literal `schema: { body: { properties: {...}, required: [...] } }` block produces `input_schema` with declared top-level fields merged in, `additionalProperties: false`, `source: 'fastify_schema_body'`, and property keys in declaration order | POST /items in Fastify fixture yields `input_schema.source='fastify_schema_body'`, `additionalProperties: false`, properties `{name:{type:'string'}, price:{type:'number'}, active:{type:'boolean'}}` with keys in that declaration order, and `required` containing `name` and `price` | PASS | 2026-04-22 (smoke M24-b) | PASS |
| REQ-082 | Path-param collision — when body schema declares a field name matching a path parameter, the path parameter entry wins (`source: 'path'`, `type: 'string'`); body-declared value is silently dropped | PUT /items/:id body declares `id:{type:'number'}` but the path parameter `:id` is authoritative; manifest `input_schema.properties.id` carries `source:'path'` and `type:'string'`; body `name` field (no collision) is present | PASS | 2026-04-22 (smoke M24-d) | PASS |
| REQ-083 | Fastify routes without a literal body-properties block fall back to M15 behavior byte-for-byte — no `fastify_schema_body` source tag, `additionalProperties: true` | GET /orders (schema with `response` only, no `body` key) and POST /orders (schema.body exists but no `properties` key) both produce `additionalProperties: true` with no `source` tag; GET /catalog (schema is a variable reference `sharedSchema`, not a literal object) also falls back to M15 with no extraction | PASS | 2026-04-22 (smoke M24-a, M24-c) | PASS |
| REQ-084 | Express and NestJS fixtures produce zero `fastify_schema_body` source tags; M24 is Fastify-only | Scanning `tests/fixtures/express-sample` after M24 yields no capability with `input_schema.source='fastify_schema_body'`; scanning `tests/fixtures/nest-sample` after M24 yields no route with `input_schema.source='fastify_schema_body'` | PASS | 2026-04-22 (smoke M24-e, M24-f) | PASS |
| REQ-085 | `schema_fields` presence adds +0.04 confidence boost in `scoreConfidence()` on top of the existing `schema_hint` signal (capped at 0.95); no other scoring branches are affected | `src/cli.js` scoreConfidence(): `if (route.schema_fields) { score += 0.04; }` is present after the `schema_hint` branch; routes without `schema_fields` retain byte-identical confidence scores; smoke test confirms no Express/NestJS confidence regression | PASS | 2026-04-22 (code inspection + smoke) | PASS |
| REQ-086 | Repeated `tusq scan` + `tusq manifest` on the same Fastify fixture produce byte-identical `input_schema.properties` ordering for the extracted schema.body route | Two consecutive `tusq manifest` runs on the Fastify fixture produce identical `JSON.stringify(capabilities.find(POST /items).input_schema.properties)` values (smoke M24-g assertion); declaration order (name, price, active) is preserved across both runs | PASS | 2026-04-22 (smoke M24-g) | PASS |
| REQ-087 | `fastify-schema-body-extraction-determinism` eval scenario asserts 3-run determinism; `npm test` exits 0 with `Eval regression harness passed (7 scenarios)` | `tests/evals/governed-cli-scenarios.json` contains `fastify-schema-body-extraction-determinism`; `tests/eval-regression.mjs` includes `runFastifySchemaExtractionScenario()`; 3 scan runs on the Fastify fixture produce identical `source: 'fastify_schema_body'`, `additionalProperties: false`, and `properties` key ordering; `npm test` exits 0 with `Eval regression harness passed (7 scenarios)` | PASS | 2026-04-22 (eval) | PASS |

| REQ-088 | `extractPiiFieldHints(properties)` pure function — whole-key normalized match (`toLowerCase()` + strip `_`/`-`), returns source-literal keys in `Object.keys(properties)` iteration order, returns `[]` for falsy/non-object/array input; defined in `src/cli.js` at line ~2489 with no framework import, no eval, no external PII library (SYSTEM_SPEC Constraint 15) | `src/cli.js` `extractPiiFieldHints` function at line 2489: input validation (`!properties || typeof properties !== 'object' || Array.isArray(properties)`) returns `[]`; normalization is `key.toLowerCase().replace(/[_-]/g, '')` and whole-key Set lookup; source-literal key pushed (not normalized form) | PASS | 2026-04-22 (code inspection) | PASS |
| REQ-089 | `capability.redaction.pii_fields` populated in `cmdManifest` from `extractPiiFieldHints(capability.input_schema?.properties)` on every manifest run; pii-hint-sample POST /auth → `pii_fields=["email","password"]`; POST /register → `pii_fields=["user_email","first_name","phone_number"]` (`account_type` excluded — normalizes to `accounttype`, not in canonical set); GET /catalog → `pii_fields=[]` | `src/cli.js` line 410-413: `capability.redaction.pii_fields = extractPiiFieldHints(capability.input_schema && capability.input_schema.properties)` before `computeCapabilityDigest()`; eval scenario `pii-field-hint-extraction-determinism` asserts all three routes across 3 repeat runs; `npm test` → exit 0 with `Eval regression harness passed (8 scenarios)` | PASS | 2026-04-22 (eval) | PASS |
| REQ-090 | `PII_CANONICAL_NAMES` frozen `Set` enumerated in `src/cli.js` with 36 normalized names across 9 categories: Email (3), Phone (5), Government ID (4), Name (4), Address (3), DOB (3), Payment (7), Secrets (7), Network (1) — matches SYSTEM_SPEC DEC-599 V1.6 canonical list exactly; any expansion is a material governance event requiring its own ROADMAP milestone | `src/cli.js` lines 15–34: `new Set([...])` with all 36 normalized names enumerated; category comments present; V1.6 governance note at line 14 | PASS | 2026-04-22 (code inspection) | PASS |
| REQ-091 | `capability.sensitivity_class` NOT auto-escalated when `pii_fields` is non-empty (stays `"unknown"`); redaction fields `log_level`, `mask_in_traces`, `retention_days` NOT auto-populated by M25 — remain at V1 permissive defaults | Smoke test line 258: `manifest.capabilities.every((capability) => capability.sensitivity_class === 'unknown')` passes for Express fixture (which produces pii_fields=[] for all capabilities); `extractPiiFieldHints` only sets `pii_fields` — `log_level`, `mask_in_traces`, `retention_days` are preserved from `normalizeRedaction()` defaults (not overwritten by M25) | PASS | 2026-04-22 (code inspection + smoke) | PASS |
| REQ-092 | Express fixture capabilities all produce `pii_fields: []` (no PII-named fields in Express sample routes); NestJS fixture M24 scan produces no `fastify_schema_body` source tags; repeated manifest generations produce byte-identical `pii_fields` ordering; capability with no `input_schema.properties` returns `[]` from `extractPiiFieldHints` | Smoke test line 264: Express manifest `capabilities.every((c) => JSON.stringify(c.redaction) === JSON.stringify(defaultRedaction))` where `defaultRedaction.pii_fields = []`; NestJS M24-f smoke at line 692-696 confirms no `fastify_schema_body`; pii-hint-sample GET /catalog (no body) verified by eval scenario | PASS | 2026-04-22 (smoke + eval) | PASS |
| REQ-093 | `pii-field-hint-extraction-determinism` eval scenario present in `tests/evals/governed-cli-scenarios.json`; `runPiiFieldHintScenario()` in `tests/eval-regression.mjs`; 3-run determinism verified across POST /auth, POST /register, GET /catalog in pii-hint-sample fixture; `npm test` exits 0 with `Eval regression harness passed (8 scenarios)` | `tests/evals/governed-cli-scenarios.json`: scenario `pii-field-hint-extraction-determinism` with `repeat_runs: 3` and `expected_routes` covering all 3 pii-hint-sample routes; `tests/eval-regression.mjs` line 534: dispatches to `runPiiFieldHintScenario()`; `npm test` → exit 0 with `Eval regression harness passed (8 scenarios)` (independently re-run) | PASS | 2026-04-22 (eval) | PASS |

| REQ-094 | `PII_CATEGORY_BY_NAME` frozen lookup is the M26 single source of truth: every M25 normalized canonical name maps to exactly one of 9 category keys (`email`, `phone`, `government_id`, `name`, `address`, `date_of_birth`, `payment`, `secrets`, `network`); `PII_CANONICAL_NAMES` is derived from this lookup so M25/M26 freeze together | `src/cli.js` lines 13-62 define `PII_CATEGORY_BY_NAME = Object.freeze({...})` with all 36 normalized names and derive `PII_CANONICAL_NAMES = new Set(Object.keys(PII_CATEGORY_BY_NAME))`; no separate duplicate name list can drift from categories; no new dependency or external dictionary import exists | PASS | 2026-04-22 (code inspection) | PASS |
| REQ-095 | `extractPiiFieldCategories(piiFields)` pure function returns a parallel category array in `pii_fields` order, returns `[]` for absent/empty/non-array inputs, applies the same normalization rule as M25, and throws synchronously if a field lacks a known category | `src/cli.js` lines 2532-2550: guard returns `[]`; iteration uses `for...of` over `piiFields`; normalization is `field.toLowerCase().replace(/[_-]/g, '')`; lookup uses `PII_CATEGORY_BY_NAME[normalized]`; missing category throws `CliError`; function has no I/O, no source scan, no eval, and no library import | PASS | 2026-04-22 (code inspection) | PASS |
| REQ-096 | Every generated capability emits `redaction.pii_categories` immediately after `pii_fields` and before `capability_digest`; empty `pii_fields` yields `pii_categories: []` (array, never absent); `defaultRedaction()` and `normalizeRedaction()` preserve the additive field shape | `src/cli.js` lines 438-443 populate `pii_fields`, then `pii_categories`, then compute digest; `defaultRedaction()` lines 2553-2559 includes `pii_categories: []`; `normalizeRedaction()` preserves valid existing `pii_categories` arrays before M26 re-derives generated capability categories; smoke default redaction assertion lines 229-235 expects `pii_categories: []` on Express capabilities | PASS | 2026-04-22 (code inspection + smoke) | PASS |
| REQ-097 | Category labels match field hints one-to-one in realistic routes: POST /auth → `pii_fields=["email","password"]`, `pii_categories=["email","secrets"]`; POST /register → `["user_email","first_name","phone_number"]`, `["email","name","phone"]`; POST /profile → `["user_email","ssn","credit_card"]`, `["email","government_id","payment"]`; GET /catalog → `[]`, `[]` | `tests/fixtures/pii-hint-sample/src/server.ts` declares the M26 mixed-category `/profile` route; `tests/evals/governed-cli-scenarios.json` lines 183-233 encodes expected fields/categories for all four routes; `tests/eval-regression.mjs` lines 542-570 asserts arrays, one-to-one length, `sensitivity_class`, and redaction policy defaults; `npm test` exits 0 with `Eval regression harness passed (9 scenarios)` | PASS | 2026-04-22 (eval) | PASS |
| REQ-098 | M26 does not change score/confidence, `sensitivity_class`, `log_level`, `mask_in_traces`, `retention_days`, `tusq serve`, or `tusq policy verify --strict`; category labels are descriptive metadata only | `scoreConfidence()` still only has the pre-existing M24 `schema_fields` branch (`src/cli.js` lines 2380-2403), with no PII/category scoring branch; eval category scenario asserts `sensitivity_class === "unknown"` and default redaction policy fields for every expected route; M23 strict smoke coverage still passes under `npm test`, confirming strict verification remains approval-alignment only and does not inspect redaction categories | PASS | 2026-04-22 (code inspection + smoke + eval) | PASS |
| REQ-099 | Documentation covers the M26 manifest shape and framing boundary without overclaiming retention enforcement, automated redaction, or regulatory compliance | `website/docs/manifest-format.md` lines 292-333 documents `pii_categories` one-to-one correspondence, empty-array behavior, nine category keys, no value/code/dictionary inspection, no `sensitivity_class` or redaction-policy auto-population, no serve/strict enforcement, and no GDPR/HIPAA/PCI compliance claim; `README.md` line 207 mentions parallel `pii_categories` labels for reviewer-led masking/retention decisions; `RELEASE_NOTES.md` includes the M26 V1.7 section | PASS | 2026-04-22 (docs inspection) | PASS |
| REQ-100 | `pii-category-label-determinism` eval scenario is present and deterministic across 3 repeat runs; full project verification runs 9 scenarios | `tests/evals/governed-cli-scenarios.json` contains scenario `pii-category-label-determinism` with `repeat_runs: 3`; `tests/eval-regression.mjs` dispatches to `runPiiCategoryLabelScenario()` at lines 610-611; `npm test` exits 0 with `Smoke tests passed` and `Eval regression harness passed (9 scenarios)` | PASS | 2026-04-22 (npm test) | PASS |

| REQ-101 | `tusq redaction` is exposed as one new CLI noun with exactly one subcommand, `review`; `tusq redaction review --help` documents only `--manifest`, `--capability`, and `--json` plus reviewer-aid framing | `node bin/tusq.js help` exits 0 and lists `redaction`; `node bin/tusq.js redaction review --help` exits 0 and prints `Usage: tusq redaction review [--manifest <path>] [--capability <name>] [--json]`, the three flags, exit codes, and `This is a reviewer aid, not a runtime enforcement gate.` | PASS | 2026-04-22 (CLI) | PASS |
| REQ-102 | `PII_REVIEW_ADVISORY_BY_CATEGORY` is the frozen M27 nine-category advisory source of truth; every M26 category has exactly one advisory string using em-dash U+2014 and reviewer-directed wording | `src/cli.js` defines `PII_REVIEW_ADVISORY_BY_CATEGORY = Object.freeze({...})` with `email`, `phone`, `government_id`, `name`, `address`, `date_of_birth`, `payment`, `secrets`, and `network`; `assertPiiReviewAdvisorySetComplete()` guards missing entries; smoke M27 advisory byte-exactness checks assert every advisory contains the UTF-8 em-dash byte sequence and matches the expected text | PASS | 2026-04-22 (code inspection + smoke) | PASS |
| REQ-103 | `tusq redaction review` emits deterministic per-capability reports in human and `--json` modes, copying `manifest_path`, `manifest_version`, `generated_at`, `approved`, `sensitivity_class`, `pii_fields`, and `pii_categories` without re-stamping wall-clock data | `buildRedactionReviewReport()` copies top-level manifest fields with `null` fallback; `formatRedactionReviewReport()` uses `unknown` fallback only for human display; smoke M27(g) asserts byte-identical human and JSON stdout across repeated runs; `npm test` exits 0 with `Smoke tests passed` | PASS | 2026-04-22 (smoke + code inspection) | PASS |
| REQ-104 | Advisory rows are one per distinct category per capability in first-appearance order, and `--capability <name>` filters to one exact capability without changing report shape | `buildPiiReviewAdvisories()` uses a `Set` and iterates `pii_categories` in order; smoke M27(k) verifies mixed-category order for email/government_id/payment; smoke M27(c) verifies `--capability` returns one exact capability; eval scenario `redaction-review-determinism` asserts advisory order for `/auth`, `/profile`, and `/catalog` | PASS | 2026-04-22 (smoke + eval) | PASS |
| REQ-105 | No-PII and empty-manifest cases are explicit, successful reviewer signals; malformed/missing inputs fail with stderr-only output and empty stdout | Capabilities with `pii_fields: []` render `No canonical PII field-name matches — reviewer action: none required from M27.` and have empty arrays/advisories; manifest with `capabilities: []` exits 0 with exactly `No capabilities in manifest — nothing to review.\n` in human mode and `{manifest_path, manifest_version, generated_at, capabilities: []}` in JSON mode; missing manifest, malformed JSON, missing/non-array `capabilities`, unknown capability, and unknown flag all exit 1 with stderr-only messages and `stdout === ""` under smoke coverage | PASS | 2026-04-22 (smoke) | PASS |
| REQ-106 | `tusq redaction review` is strictly read-only: it does not mutate `tusq.manifest.json`, does not write policy/tool/scan artifacts, and does not change `tusq policy verify` default or `--strict` behavior | Smoke M27(h) captures manifest content and mtime before/after a review run and asserts both unchanged; smoke M27 policy-verify check compares default and strict policy verify stdout/exit code before and after `redaction review`; `cmdRedactionReview()` only reads the manifest and writes stdout/stderr | PASS | 2026-04-22 (smoke + code inspection) | PASS |
| REQ-107 | M27 does not mutate or infer governance defaults: `sensitivity_class`, `retention_days`, `log_level`, `mask_in_traces`, approval metadata, and `capability_digest` remain manifest-owned inputs, not redaction-review outputs | `buildRedactionReviewReport()` echoes `sensitivity_class` verbatim and emits no retention/log/mask fields; smoke M27(i) asserts report sensitivity equals the manifest value; `cmdRedactionReview()` never calls scanner, manifest generation, approve, compile, policy write, or digest code; SYSTEM_SPEC Constraint 20 remains satisfied | PASS | 2026-04-22 (smoke + code inspection) | PASS |
| REQ-108 | `redaction-review-determinism` eval scenario is present and full project verification now runs 10 scenarios | `tests/evals/governed-cli-scenarios.json` contains `redaction-review-determinism` with `repeat_runs: 3`; `tests/eval-regression.mjs` dispatches to `runRedactionReviewDeterminismScenario()`; `npm test` exits 0 with `Smoke tests passed` and `Eval regression harness passed (10 scenarios)` | PASS | 2026-04-22 (npm test) | PASS |

| REQ-109 | `classifySensitivity(cap)` pure function implementing the frozen six-rule first-match-wins decision table — zero-evidence guard; R1 preserve→restricted; R2 admin/destructive verb or admin route→restricted; R3 pii_categories non-empty→confidential; R4 write verb+financial-context→confidential; R5 auth_hints or write verb→internal; R6 default→public | `src/cli.js` `classifySensitivity` at line 2701: all six rules present in order; zero-evidence guard returns 'unknown' before R1; R1 `preserve === true` check before R2; R2 checks `RESTRICTED_VERBS` set and `/^(admin\|root\|superuser)(\/\|$)/i` route pattern; R3 checks `piiCats && piiCats.length`; R4 checks `WRITE_VERBS` + `FINANCIAL_RE` on route and property keys; R5 checks `authRequired \|\| WRITE_VERBS.has(verb)`; R6 returns 'public'; `SENSITIVITY_CLASSES` const at line 8 is frozen five-value array | PASS | 2026-04-25 (code inspection + smoke) | PASS |
| REQ-110 | `classifySensitivity` is called on every capability in `cmdManifest` after `pii_categories` are populated; result is stored as `capability.sensitivity_class`; `sensitivity_class` is included in `computeCapabilityDigest` payload so any classification change flips the digest and resets `approved=false` | `src/cli.js` line 468: `capability.sensitivity_class = classifySensitivity(capability)` called after `pii_categories` are set; line 2964: `sensitivity_class: normalizeSensitivityClass(capability.sensitivity_class)` is in the digest payload object; smoke M28 valid-enum assertion at line 271 verifies all manifest capabilities have a valid sensitivity class | PASS | 2026-04-25 (code inspection + smoke) | PASS |
| REQ-111 | 8-case smoke matrix (AC-7) covers all M28 inference rule precedence scenarios: (1) preserve=true+PII→restricted (R1 beats R3); (2) DELETE /accounts with PII→restricted (R2 beats R3); (3) POST /auth with PII→confidential (R3); (4) POST /payments/new→confidential (R4); (5) auth_hints→internal (R5); (6) PUT /items/:id no PII/auth→internal (R5-write); (7) GET /catalog no PII/auth→public (R6); (8) zero-evidence→unknown | `tests/smoke.mjs` lines 1625–1845: cases 1–8 asserted via pii-hint-sample and express-sample fixture scans plus synthetic manifest injection; compile-output-invariant also asserted (two capabilities differing only in sensitivity_class → byte-identical compiled output); `node tests/smoke.mjs` → exit 0 | PASS | 2026-04-25 (smoke) | PASS |
| REQ-112 | `tusq compile` output is byte-identical regardless of `sensitivity_class` value; `sensitivity_class` is absent from compiled tool definitions and from MCP `tools/list` and `tools/call` responses | `src/cli.js` `cmdCompile` at line 500: tool object constructed without `sensitivity_class` field; smoke line 370 asserts `'sensitivity_class' in compiledTool` is false; smoke line 730 asserts `tools/list` first tool does not include `sensitivity_class`; smoke line 751 asserts `tools/call` response does not include `sensitivity_class`; compile-output-invariant confirms byte-equality for two manifests differing only in `sensitivity_class` | PASS | 2026-04-25 (code inspection + smoke) | PASS |
| REQ-113 | `tusq review --sensitivity <class>` optional filter limits displayed output to capabilities matching the specified class; unrecognized values exit 1 before any output; missing value exits 1; filter does not affect exit code calculation (unapproved/low-confidence capabilities not in the filter still cause `--strict` failure); `tusq review --help` documents `--sensitivity <class>` | `src/cli.js` `cmdReview` lines 800–831: `--sensitivity` flag parsed, validated against `SENSITIVITY_CLASSES`; filter applied at line 860 as display-only; `src/cli.js` line 2189: `printCommandHelp('review')` shows `--sensitivity <class>` in usage; smoke M28 `--sensitivity restricted` and `--sensitivity public` assertions pass; bad-filter exits 1 with `Unknown sensitivity class: bogus`; missing-value exits 1 with `--sensitivity requires a value` | PASS | 2026-04-25 (smoke + CLI) | PASS |
| REQ-114 | Three new M28 eval regression scenarios are present in `governed-cli-scenarios.json` and pass: R4 financial inference (POST /payments → confidential), R1 preserve precedence (preserve=true+PII → restricted), zero-evidence unknown bucket; full project verification now runs 13 scenarios | `tests/evals/governed-cli-scenarios.json` contains `sensitivity-class-r4-financial-inference`, `sensitivity-class-r1-preserve-precedence`, and `sensitivity-class-zero-evidence-unknown`; `tests/eval-regression.mjs` dispatches to `runSensitivityClassSyntheticScenario` and `runSensitivityClassPreserveScenario`; `npm test` exits 0 with `Eval regression harness passed (13 scenarios)` | PASS | 2026-04-25 (npm test) | PASS |
| REQ-115 | M28 documentation: `website/docs/manifest-format.md` updated with a Sensitivity Class section documenting the five enum values, the six-rule table, the zero-evidence guard, digest-flip migration note, and framing boundary (Constraint 21); `website/docs/cli-reference.md` documents `--sensitivity <class>` on `tusq review` with usage, legal values, and display-only semantics | `website/docs/manifest-format.md` § `sensitivity_class rules` updated with the five-value enum table, the six-rule decision table, zero-evidence guard description, digest-flip note, framing boundary, and compile/MCP surface invariant; `website/docs/cli-reference.md` § `tusq review` updated with `--sensitivity <class>` in the usage line, a new example row with `sensitivity=public`, and a flag description paragraph | PASS | 2026-04-25 (docs inspection) | PASS |

| REQ-116 | `classifyAuthRequirements(cap)` pure function — closed 7-value `auth_scheme` enum {bearer, api_key, session, basic, oauth, none, unknown}; frozen 6-rule first-match-wins decision table; zero-evidence guard fires as the FIRST check before R1; function is pure (no I/O, no clock, no network, no auth-library import) | `src/cli.js` `classifyAuthRequirements` at line 2803: zero-evidence guard at lines 2808–2814 before R1; `AUTH_SCHEMES` const at line 9 is the frozen 7-value array; function reads only `cap.auth_hints`, `cap.path`, `cap.auth_required`, `cap.sensitivity_class`; `npm test` → exit 0, `Smoke tests passed` | PASS | 2026-04-25 (code inspection + smoke) | PASS |
| REQ-117 | Zero-evidence guard returns `auth_scheme:'unknown'` (NEVER `'none'`) for capabilities with no middleware (`auth_hints: []`), no route (`path` empty), no `auth_required` flag (undefined), and no sensitivity signal (`sensitivity_class: 'unknown'`) | `src/cli.js` lines 2808–2814: `if (!hasMiddleware && !hasRoute && !hasAuthFlag && !hasSensitivitySignal) return { auth_scheme: 'unknown', ..., evidence_source: 'none' }`; smoke M29(case8) at line 2002 asserts `auth_scheme !== 'none'` for zero-evidence caps; eval scenario `auth-scheme-zero-evidence-unknown-bucket` asserts `auth_scheme === 'unknown'`; `npm test` → exit 0 | PASS | 2026-04-25 (code inspection + smoke + eval) | PASS |
| REQ-118 | R1–R5 middleware-name first-match-wins: R1 `/bearer\|jwt\|access[_-]?token/i` → `bearer`; R2 `/api[_-]?key\|x-api-key/i` → `api_key`; R3 `/session\|cookie\|passport-local/i` → `session`; R4 `/basic[_-]?auth/i` → `basic`; R5 `/oauth\|oidc\|openid/i` → `oauth`; R6 `auth_required === false` AND non-admin route → `none`; default → `unknown`; R1 always beats later rules | `src/cli.js` RULES array lines 2822–2828; smoke M29 cases 1–5 at lines 1874–1924 verify R1–R5 respectively; eval scenario `auth-scheme-bearer-r1-precedence` asserts R1 precedence; NOTE: OBJ-001 (medium): R6 is implemented but `auth_required` is never set in scan output, making R6 unreachable through the automated pipeline in V1.10; manual manifest edits can reach R6 and the code is correct | PASS | 2026-04-25 (code inspection + smoke + eval) | PASS |
| REQ-119 | Scope/role extraction via frozen patterns `/scopes?:\s*\[([^\]]+)\]/` and `/role[s]?:\s*\[([^\]]+)\]/` over `cap.auth_hints`; order-preserving, case-sensitive dedup; empty arrays (NEVER `null` or absent) on zero matches | `src/cli.js` `extractFrozenList` at lines 2785–2801; smoke M29(case7) at lines 1970–1974 asserts `auth_scopes` and `auth_roles` are arrays; `AUTH_SCHEMES` const covers closed enum; `npm test` → exit 0 | PASS | 2026-04-25 (code inspection + smoke) | PASS |
| REQ-120 | `auth_requirements` is included in `computeCapabilityDigest` payload (AC-5); any value change flips `capability_digest` and resets `approved` to `false`; the digest-flip + M13 re-approval path is unchanged | `src/cli.js` line 3066: `auth_requirements: capability.auth_requirements \|\| null` in digest payload; `computeCapabilityDigest` at lines 3055–3075; smoke M29 compile-output-invariant at lines 2023–2065 verifies two capabilities differing only in `auth_requirements` produce byte-identical compile output (confirming digest inclusion without compile surface leak) | PASS | 2026-04-25 (code inspection + smoke) | PASS |
| REQ-121 | `auth_requirements` MUST NOT appear in compiled tool definitions (`tusq compile` / `tusq-tools/*.json`) or in any MCP serve response shape (`tools/list`, `tools/call`, `dry_run_plan`) — AC-7 compile/serve byte-identity invariant | `src/cli.js` `cmdCompile` lines 542–555: tool object fields: name, description, method, path, parameters, returns, side_effect_class, auth_hints, examples, constraints, redaction, provenance — `auth_requirements` absent; `tools/list` lines 661–673 and `dry_run_plan` lines 723–748 — `auth_requirements` absent; smoke M29 assertions at lines 374–375, 739–740, 763–764 throw if `auth_requirements` appears in any of these surfaces; `npm test` → exit 0 | PASS | 2026-04-25 (code inspection + smoke) | PASS |
| REQ-122 | `tusq review --auth-scheme <scheme>` optional filter: limits displayed output to capabilities matching the specified scheme; closed 7-value validation — unrecognized value exits 1 with `Unknown auth scheme: <value>` on stderr and empty stdout; missing value exits 1 with `--auth-scheme requires a value`; filter is display-only; mutually compatible with `--sensitivity` (AND-style intersection); `tusq review --help` documents the flag | `src/cli.js` lines 812–887: pre-check, parsing, validation, filter application; `printCommandHelp('review')` at line 2220: `'Usage: tusq review [--format json] [--strict] [--sensitivity <class>] [--auth-scheme <scheme>] [--verbose]'`; smoke M29 at lines 2068–2105: valid filter, unknown-filter exit-1+empty-stdout, no-value exit-1, AND intersection, review-output auth-display all pass; `node bin/tusq.js review --help` → includes `--auth-scheme <scheme>` | PASS | 2026-04-25 (code inspection + smoke + CLI) | PASS |
| REQ-123 | Three new M29 eval scenarios present in `governed-cli-scenarios.json` and passing: `auth-scheme-bearer-r1-precedence` (R1: requireJwtAuth → bearer), `auth-scheme-zero-evidence-unknown-bucket` (zero-evidence → unknown), `auth-scheme-scopes-extraction-order` (auth_scopes/auth_roles are arrays); total eval harness ≥16 scenarios | `tests/evals/governed-cli-scenarios.json` lines 314–372 define the 3 new scenarios of type `auth_requirements_synthetic`; `tests/eval-regression.mjs` dispatches to M29 eval handler; `npm test` exits 0 with `Eval regression harness passed (16 scenarios)`; `python3 -c "import json,sys; d=json.load(sys.stdin); print(len(d['scenarios']))"` on the JSON file → 16 | PASS | 2026-04-25 (eval + npm test) | PASS |
| REQ-124 | M29 documentation: `website/docs/manifest-format.md` has `auth_requirements` section documenting 7-value enum, 6-rule table, zero-evidence guard, digest-flip note (AC-5), AC-7 compile/serve invariant, and Constraint 22 framing boundary; `website/docs/cli-reference.md` documents `--auth-scheme <scheme>` on `tusq review` with legal values, exit-1 semantics, and AND-style intersection note | `website/docs/manifest-format.md` lines 250–299: full M29 section including enum table, decision table, zero-evidence guard, AC-5/AC-7 invariants, Constraint 22 framing; `website/docs/cli-reference.md` line 72: usage line includes `--auth-scheme <scheme>`; line 87: flag description with legal values and AND-style semantics | PASS | 2026-04-25 (docs inspection) | PASS |
| REQ-125 | M29 R2 eval regression: `auth-scheme-api-key-r2-precedence` — middleware matching `/api[_-]?key\|x-api-key/i` pattern produces `auth_scheme: 'api_key'` with `evidence_source: 'middleware_name'`; R2 fires after R1 zero-evidence guard passes; result present in eval regression harness (scenario 17 of 20) | `tests/evals/governed-cli-scenarios.json` id `auth-scheme-api-key-r2-precedence`; `synthetic_routes` has middleware `apiKeyAuth`; expected `auth_scheme: api_key`, `evidence_source: middleware_name`; `npm test` exits 0 with `Eval regression harness passed (20 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |
| REQ-126 | M29 R3 eval regression: `auth-scheme-session-r3-precedence` — middleware matching `/session\|cookie\|passport-local/i` pattern produces `auth_scheme: 'session'` with `evidence_source: 'middleware_name'`; R3 fires only when R1–R2 do not match | `tests/evals/governed-cli-scenarios.json` id `auth-scheme-session-r3-precedence`; `synthetic_routes` has middleware `sessionAuth`; expected `auth_scheme: session`, `evidence_source: middleware_name`; `npm test` exits 0 with `Eval regression harness passed (20 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |
| REQ-127 | M29 R4 eval regression: `auth-scheme-basic-r4-precedence` — middleware matching `/basic[_-]?auth/i` pattern produces `auth_scheme: 'basic'` with `evidence_source: 'middleware_name'`; R4 fires only when R1–R3 do not match | `tests/evals/governed-cli-scenarios.json` id `auth-scheme-basic-r4-precedence`; `synthetic_routes` has middleware `basicAuth`; expected `auth_scheme: basic`, `evidence_source: middleware_name`; `npm test` exits 0 with `Eval regression harness passed (20 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |
| REQ-128 | M29 R5 eval regression: `auth-scheme-oauth-r5-precedence` — middleware matching `/oauth\|oidc\|openid/i` pattern produces `auth_scheme: 'oauth'` with `evidence_source: 'middleware_name'`; R5 fires only when R1–R4 do not match; total eval harness = 20 scenarios | `tests/evals/governed-cli-scenarios.json` id `auth-scheme-oauth-r5-precedence`; `synthetic_routes` has middleware `oauthMiddleware`; expected `auth_scheme: oauth`, `evidence_source: middleware_name`; `npm test` exits 0 with `Eval regression harness passed (20 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |

**M26 recovered QA verification:** `npm test` was re-run after the runtime recovery and exited 0 with `Smoke tests passed` and `Eval regression harness passed (9 scenarios)`. No M26 acceptance item remains open.

**M27 QA verification:** `npm test` was re-run on HEAD d242727 and exited 0 with `Smoke tests passed` and `Eval regression harness passed (10 scenarios)`. `node bin/tusq.js help` and `node bin/tusq.js redaction review --help` both exited 0 and confirmed the redaction noun/help surface.

**turn_430f9b5d0f850456 re-verification (2026-04-24, HEAD 4ed3270):** `npm test` exits 0 with `Smoke tests passed` and `Eval regression harness passed (10 scenarios)`. CLI surface confirmed: 13 commands, `redaction` at position 11, `redaction review --help` shows three-flag surface (`--manifest`, `--capability`, `--json`) and reviewer-aid framing. All 108 criteria remain PASS.

| REQ-129 | M30 CLI surface: `tusq surface` enumerator and `tusq surface plan` command exist; CLI surface grows 13→14; `tusq surface plan --help` exits 0 with planning-aid framing callout | `node bin/tusq.js help` exits 0; stdout includes `surface`; `node bin/tusq.js surface plan --help` exits 0; stdout includes `planning aid`; `npm test` exits 0 | PASS | 2026-04-26 (smoke + npm test) | PASS |
| REQ-130 | `tusq surface plan` with default (all) exits 0 and emits all four surface sections with planning-aid framing | Smoke M30(a): default run emits all four surfaces (chat, palette, widget, voice) and includes `planning aid` string; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-131 | `--surface <name>` filter emits only the requested surface section (chat, palette, widget, voice each tested independently) | Smoke M30(b): `--surface chat` stdout includes `chat` section only; same for palette, widget, voice; each exits 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-132 | `--surface all` emits all four surfaces in frozen canonical order: chat, palette, widget, voice | Smoke M30(c): `--surface all --json` → surfaces array order is `['chat','palette','widget','voice']` exactly | PASS | 2026-04-26 (smoke) | PASS |
| REQ-133 | Unknown `--surface` value exits 1 with `Unknown surface: <value>` on stderr and empty stdout | Smoke M30(d): `--surface email` exits 1; stderr includes `Unknown surface: email`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-134 | Missing `--manifest` path exits 1 with `Manifest not found:` on stderr and empty stdout | Smoke M30(e): non-existent path exits 1; stderr includes `Manifest not found:`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-135 | Malformed JSON manifest exits 1 with `Invalid manifest JSON:` on stderr and empty stdout | Smoke M30(f): bad-JSON file exits 1; stderr includes `Invalid manifest JSON:`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-136 | Missing capabilities array exits 1 with `Invalid manifest: missing capabilities array` on stderr and empty stdout; unknown flag exits 1 with `Unknown flag: <flag>` on stderr and empty stdout | Smoke M30: no-caps manifest exits 1 with correct message; `--badFlag` exits 1 with `Unknown flag: --badFlag`; both have empty stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-137 | Deterministic output: byte-identical human and JSON plan output across repeated runs on the same manifest | Smoke M30(g): two consecutive human-plan runs produce identical stdout; two consecutive JSON-plan runs produce identical stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-138 | Read-only invariants: manifest mtime/content unchanged after plan run; capability_digest does not flip on any capability after plan run | Smoke M30(h)(i): mtime before/after plan run matches; capability_digest for every capability is the same before and after a plan run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-139 | Empty-capabilities manifest exits 0 with documented line `No capabilities in manifest — nothing to plan.`; JSON mode emits `surfaces: []` | Smoke M30(l): human output matches the documented exact line; JSON output has `surfaces` as empty array; both exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-140 | `--out <path>` writes plan JSON to file and emits no stdout on success; file contains all four surfaces; `--out` to unwritable path exits 1 with empty stdout; `--out .tusq/<path>` rejected with `--out path must not be inside .tusq/` on stderr and empty stdout | Smoke M30(m)(n) + .tusq/ guard: `--out` to valid path produces empty stdout and JSON file with 4 surfaces; `/no-such-dir/...` exits 1; `.tusq/plan.json` exits 1 with correct error message | PASS | 2026-04-26 (smoke) | PASS |
| REQ-141 | All gating reasons resolve to one of the six closed enum values: unapproved, restricted_sensitivity, confidential_sensitivity, destructive_side_effect, auth_scheme_unknown, auth_scheme_oauth_pending_v2; synchronous throw guard on out-of-set return | Smoke M30(o): every `gated_capabilities[].reason` value is in the frozen six-value set across all four surface sections of the JSON plan | PASS | 2026-04-26 (smoke) | PASS |
| REQ-142 | Per-surface gate logic: destructive verb is gated for palette/voice; widget splits into action_widgets (destructive) and insight_widgets (read/non-destructive); approved bearer-auth read capability eligible for chat | Smoke M30(p): `list_users` is in chat eligible_capabilities; `delete_user` is gated in palette; `delete_user` is eligible for widget and in action_widgets; `list_users` is in insight_widgets for widget | PASS | 2026-04-26 (smoke) | PASS |
| REQ-143 | `brand_inputs_required` per-surface frozen named lists: chat=['brand.tone','brand.color_primary','brand.color_secondary','brand.font_family'], voice=['brand.tone','voice.persona','voice.greeting'] | Smoke M30: `m30ChatSection.brand_inputs_required` JSON-stringified equals expected chat list; `m30VoiceSection.brand_inputs_required` JSON-stringified equals expected voice list | PASS | 2026-04-26 (smoke) | PASS |
| REQ-144 | Compile byte-identity: `tusq surface plan` does not write any files to `tusq-tools/` or affect compile output | Smoke M30: after running `tusq surface plan`, `tusq-tools/` file list contains no file with `surface` in name; compile output files are unchanged | PASS | 2026-04-26 (smoke) | PASS |
| REQ-145 | M30 eval regression: `surface-plan-determinism` scenario (21st scenario) — byte-identical JSON across 3 runs; all gated_reason values are from the closed six-value enum | `tests/evals/governed-cli-scenarios.json` id `surface-plan-determinism`; `npm test` exits 0 with `Eval regression harness passed (21 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |
| REQ-146 | M31 CLI surface: `tusq domain` enumerator and `tusq domain index` command exist; CLI surface grows 14→15; `domain` inserted alphabetically between `diff` and `policy`; `tusq domain index --help` exits 0 with planning-aid framing callout | `node bin/tusq.js help` exits 0; stdout lists `domain` between `diff` and `policy`; `node bin/tusq.js domain index --help` exits 0; stdout includes `planning aid` | PASS | 2026-04-26 (smoke + npm test) | PASS |
| REQ-147 | `tusq domain index` default (all-domain) run produces exit 0 and per-domain entries in manifest first-appearance order with all three domain types (named + unknown) | Smoke M31(a): default run includes `[users]`, `[billing]`, `[unknown]` buckets; buckets appear in manifest first-appearance order; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-148 | `--domain <name>` filter emits only the specified named domain bucket | Smoke M31(b): `--domain users` stdout includes `[users]` but not `[billing]` or `[unknown]`; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-149 | `--domain unknown` filter emits only the unknown bucket | Smoke M31(b): `--domain unknown` stdout includes `[unknown]` but not `[users]` or `[billing]`; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-150 | Unknown `--domain` value exits 1 with `Unknown domain: <value>` on stderr and empty stdout | Smoke M31(c): `--domain bogus` exits 1; stderr includes `Unknown domain: bogus`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-151 | Missing `--manifest` path exits 1 with `Manifest not found:` on stderr and empty stdout | Smoke M31(d): non-existent manifest path exits 1; stderr includes `Manifest not found:`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-152 | Malformed JSON manifest exits 1 with `Invalid manifest JSON:` on stderr and empty stdout | Smoke M31(e): bad-JSON file exits 1; stderr includes `Invalid manifest JSON:`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-153 | Missing capabilities array exits 1 with `Invalid manifest: missing capabilities array` on stderr and empty stdout | Smoke M31: no-caps manifest exits 1; stderr includes `Invalid manifest: missing capabilities array`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-154 | Unknown flag exits 1 with `Unknown flag: <flag>` on stderr and empty stdout | Smoke M31: `--badFlag` exits 1; stderr includes `Unknown flag: --badFlag`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-155 | Deterministic output: byte-identical human and JSON domain index output across repeated runs on the same manifest | Smoke M31(f): two consecutive human-index runs produce identical stdout; two consecutive JSON-index runs produce identical stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-156 | Read-only invariant: manifest mtime/content unchanged after domain index run | Smoke M31(g): manifest bytes before and after index run are identical (byte-for-byte) | PASS | 2026-04-26 (smoke) | PASS |
| REQ-157 | `capability_digest` does not flip on any capability after a domain index run | Smoke M31(h): digest values for every capability are identical before and after a domain index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-158 | Empty-capabilities manifest exits 0 with documented human line `No capabilities in manifest — nothing to index.`; JSON mode emits `domains: []` | Smoke M31(l): human output trim equals the exact documented line; JSON output has `domains` as empty array; both exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-159 | `--out <path>` writes index JSON to file and emits no stdout on success; file contains all three domain entries | Smoke M31(m): `--out` to valid path produces empty stdout; written file contains `domains` array with 3 entries; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-160 | `--out` to an unwritable path exits 1 with empty stdout | Smoke M31(n): `/no-such-dir/a/b/c/index.json` exits 1; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-161 | `--out .tusq/<path>` rejected with `--out path must not be inside .tusq/` on stderr and empty stdout | Smoke M31(o): `.tusq/index.json` exits 1; stderr includes correct rejection message; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-162 | Closed two-value `aggregation_key` enum (`domain \| unknown`); synchronous throw guard fires on out-of-set value; every emitted bucket has exactly one of the two values | Smoke M31(q): every `aggregation_key` in `domains[]` is in `{domain, unknown}`; named domains have `aggregation_key: 'domain'`; unknown bucket has `aggregation_key: 'unknown'` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-163 | Manifest first-appearance ordering rule: named domain buckets appear in the order their first capability appears in `capabilities[]`; unknown bucket is always appended last regardless of position | Smoke M31(p): `unknown` bucket is the last entry in `domains[]`; `no_domain_route` (domain: null) is in the unknown bucket; bucket order is users → billing → unknown matching the fixture | PASS | 2026-04-26 (smoke) | PASS |
| REQ-164 | Per-domain entry shape: exactly 8 fields present (`domain`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`, `has_unknown_auth`) | `node bin/tusq.js domain index --json` output has `domains[0]` with all 9 declared fields (8 per-domain fields + domain name); capability names listed as strings in `capabilities[]` | PASS | 2026-04-26 (manual JSON verification) | PASS |
| REQ-165 | `tusq compile` output byte-identical pre and post-`tusq domain index`; domain index does not write into `tusq-tools/` | Smoke M31(i): compile output file content is identical before and after a domain index run; `tusq-tools/` contains no file with `domain` in name after index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-166 | `tusq surface plan` output byte-identical pre and post-`tusq domain index` | Smoke M31(j): surface plan JSON stdout is identical before and after a domain index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-167 | M31 eval regression: `domain-index-determinism` scenario (22nd scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (22 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `domain-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (22 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |
| REQ-168 | M32 CLI surface: `tusq effect` enumerator and `tusq effect index` command exist; CLI surface grows 15→16; `effect` inserted alphabetically between `domain` and `policy`; `tusq effect index --help` exits 0 with planning-aid framing callout | `node bin/tusq.js help` exits 0; stdout lists `effect` between `domain` and `policy`; `node bin/tusq.js effect index --help` exits 0; stdout includes `planning aid` | PASS | 2026-04-26 (smoke + npm test) | PASS |
| REQ-169 | `tusq effect index` default (all-effect) run produces exit 0 and per-bucket entries in closed-enum order (read → write → destructive → unknown) with planning-aid framing callout | Smoke M32(a): default run includes all four buckets; buckets appear in closed-enum iteration order; stdout includes `planning aid` framing; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-170 | `--effect <read\|write\|destructive\|unknown>` filter emits only the specified bucket; all four values produce one matching entry | Smoke M32(b): each of `--effect read`, `--effect write`, `--effect destructive`, `--effect unknown` emits only the targeted bucket and exits 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-171 | Unknown `--effect` value exits 1 with `Unknown effect: <value>` on stderr and empty stdout | Smoke M32(c): `--effect bogus` exits 1; stderr includes `Unknown effect: bogus`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-172 | Missing `--manifest` path exits 1 with `Manifest not found:` on stderr and empty stdout | Smoke M32(d): non-existent manifest path exits 1; stderr includes `Manifest not found:`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-173 | Malformed JSON manifest exits 1 with `Invalid manifest JSON:` on stderr and empty stdout | Smoke M32(e): bad-JSON file exits 1; stderr includes `Invalid manifest JSON:`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-174 | Missing capabilities array exits 1 with `Invalid manifest: missing capabilities array` on stderr and empty stdout | Smoke M32: no-caps manifest exits 1; stderr includes `Invalid manifest: missing capabilities array`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-175 | Unknown flag exits 1 with `Unknown flag: <flag>` on stderr and empty stdout | Smoke M32: `--badFlag` exits 1; stderr includes `Unknown flag: --badFlag`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-176 | Deterministic output: byte-identical human and JSON effect index output across repeated runs on the same manifest | Smoke M32(f): two consecutive human-index runs produce identical stdout; two consecutive JSON-index runs produce identical stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-177 | Read-only invariant: manifest mtime/content unchanged after effect index run | Smoke M32(g): manifest bytes before and after index run are identical (byte-for-byte) | PASS | 2026-04-26 (smoke) | PASS |
| REQ-178 | `capability_digest` does not flip on any capability after an effect index run | Smoke M32(h): digest values for every capability are identical before and after an effect index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-179 | `tusq compile` output byte-identical pre and post-`tusq effect index`; effect index does not write into `tusq-tools/` | Smoke M32(i): compile output file content is identical before and after an effect index run; `tusq-tools/` contains no file with `effect` in name after index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-180 | `tusq surface plan` and `tusq domain index` outputs byte-identical pre and post-`tusq effect index` | Smoke M32(j): surface plan JSON stdout and domain index JSON stdout are identical before and after an effect index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-181 | Empty-capabilities manifest exits 0 with documented human line `No capabilities in manifest — nothing to index.`; JSON mode emits `effects: []` | Smoke M32(l): human output trim equals the exact documented line; JSON output has `effects` as empty array; both exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-182 | `--out <path>` writes index JSON to file and emits no stdout on success; file contains all four effect bucket entries | Smoke M32(m): `--out` to valid path produces empty stdout; written file contains `effects` array with 4 entries; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-183 | `--out` to an unwritable path exits 1 with empty stdout | Smoke M32(n): `/no-such-dir/a/b/c/index.json` exits 1; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-184 | `--out .tusq/<path>` rejected with `--out path must not be inside .tusq/` on stderr and empty stdout | Smoke M32(o): `.tusq/index.json` exits 1; stderr includes correct rejection message; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-185 | Unknown bucket appended last regardless of manifest position; capability with `side_effect_class: null` is bucketed into `unknown` | Smoke M32(p): `unknown` bucket is last entry in `effects[]`; `no_class_route` (side_effect_class: null) is in the unknown bucket | PASS | 2026-04-26 (smoke) | PASS |
| REQ-186 | Closed four-value `side_effect_class` enum (`read \| write \| destructive \| unknown`) and closed two-value `aggregation_key` enum (`class \| unknown`); synchronous throw guards fire on out-of-set values; named buckets have `aggregation_key: 'class'`; unknown bucket has `aggregation_key: 'unknown'` | Smoke M32(q): every `aggregation_key` in `effects[]` is in `{class, unknown}`; named class 'read' has `aggregation_key: 'class'`; unknown bucket has `aggregation_key: 'unknown'` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-187 | Empty buckets MUST NOT appear in output; closed-enum iteration order (read → write → destructive → unknown) with only non-empty buckets emitted; read-only manifest produces exactly one bucket | Smoke M32(r): default fixture produces exactly 4 non-empty buckets in order (read, write, destructive, unknown); read-only manifest produces exactly one bucket [read]; no extra or empty buckets emitted | PASS | 2026-04-26 (smoke) | PASS |
| REQ-188 | Per-bucket 8-field entry shape: `side_effect_class`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`, `has_unknown_auth`; within each bucket, capability names follow manifest declared order (NOT alphabetized) | Smoke M32(s): within read bucket, capabilities follow manifest declared order (list_users, get_status); per-bucket shape carries all 8 fields | PASS | 2026-04-26 (smoke) | PASS |
| REQ-189 | `has_restricted_or_confidential_sensitivity` and `has_unknown_auth` flag values are correct per bucket; `has_restricted_or_confidential_sensitivity=true` for write bucket (confidential capability); `has_unknown_auth=true` for read bucket (unknown-auth capability); values `false` on non-matching buckets | Smoke M32(t) + M32(u): write bucket `has_restricted_or_confidential_sensitivity=true`; read bucket `has_restricted_or_confidential_sensitivity=false`; read bucket `has_unknown_auth=true`; write bucket `has_unknown_auth=false` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-190 | M32 `tusq effect index --help` exits 0; unknown subcommand exits 1 with `Unknown subcommand:` on stderr and empty stdout | Smoke M32: `effect index --help` exits 0 with planning-aid framing; `effect bogus` exits 1; stderr includes `Unknown subcommand:`; stdout is empty | PASS | 2026-04-26 (smoke) | PASS |
| REQ-191 | M32 eval regression: `effect-index-determinism` scenario (23rd scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (23 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `effect-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (23 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |
| REQ-192 | M33 CLI surface: `tusq sensitivity` enumerator and `tusq sensitivity index` command exist; CLI surface grows 16→17; `sensitivity` inserted alphabetically between `redaction` and `surface`; `tusq sensitivity index --help` exits 0 with planning-aid framing callout `This is a planning aid, not a runtime sensitivity enforcer or compliance certifier.` and bucket iteration order `public → internal → confidential → restricted → unknown` | `node bin/tusq.js help` exits 0; stdout lists `sensitivity` between `redaction` and `surface`; `node bin/tusq.js sensitivity index --help` exits 0; stdout includes `planning aid` and correct bucket order | PASS | 2026-04-26 (smoke + npm test) | PASS |
| REQ-193 | `tusq sensitivity index` default (all-sensitivity) run produces exit 0 and per-bucket entries in closed-enum order (`public → internal → confidential → restricted → unknown`) with planning-aid framing callout | Smoke M33(a): default run includes all five buckets; buckets appear in closed-enum iteration order; stdout includes `planning aid` framing; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-194 | `--sensitivity <public\|internal\|confidential\|restricted\|unknown>` filter emits only the specified bucket; all five values each produce exactly one matching entry | Smoke M33(b): each of `--sensitivity public`, `--sensitivity internal`, `--sensitivity confidential`, `--sensitivity restricted`, `--sensitivity unknown` emits only the targeted bucket and exits 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-195 | Unknown `--sensitivity` value exits 1 with `Unknown sensitivity` on stderr and empty stdout | Smoke M33(c): `--sensitivity bogus` exits 1; stderr includes `Unknown sensitivity`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-196 | Missing `--manifest` path exits 1 with `Manifest not found:` on stderr and empty stdout | Smoke M33(d): non-existent manifest path exits 1; stderr includes `Manifest not found:`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-197 | Malformed JSON manifest exits 1 with `Invalid manifest JSON:` on stderr and empty stdout | Smoke M33(e): bad-JSON file exits 1; stderr includes `Invalid manifest JSON:`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-198 | Missing capabilities array exits 1 with `Invalid manifest: missing capabilities array` on stderr and empty stdout | Smoke M33: no-caps manifest exits 1; stderr includes `Invalid manifest: missing capabilities array`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-199 | Unknown flag exits 1 with `Unknown flag: <flag>` on stderr and empty stdout | Smoke M33: `--badFlag` exits 1; stderr includes `Unknown flag: --badFlag`; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-200 | Deterministic output: byte-identical human and JSON sensitivity index output across repeated runs on the same manifest | Smoke M33(f): two consecutive human-index runs produce identical stdout; two consecutive JSON-index runs produce identical stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-201 | Read-only invariant: manifest mtime/content unchanged after sensitivity index run | Smoke M33(g): manifest bytes before and after index run are identical (byte-for-byte) | PASS | 2026-04-26 (smoke) | PASS |
| REQ-202 | `capability_digest` does not flip on any capability after a sensitivity index run | Smoke M33(h): digest values for every capability are identical before and after a sensitivity index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-203 | `tusq compile` output byte-identical pre and post-`tusq sensitivity index`; sensitivity index does not write into `tusq-tools/` | Smoke M33(i): compile output file content is identical before and after a sensitivity index run; `tusq-tools/` contains no file with `sensitivity` in name after index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-204 | `tusq surface plan`, `tusq domain index`, and `tusq effect index` outputs byte-identical pre and post-`tusq sensitivity index` | Smoke M33(j): surface plan JSON stdout, domain index JSON stdout, and effect index JSON stdout are all identical before and after a sensitivity index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-205 | Empty-capabilities manifest exits 0 with documented human line `No capabilities in manifest — nothing to index.`; JSON mode emits `sensitivities: []` | Smoke M33(l): human output trim equals the exact documented line; JSON output has `sensitivities` as empty array; both exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-206 | `--out <path>` writes index JSON to file and emits no stdout on success; file contains all five sensitivity bucket entries | Smoke M33(m): `--out` to valid path produces empty stdout; written file contains `sensitivities` array with 5 entries; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-207 | `--out` to an unwritable path exits 1 with empty stdout | Smoke M33(n): `/no-such-dir/a/b/c/index.json` exits 1; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-208 | `--out .tusq/<path>` rejected with `--out path must not be inside .tusq/` on stderr and empty stdout | Smoke M33(o): `.tusq/index.json` exits 1; stderr includes correct rejection message; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-209 | Unknown bucket appended last regardless of manifest position; capability with `sensitivity_class: null` is bucketed into `unknown` | Smoke M33(p): `unknown` bucket is last entry in `sensitivities[]`; `no_class_route` (sensitivity_class: null) is in the unknown bucket | PASS | 2026-04-26 (smoke) | PASS |
| REQ-210 | Closed two-value `aggregation_key` enum (`class \| unknown`); named buckets have `aggregation_key: 'class'`; unknown bucket has `aggregation_key: 'unknown'`; M33 references M28 SENSITIVITY_CLASSES constant directly — `_guardSensitivityBucketKey` uses `SENSITIVITY_CLASSES.includes()` (no independent enum redeclaration per M33 Key Risk) | Smoke M33(q): every `aggregation_key` in `sensitivities[]` is in `{class, unknown}`; named class 'public' has `aggregation_key: 'class'`; unknown bucket has `aggregation_key: 'unknown'`; `src/cli.js` uses `SENSITIVITY_CLASSES.includes()` for bucket-key guard | PASS | 2026-04-26 (smoke + code audit) | PASS |
| REQ-211 | Empty buckets MUST NOT appear in output; closed five-value `sensitivity_class` iteration order (`public → internal → confidential → restricted → unknown`) with only non-empty buckets emitted; public-only manifest produces exactly one bucket | Smoke M33(r): default fixture produces exactly 5 non-empty buckets in order (public, internal, confidential, restricted, unknown); public-only manifest produces exactly one bucket [public]; no extra or empty buckets emitted | PASS | 2026-04-26 (smoke) | PASS |
| REQ-212 | Per-bucket 8-field entry shape: `sensitivity_class`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_unknown_auth`; within each bucket, capability names follow manifest declared order (NOT alphabetized) | Smoke M33(s): within internal bucket, capabilities follow manifest declared order (list_users, internal_report); per-bucket shape carries all 8 fields | PASS | 2026-04-26 (smoke) | PASS |
| REQ-213 | `has_destructive_side_effect` flag correct per bucket; `has_destructive_side_effect=true` for confidential bucket (has a destructive side-effect capability); `has_destructive_side_effect=false` for public bucket (only read capabilities) | Smoke M33(t): confidential bucket `has_destructive_side_effect=true`; public bucket `has_destructive_side_effect=false` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-214 | `has_unknown_auth` flag correct per bucket; `has_unknown_auth=true` for public bucket (capability with `auth_scheme: unknown`); values `false` on non-matching buckets | Smoke M33(u): public bucket `has_unknown_auth=true`; verified against fixture capability with `auth_scheme: unknown` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-215 | M33 eval regression: `sensitivity-index-determinism` scenario (24th scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (24 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `sensitivity-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (24 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |
| REQ-216 | CLI surface grows from 17 → 18 commands with `method` inserted alphabetically between `effect` and `policy` (m-e < m-e-t alphabetically `effect` < `method` < `policy`) | `node bin/tusq.js help` → exit 0, 18 commands including `method` between `effect` and `policy` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-217 | Default `tusq method index` produces exit 0 with per-bucket entries in closed-enum order GET→POST→PATCH→DELETE→unknown; PUT absent when no capability has method: PUT; planning-aid framing present | Smoke M34(a): stdout includes [GET], [POST], [PATCH], [DELETE], [unknown] in that order; [PUT] absent; stdout includes 'planning aid'; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-218 | `--method GET/POST/PATCH/DELETE/unknown` each emit exactly one matching bucket; case-sensitive uppercase enforcement (lowercase `get` exits 1); `unknown` is a valid filter value when an unknown bucket exists | Smoke M34(b)(c)(c2): five filter values each produce exactly one bucket; `--method bogus` exits 1 stderr `Unknown method: bogus` empty stdout; `--method get` exits 1 stderr `Unknown method: get` empty stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-219 | Missing manifest exits 1 with `Manifest not found:` on stderr and empty stdout | Smoke M34(d): absent manifest path exits 1; stderr includes `Manifest not found:`; stdout is empty | PASS | 2026-04-26 (smoke) | PASS |
| REQ-220 | Malformed JSON manifest exits 1 with `Invalid manifest JSON:` on stderr and empty stdout | Smoke M34(e): non-JSON manifest content exits 1; stderr includes `Invalid manifest JSON:`; stdout is empty | PASS | 2026-04-26 (smoke) | PASS |
| REQ-221 | Invalid manifest missing `capabilities` array exits 1 with `Invalid manifest: missing capabilities array` on stderr and empty stdout | Smoke M34 edge: `{ schema_version: '1.0' }` manifest exits 1; stderr includes `Invalid manifest: missing capabilities array`; stdout is empty | PASS | 2026-04-26 (smoke) | PASS |
| REQ-222 | Unknown flag exits 1 with `Unknown flag: --<name>` on stderr and empty stdout | Smoke M34 edge: `--badFlag` exits 1; stderr includes `Unknown flag: --badFlag`; stdout is empty | PASS | 2026-04-26 (smoke) | PASS |
| REQ-223 | Deterministic output: byte-identical human and JSON method index output across repeated runs on the same manifest | Smoke M34(f): two consecutive human runs produce identical stdout; two consecutive JSON runs produce identical stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-224 | Read-only invariant: manifest content unchanged after method index run | Smoke M34(g): manifest bytes before and after index run are identical (byte-for-byte) | PASS | 2026-04-26 (smoke) | PASS |
| REQ-225 | `capability_digest` does not flip on any capability after a method index run | Smoke M34(h): digest values for every capability are identical before and after a method index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-226 | `tusq compile` output byte-identical pre and post-`tusq method index`; method index does not write into `tusq-tools/` | Smoke M34(i): compile output file content is identical before and after method index run; `tusq-tools/` contains no file with `method` in name after index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-227 | `tusq surface plan`, `tusq domain index`, `tusq effect index`, and `tusq sensitivity index` outputs byte-identical pre and post-`tusq method index` | Smoke M34(k): surface plan JSON, domain index JSON, effect index JSON, and sensitivity index JSON all identical before and after method index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-228 | Empty-capabilities manifest exits 0 with `No capabilities in manifest — nothing to index.` (human); JSON mode emits `methods: []` | Smoke M34(l): human output trim equals exact documented line; JSON output has `methods` as empty array; both exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-229 | `--out <path>` writes index JSON to file and emits no stdout on success; file contains ≥4 method bucket entries | Smoke M34(m): `--out` to valid path produces empty stdout; written file contains `methods` array with ≥4 entries; exit 0 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-230 | `--out` to unwritable path exits 1 with empty stdout | Smoke M34(n): `/no-such-dir/a/b/c/index.json` exits 1; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-231 | `--out .tusq/<path>` rejected with `--out path must not be inside .tusq/` on stderr and empty stdout | Smoke M34(o): `.tusq/index.json` exits 1; stderr includes correct rejection message; stdout is empty string | PASS | 2026-04-26 (smoke) | PASS |
| REQ-232 | Capabilities with method: null, missing, empty-string, HEAD, OPTIONS, or non-canonical values are bucketed into `unknown`; unknown bucket appended last | Smoke M34(p): `no_method_route` (method: null) in unknown bucket; unknown bucket is last entry in `methods[]`; OPTIONS method routes to unknown bucket | PASS | 2026-04-26 (smoke) | PASS |
| REQ-233 | Closed two-value `aggregation_key` enum (`method \| unknown`); named method buckets have `aggregation_key: 'method'`; unknown bucket has `aggregation_key: 'unknown'` | Smoke M34(q): every `aggregation_key` in `methods[]` is in `{method, unknown}`; named method 'GET' has `aggregation_key: 'method'`; unknown bucket has `aggregation_key: 'unknown'` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-234 | Empty buckets MUST NOT appear; closed six-value bucket iteration order GET→POST→PUT→PATCH→DELETE→unknown with only non-empty buckets emitted; GET-only manifest produces exactly one bucket | Smoke M34(r): default fixture produces non-empty buckets only in order; GET-only manifest produces exactly one bucket [GET]; no extra or empty buckets emitted | PASS | 2026-04-26 (smoke) | PASS |
| REQ-235 | Per-bucket 8-field entry shape: `http_method`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_unknown_auth`; within each bucket, capability names follow manifest declared order (NOT alphabetized) | Smoke M34(s): within GET bucket, capabilities follow manifest declared order (list_users, get_status, internal_report); per-bucket shape carries all 8 fields | PASS | 2026-04-26 (smoke) | PASS |
| REQ-236 | `has_destructive_side_effect` flag correct per bucket; `has_destructive_side_effect=true` for POST bucket (has a destructive side-effect capability); `has_destructive_side_effect=false` for PATCH bucket (write-only capabilities) | Smoke M34(t): POST bucket `has_destructive_side_effect=true`; PATCH bucket `has_destructive_side_effect=false` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-237 | `has_unknown_auth` flag correct per bucket; `has_unknown_auth=true` for GET bucket (capability with `auth_scheme: unknown`); `has_unknown_auth=false` for POST bucket (all bearer auth) | Smoke M34(u): GET bucket `has_unknown_auth=true`; POST bucket `has_unknown_auth=false` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-238 | M34 help text includes planning-aid framing: `This is a planning aid, not a runtime HTTP-method router, REST-convention validator, or idempotency classifier.` | Smoke M34 edge: `tusq method index --help` exits 0; stdout includes `planning aid` framing | PASS | 2026-04-26 (smoke) | PASS |
| REQ-239 | M34 eval regression: `method-index-determinism` scenario (25th scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (25 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `method-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (25 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |

**turn_28b0298523d838a3 M34 verification (run_bf8efb6b9c733000, 2026-04-26, HEAD ddfa687):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (25 scenarios)`. `node bin/tusq.js help` → exit 0, 18 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, domain, effect, method, policy, redaction, sensitivity, surface, version, help) with `method` between `effect` and `policy`. `node bin/tusq.js method index --help` → exit 0; includes `planning aid` framing; bucket iteration order `GET → POST → PUT → PATCH → DELETE → unknown`. `git diff HEAD -- package.json package-lock.json` → empty (zero new dependencies). `node bin/tusq.js method index --method get --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown method: get`, empty stdout (case-sensitive enforcement confirmed). Dev turn (turn_c530db27dd4d2941) implemented M34 across nine dev-owned files: `src/cli.js` (METHOD_INDEX_AGGREGATION_KEY_ENUM, METHOD_INDEX_BUCKET_ORDER, METHOD_INDEX_VALID_METHODS, cmdMethod, cmdMethodIndex, parseMethodIndexArgs, _guardMethodBucketKey, _guardMethodAggregationKey, buildMethodIndex, formatMethodIndex), `tests/smoke.mjs` (M34 assertions cases a-u plus edge cases), `tests/evals/governed-cli-scenarios.json` (method-index-determinism, count 24→25), `tests/eval-regression.mjs` (runMethodIndexDeterminismScenario), `.planning/SYSTEM_SPEC.md` (§ M34 + Constraint 27), `.planning/command-surface.md` (§ M34), `.planning/IMPLEMENTATION_NOTES.md`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`. OBJ-001 (medium, non-blocking) carried forward. OBJ-002 (low, non-blocking) carried forward. OBJ-003 (low, non-blocking) carried forward. REQ-216–REQ-239 added (24 new M34 criteria). All 239 acceptance criteria (REQ-001–REQ-239) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_a291b557e6bf6051 M33 re-verification (run_cd98cdad0fb83285, 2026-04-26, HEAD 0f4ecaa):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (24 scenarios)`. `node bin/tusq.js help` → exit 0, 17 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, domain, effect, policy, redaction, sensitivity, surface, version, help) with `sensitivity` between `redaction` and `surface`. `node bin/tusq.js sensitivity index --help` → exit 0; includes `planning aid` framing; bucket iteration order `public → internal → confidential → restricted → unknown`. `git diff HEAD -- src/ bin/ tests/ website/ package.json package-lock.json` → empty (zero source drift). `git diff 753cf31..0f4ecaa --name-only` → `.planning/IMPLEMENTATION_NOTES.md`, `.planning/PM_SIGNOFF.md`, `.planning/ROADMAP.md`, `.planning/SYSTEM_SPEC.md`, `.planning/command-surface.md` only. Dev turn (turn_0e359b277c048d1f) made zero source changes — confirmed M33 (V1.14) shipped from run_4506c41d74e23e8e; only `.planning/IMPLEMENTATION_NOTES.md` modified by dev. PM turn (turn_73d77d855d310a6c) reconciled 20 M33 ROADMAP checkboxes from `[ ]` to `[x]` as a stale-checkbox false positive from vision_scan (intent_1777238952808_4fd5, roadmap_open_work_detected). This is the sixth recurrence of the stale-checkbox pattern (M28→M30→M31→M32→M33→M33-again). No new scope shipped. OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation is correct for manually-edited manifests. OBJ-002 (low, non-blocking) carried forward: surface-plan-determinism eval uses synthetic_capabilities. OBJ-003 (low, non-blocking) carried forward: M31 flag value assertion gap non-blocking; M32/M33 close their own analogous flags at REQ-189/REQ-213/REQ-214. All 215 acceptance criteria (REQ-001–REQ-215) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_76e3bd05609910ce M33 verification (run_4506c41d74e23e8e, 2026-04-26, HEAD 31a0bd9):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (24 scenarios)`. `node bin/tusq.js help` → exit 0, 17 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, domain, effect, policy, redaction, sensitivity, surface, version, help) with `sensitivity` between `redaction` and `surface`. `node bin/tusq.js sensitivity index --help` → exit 0; includes `planning aid` framing; bucket iteration order `public → internal → confidential → restricted → unknown`. `git diff HEAD -- package.json package-lock.json` → empty (zero new dependencies). `node bin/tusq.js sensitivity index --sensitivity bogus` → exit 1, stderr `Unknown sensitivity`, empty stdout. Dev turn (turn_42c7748a59fa5ef3) implemented M33 across nine dev-owned files: `src/cli.js` (SENSITIVITY_INDEX_AGGREGATION_KEY_ENUM frozen two-value Set, SENSITIVITY_INDEX_BUCKET_ORDER frozen array ['public','internal','confidential','restricted'], cmdSensitivity enumerator, cmdSensitivityIndex handler with detection-before-output and --out .tusq/ rejection and --sensitivity filter using SENSITIVITY_CLASSES.includes() per M33 Key Risk, parseSensitivityIndexArgs 4-flag parser, _guardSensitivityBucketKey using SENSITIVITY_CLASSES.includes directly, _guardSensitivityAggregationKey, buildSensitivityIndex, formatSensitivityIndex), `tests/smoke.mjs` (21 M33 assertions cases a-u plus edge cases), `tests/evals/governed-cli-scenarios.json` (sensitivity-index-determinism, eval count 23→24), `tests/eval-regression.mjs` (runSensitivityIndexDeterminismScenario), `.planning/SYSTEM_SPEC.md` (§ M33 + Constraint 26), `.planning/command-surface.md` (§ M33 CLI Surface), `.planning/IMPLEMENTATION_NOTES.md`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`. OBJ-001 (medium, non-blocking) carried forward: R6 dead code. OBJ-002 (low, non-blocking) carried forward: surface-plan-determinism eval uses synthetic_capabilities. OBJ-003 (low, non-blocking) carried forward: M31 flag value assertion gap non-blocking; M32/M33 close their own analogous flags at REQ-189/REQ-213/REQ-214. REQ-192–REQ-215 added (24 new M33 criteria). All 215 acceptance criteria (REQ-001–REQ-215) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_5ae9cd7ed8e985f1 M32 re-verification (run_7183d8c70482329b, 2026-04-26, HEAD 0a38455):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (23 scenarios)`. `node bin/tusq.js help` → exit 0, 16 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, domain, effect, policy, redaction, surface, version, help) with `effect` between `domain` and `policy`. `node bin/tusq.js effect index --help` → exit 0; includes `planning aid` framing; bucket iteration order `read → write → destructive → unknown`. `git diff HEAD -- src/ bin/ tests/ website/ package.json package-lock.json` → empty (zero source drift). Dev turn (turn_02b2c0e4dceeead3) made zero source changes — confirmed M32 (V1.13) shipped from run_ae841429202c5bb7; only `.planning/IMPLEMENTATION_NOTES.md` modified (`git diff 5dca9a8..0a38455 --name-only` confirms single file). PM turn (turn_f69f5083f63ad652) reconciled 20 M32 ROADMAP checkboxes from `[ ]` to `[x]` as a stale-checkbox false positive from vision_scan (intent_1777236173495_2303, roadmap_open_work_detected). This is the fifth recurrence of the stale-checkbox pattern (M28/M30/M31/M32/M32-again). No new scope shipped. OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation is correct for manually-edited manifests. OBJ-002 (low, non-blocking) carried forward: surface-plan-determinism eval uses synthetic_capabilities. OBJ-003 (low, non-blocking) carried forward: M31 flag value assertion gap non-blocking; M32 closes its own analogous gap at REQ-189. All 191 acceptance criteria (REQ-001–REQ-191) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_131a847ada2fad7c M32 verification (run_ae841429202c5bb7, 2026-04-26, HEAD e9d2d10):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (23 scenarios)`. `node bin/tusq.js help` → exit 0, 16 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, domain, effect, policy, redaction, surface, version, help) with `effect` between `domain` and `policy`. `node bin/tusq.js effect index --help` → exit 0; includes `planning aid` framing; bucket iteration order `read → write → destructive → unknown`. `git diff HEAD -- package.json package-lock.json` → empty (zero new dependencies). Dev turn (turn_18e3b3d9ce515cf1) implemented M32 across nine dev-owned files: `src/cli.js` (EFFECT_INDEX_SIDE_EFFECT_CLASS_ENUM 4-value frozen Set, EFFECT_INDEX_AGGREGATION_KEY_ENUM 2-value frozen Set, EFFECT_INDEX_BUCKET_ORDER frozen array, cmdEffect enumerator, cmdEffectIndex handler, parseEffectIndexArgs, _guardEffectBucketKey, _guardEffectAggregationKey, buildEffectIndex, formatEffectIndex, dispatch/printHelp/printCommandHelp wiring), `tests/smoke.mjs` (21 M32 assertions cases a-u plus edge cases), `tests/evals/governed-cli-scenarios.json` (effect-index-determinism, count 22→23), `tests/eval-regression.mjs` (runEffectIndexDeterminismScenario), `.planning/SYSTEM_SPEC.md` (§ M32 + Constraint 25), `.planning/command-surface.md` (§ M32 CLI Surface), `.planning/IMPLEMENTATION_NOTES.md`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`. OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation is correct for manually-edited manifests. OBJ-002 (low, non-blocking) carried forward: surface-plan-determinism eval uses synthetic_capabilities. OBJ-003 (low, non-blocking) carried forward (formerly OBJ-002-M31): M31 flag value assertion gap remains non-blocking. M32 explicitly closes this gap for its own flags (REQ-189). REQ-168–REQ-191 added (24 new M32 criteria). All 191 acceptance criteria (REQ-001–REQ-191) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_21e6a71b08014338 M31 re-verification (run_25308eabf162ba8b, 2026-04-26, HEAD 8c80a26):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (22 scenarios)`. `node bin/tusq.js help` → exit 0, 15 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, domain, policy, redaction, surface, version, help) with `domain` between `diff` and `policy`. `node bin/tusq.js domain index --help` → exit 0; includes `planning aid` framing. `git diff HEAD -- src/ bin/ tests/ website/ package.json package-lock.json` → empty (zero source drift). Dev turn (turn_2a28545f82ce5e1b) made zero source changes — confirmed M31 (V1.12) shipped from run_e40832d436a42d75; only `.planning/IMPLEMENTATION_NOTES.md` modified (git diff 67d96ae..8c80a26 confirms single file). PM turn (turn_cf57b41e62c05af6) reconciled 19 M31 ROADMAP checkboxes from `[ ]` to `[x]` as a stale-checkbox false positive from vision_scan (intent_1777233493634_b5f9, roadmap_open_work_detected). No new scope shipped. OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation is correct for manually-edited manifests. OBJ-001-M30 (low, non-blocking) carried forward: surface-plan-determinism eval uses synthetic_capabilities. OBJ-002-M31 (low, non-blocking) carried forward: flag value assertions (`has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`, `has_unknown_auth`) not independently smoke-asserted for correctness; deferred. All 167 acceptance criteria (REQ-001–REQ-167) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_4125be3cf057395a M31 verification (run_e40832d436a42d75, 2026-04-26, HEAD 33eaa87):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (22 scenarios)`. `node bin/tusq.js help` → exit 0, 15 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, domain, policy, redaction, surface, version, help) with `domain` between `diff` and `policy`. `node bin/tusq.js domain index --help` → exit 0; includes `planning aid` framing. `git diff HEAD -- package.json package-lock.json` → empty (zero new dependencies). JSON shape verified: `domains[]` entries carry all 8 per-domain fields with correct counter logic. Dev turn (turn_59bd0fdb1abd4a32) implemented M31 across nine dev-owned files: `src/cli.js` (DOMAIN_INDEX_AGGREGATION_KEY_ENUM, _guardAggregationKey, cmdDomain, cmdDomainIndex, parseDomainIndexArgs, buildDomainIndex, formatDomainIndex, dispatch/printHelp/printCommandHelp wiring), `tests/smoke.mjs` (17 M31 assertions cases a-q), `tests/evals/governed-cli-scenarios.json` (domain-index-determinism, count 21→22), `tests/eval-regression.mjs` (runDomainIndexDeterminismScenario), `.planning/SYSTEM_SPEC.md` (§ M31 + Constraint 29), `.planning/command-surface.md` (§ M31 CLI Surface), `.planning/IMPLEMENTATION_NOTES.md`, `website/docs/cli-reference.md`, `website/docs/manifest-format.md`. OBJ-001 (medium, non-blocking) carried forward: R6 dead code. OBJ-001-M30 (low, non-blocking) carried forward: synthetic_capabilities in surface-plan-determinism eval. OBJ-002-M31 (low, non-blocking) raised: flag value assertions (`has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`, `has_unknown_auth`) not independently smoke-tested for correctness — only for presence via output shape; determinism scenario covers value stability but not specific counter correctness. REQ-146–REQ-167 added (22 new M31 criteria). All 167 acceptance criteria (REQ-001–REQ-167) pass. Ship verdict: SHIP. Phase transition to launch (auto_approve policy).

**turn_98d87515fa014a92 M30 re-verification (run_7894753f9c47c8e3, 2026-04-26, HEAD e67c9d5):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (21 scenarios)`. `node bin/tusq.js help` → exit 0, 14 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, policy, redaction, surface, version, help). `node bin/tusq.js surface plan --help` → exit 0; includes `planning aid` framing. `git diff HEAD -- src/ bin/ tests/ website/ package.json package-lock.json` → empty (zero source drift). Challenged dev turn (turn_f766c529523ce892) — zero source changes; M30 already shipped at V1.11 from run_24ccd92f593d8647; only `.planning/IMPLEMENTATION_NOTES.md` modified this run cycle. PM turn (turn_b2926375e6189caf) correctly reconciled 21 M30 ROADMAP checkboxes from stale `[ ]` to `[x]` (stale-checkbox false positive from vision_scan). OBJ-001 (medium, non-blocking) carried forward: R6 dead code. OBJ-001-M30 (low, non-blocking) carried forward: synthetic_capabilities in eval. All 145 acceptance criteria (REQ-001–REQ-145) pass. Ship verdict: SHIP. Phase transition to launch (auto_approve policy).

**turn_5584c661462c5226 re-verification (attempt 2, 2026-04-25, HEAD 6b2cc50):** `npm test` exits 0 with `Smoke tests passed` and `Eval regression harness passed (10 scenarios)`. `node bin/tusq.js help` exits 0, 13 commands, `redaction` at position 11. git diff 7756d19..6b2cc50 confirms only `.planning/IMPLEMENTATION_NOTES.md` changed — zero source/test/QA-owned artifact drift. All 108 acceptance criteria (REQ-001–REQ-108) remain PASS. This attempt 2 modifies all three QA-owned gate artifacts (acceptance-matrix.md, ship-verdict.md, RELEASE_NOTES.md) per the rejection feedback from attempt 1.

**turn_a769aa550ba55c00 re-verification (attempt 8, 2026-04-25, HEAD 524520f):** `npm test` exits 0 with `Smoke tests passed` and `Eval regression harness passed (10 scenarios)`. `node bin/tusq.js help` exits 0, 13 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, policy, redaction, version, help), `redaction` at position 11. `git diff HEAD~1..HEAD --name-only` confirms only `.planning/IMPLEMENTATION_NOTES.md` changed in prior dev turn — zero source/test/QA-owned artifact drift. All 108 acceptance criteria (REQ-001–REQ-108) remain PASS. Ship verdict: SHIP. Gate artifacts updated per auto_approve policy; phase_transition_request: launch.

**turn_9c2522b83d39efec M29 re-verification (run_8fe3b8b418dc589c, 2026-04-25, HEAD 3074ee5):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (16 scenarios)`. `node bin/tusq.js help` → exit 0, 13 commands. `node bin/tusq.js review --help` → includes `--auth-scheme <scheme>` and `--sensitivity <class>`. `git diff HEAD --stat -- src/ bin/ tests/ website/` → no output (zero source drift). Challenged dev turn (turn_0528de27fb8f6d22) — source-analysis-only with only `.planning/IMPLEMENTATION_NOTES.md` changed; M29 already on HEAD from prior run. OBJ-001 (medium, non-blocking) carried forward: R6 dead code in automated pipeline. All 124 acceptance criteria (REQ-001–REQ-124) pass. Ship verdict: SHIP. Phase transition to launch (auto_approve policy).

**turn_c72ee10c438066e0 M29 re-verification (run_44a179ccf81697c3, 2026-04-25, HEAD bc5e2fe):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (16 scenarios)`. `node bin/tusq.js help` → exit 0, 13 commands. `node bin/tusq.js review --help` → includes `--auth-scheme <scheme>` and `--sensitivity <class>`. `git diff HEAD --stat -- src/ bin/ tests/ website/` → no output (zero source drift). Challenged dev turn (turn_91da85658fdfe27c) — source-analysis-only with only `.planning/IMPLEMENTATION_NOTES.md` changed; M29 already on HEAD from prior run. OBJ-001 (medium, non-blocking) carried forward: R6 dead code in automated pipeline. All 124 acceptance criteria (REQ-001–REQ-124) pass. Ship verdict: SHIP. Phase transition to launch (auto_approve policy).

**turn_f01a675bc13a2594 M29 QA verification (2026-04-25, HEAD 2ba4452 + working tree):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (16 scenarios)`. `node bin/tusq.js help` → exit 0, 13 commands. `node bin/tusq.js review --help` → includes `--auth-scheme <scheme>`. Challenged dev turn (turn_bf924bb02628f024) on OBJ-001 (medium): AC-8 Case 6 (R6) is dead code in the automated pipeline — `auth_required` is never set by the scanner. Implementation is correct; the gap is test coverage only. All 124 acceptance criteria (REQ-001–REQ-124) pass. Ship verdict: SHIP. Phase transition to launch (auto_approve policy).

**turn_bff61126f4accd83 M28 QA verification (2026-04-25, HEAD 6fb4fa1 + working tree):** Challenged dev turn on AC-8 gap (4 inline assertions vs. ≥3 new top-level scenarios/total ≥13), missing `tusq review --help` `--sensitivity` documentation, and missing website doc updates. QA fixes applied directly (write authority: authoritative): (1) updated `printCommandHelp('review')` to include `--sensitivity <class>`; (2) added 3 new top-level eval scenarios (`sensitivity-class-r4-financial-inference`, `sensitivity-class-r1-preserve-precedence`, `sensitivity-class-zero-evidence-unknown`) with two new runner functions in `eval-regression.mjs`; (3) updated `website/docs/manifest-format.md` with full M28 Sensitivity Class section; (4) updated `website/docs/cli-reference.md` with `--sensitivity` flag documentation. `npm test` exits 0 with `Smoke tests passed` and `Eval regression harness passed (13 scenarios)`. All 115 acceptance criteria (REQ-001–REQ-115) pass. Ship verdict: SHIP.

## Checklist

- [x] Command help audited for every user-facing command (init, scan, manifest, compile, serve, review, version, help)
- [x] Invocation path checked: `node bin/tusq.js` works from repo root
- [x] Failure-mode UX reviewed for invalid flags and missing inputs (REQ-003, REQ-004, REQ-006, REQ-010, REQ-015)
- [x] Full smoke test suite executed and passed (`node tests/smoke.mjs` → exit 0) — re-verified on attempt 2 against post-checkpoint HEAD
- [x] Framework coverage verified: Express, Fastify, and NestJS all scanned successfully (local describe-only MCP endpoint verified via `tusq serve` smoke tests)
- [x] Live-site consolidation explicitly covered in QA evidence: direct file inspection confirms the legacy `websites/` hero structure, recovery path, and styling cues now live in `website/`, and REQ-023 through REQ-025 record canonical `website/` ownership
- [x] Acceptance contract explicitly closed: `website/` is the canonical website surface, `.planning/IMPLEMENTATION_NOTES.md` includes a literal `## Changes` heading, and no ship decision depends on `websites/` remaining active
- [x] Vision goal "capabilities with provenance back to source" verified: scan.json, tusq.manifest.json, and tusq-tools/*.json all carry `provenance.{file,line}` tracing each capability to its originating source declaration (REQ-026 through REQ-028)
- [x] Human approval package reviewed for market-facing truthfulness: `.planning/*` and `website/` launch copy stay anchored to the defendable v0.1.0 surface and do not treat broader README roadmap items as shipped scope
- [x] Off-site launch metadata aligned: `website/docusaurus.config.ts` now uses a tusq-specific social share card so social previews no longer undercut the v0.1.0 launch framing with default Docusaurus branding
- [x] Post-v0.1.0 roadmap page added with explicit non-shipped boundary statement (REQ-029); roadmap is wired into `Help` group in `website/sidebars.ts` and linked from homepage CTA
- [x] Manifest format doc added (REQ-030): `website/docs/manifest-format.md` accurately documents all V1 capability fields, the conservative schema shape, and the approval flow; wired in sidebars.ts Reference group at position 3 with no conflicts
- [x] `sensitivity_class` field verified end-to-end (REQ-031): `classifySensitivity()` and `normalizeSensitivityClass()` implemented in `src/cli.js`; field present in manifest, compiled tools, MCP `tools/list`, and MCP `tools/call`; all four smoke-test assertions pass (`node tests/smoke.mjs` → exit 0); documented with 5 levels and V1-always-unknown note in `website/docs/manifest-format.md`
- [x] `auth_hints` field verified end-to-end (REQ-032): `inferAuthHints()` and `dedupe()` implemented in `src/cli.js`; field present at all 4 pipeline stages (scan.json, tusq.manifest.json, tusq-tools/*.json, MCP runtime); two new smoke-test assertions check `Array.isArray(auth_hints)` on both `tools/list` and `tools/call` responses (`node tests/smoke.mjs` → exit 0); `website/docs/mcp-server.md` updated to list `auth_hints` alongside `side_effect_class` and `sensitivity_class` in governance metadata
- [x] `examples` and `constraints` fields verified end-to-end (REQ-033): `normalizeExamples()`, `defaultExamples()`, `normalizeConstraints()`, and `defaultConstraints()` implemented in `src/cli.js`; fields present at manifest, tusq-tools/*.json, and MCP `tools/call` stages per SYSTEM_SPEC design (scan and `tools/list` intentionally omit them — SYSTEM_SPEC.md lines 564-571 confirm this); smoke tests cover default values on manifest and custom-value propagation through compile and `tools/call` (`node tests/smoke.mjs` → exit 0); `website/docs/manifest-format.md` documents both fields with V1 limitations
- [x] `redaction` and approval audit fields verified end-to-end (REQ-034): `defaultRedaction()`, `normalizeRedaction()`, `normalizeApprovedBy()`, and `normalizeApprovedAt()` implemented in `src/cli.js`; `redaction` present at manifest, tusq-tools/*.json, and MCP `tools/call` stages; `tools/list` intentionally excludes `redaction` (smoke test asserts absence); `approved_by`/`approved_at` are manifest-only — compile output and MCP responses explicitly exclude them (smoke tests assert absence at both stages); 7 smoke assertions cover all boundaries (`node tests/smoke.mjs` → exit 0); `website/docs/manifest-format.md` and `website/docs/mcp-server.md` accurately document the redaction field and manifest-only boundary for approval metadata
- [x] Version-history fields verified end-to-end (REQ-035): `manifest_version` and `previous_manifest_hash` implemented at manifest root in `src/cli.js`; `capability_digest` implemented per capability using stable sorted-key JSON + SHA-256 over content fields only; smoke tests verify increment/hash semantics, digest determinism, digest invariance for approval-only edits, digest changes for content edits, and explicit absence of version-history metadata from compiled tools and MCP responses (`node tests/smoke.mjs` → exit 0); docs updated to mark these fields as manifest-only (`website/docs/manifest-format.md`, `website/docs/mcp-server.md`)
- [x] Framework-specific deep extraction verified (REQ-036): Fastify 3-argument inline bug (`fastify.get(path, {options}, handler)`) discovered and fixed in `src/cli.js`; previously the GET route from the Fastify fixture was silently dropped; fix adds `inlineWithOptionsPattern` multiline regex handling that pattern; NestJS guard inheritance and path composition confirmed correct via live scan; 7 new smoke-test assertions covering Fastify route count, named handler, schema-inferred input_schema, auth_hints on POST, NestJS route count, path composition, guard inheritance, and additive guard on POST all pass (`node tests/smoke.mjs` → exit 0)
- [x] First-pass manifest usability verified (REQ-037): `extractPathParameters`, `buildInputSchema`, `extractDomainSegments` (with `DOMAIN_PREFIX_SEGMENTS` skip list), `describeCapability`, `describeVerb`, `describeSideEffect`, `singularizeWord`, and `describePathQualifier` implemented in `src/cli.js`; `scoreConfidence` applies a -0.10 penalty when `schema_hint` is absent and clamps to `[0, 0.95]`; Express fixture extended with `GET /api/v1/users/:id` so smoke tests observe prefix-aware domain inference (`domain="users"` despite `api/v1` prefix), path parameter extraction into `input_schema.properties`, the rich description with handler/auth context, and sub-0.8 confidence that flips `review_needed=true`; `node tests/smoke.mjs` → exit 0
- [x] Manifest review governance improvements verified (REQ-038): `enforceStrictReviewIfRequested` gates `tusq review --strict` with exit 1 and `Review gate failed:` message when any capability is unapproved or low-confidence; `summarizeInputSchemaForReview`, `summarizeOutputSchemaForReview`, and `summarizeProvenanceForReview` render inputs/returns/source+handler+framework on the review summary line; input_schema inference adds `body` (`source: "request_body"`) for write methods without other inputs; output_schema inference infers `array<object>` or object-with-properties from handler return/json usage; path parameters propagate with `source: "path"` through manifest and compile (including NestJS POST `/users/:id/disable`); provenance now carries `framework` and `handler` through the pipeline; smoke tests assert strict-mode failure and success paths, review-output substrings, body/array/property inference, and path-parameter source marker — all pass (`node tests/smoke.mjs` → exit 0)
- [x] Manifest diff and review queue verified (REQ-039 through REQ-044): `tusq diff` compares explicit manifest files, reports added/removed/changed/unchanged counts, emits JSON, detects changed capabilities by `capability_digest`, reports `fields_changed`, generates review queues, and gates unapproved or review-needed changed capabilities with `--fail-on-unapproved-changes`; smoke tests cover human output, JSON output, gate failure, and gate success (`node tests/smoke.mjs` → exit 0); docs updated in README, CLI reference, and manifest format docs without claiming history-store, git, or automatic re-approval support
- [x] Governed CLI eval/regression harness verified (REQ-045 through REQ-049): `tests/evals/governed-cli-scenarios.json` defines two repo-local scenarios; `tests/eval-regression.mjs` validates strict review gates, compile metadata boundaries, diff review queues, and CI failure behavior; `npm test` runs both smoke and eval suites and exits 0
- [x] Governed manifest approval CLI verified (REQ-050 through REQ-053): `tusq approve` is in help, supports single-capability and `--all` approval, records `approved_by`/`approved_at`, clears `review_needed`, supports dry-run JSON, and is covered by `npm test` (`Smoke tests passed`; `Eval regression harness passed (2 scenarios)`)
- [x] Repo-local capability documentation generator verified (REQ-054 through REQ-057): `tusq docs` is in help, generates Markdown with manifest metadata and per-capability sections, writes deterministic `--out` file byte-identical to stdout, exits 1 with actionable message for missing manifests; covered by `npm test` (`Smoke tests passed`)
- [x] Opt-in execution policy verified (REQ-058 through REQ-063): `tusq serve --policy` startup failures correctly exit 1 with actionable messages for all 5 error conditions; describe-only policy is a no-op; dry-run mode returns `executed: false` with `dry_run_plan` shape, path param substitution, 64-char `plan_hash`, and policy echo; `plan_hash` is deterministic (same args → same hash, different args → different hash); argument validation returns `-32602` with `data.validation_errors` array; `allowed_capabilities` filter enforced with `-32602` for non-listed capabilities; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (4 scenarios)`)
- [x] Policy scaffold generator verified (REQ-064 through REQ-069): `tusq policy init` is in 12-command help surface; default generation produces `schema_version:1.0`, `mode:describe-only`, ISO-8601 `approved_at`, no `allowed_capabilities`; `--mode dry-run`, `--allowed-capabilities`, `--force`, `--dry-run` flags all verified; unknown mode exits 1; empty capabilities exits 1; file-conflict protection exits 1 with `--force` hint; `--dry-run` prints JSON to stdout without writing file; round-trip validator confirmation via `tusq serve --policy <generated>`; `policy-init-generator-round-trip` eval scenario present and passing; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (5 scenarios)`)
- [x] Policy verify command verified (REQ-070 through REQ-074): `tusq policy verify --help` exits 0; help + round-trip (init→verify) exits 0; `--json` success shape (`valid:true`, `path`, `policy` echo) verified; missing-file exit 1 + `--json` failure shape (`valid:false`, `path`, `error`) verified; all four malformed-policy conditions (bad JSON, bad schema_version, bad mode, bad allowed_capabilities) each exit 1 with the correct message; parity confirmed — `verify` and `serve --policy` share `loadAndValidatePolicy()` and produce identical error messages for all four fixtures; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (5 scenarios)`)
- [x] Policy strict verify verified (REQ-075 through REQ-080): `tusq policy verify --strict [--manifest <path>]` flag surface present in `--help`; default (no-`--strict`) code path is byte-identical to M22 even with manifest present in working directory (REQ-075); `--strict` cross-references every `allowed_capabilities` entry against `tusq.manifest.json` and exits 1 for `not_in_manifest`, `not_approved`, and `requires_review` reasons (REQ-076); `--strict` exits 1 with actionable messages for missing manifest, malformed manifest JSON, and Constraint 11 guard (`--manifest requires --strict` before any file read) (REQ-077); `--strict --json` success shape verified (`valid:true`, `strict:true`, `manifest_path`, `manifest_version`, `approved_allowed_capabilities`); failure shape includes `strict_errors` array with `{name,reason}` entries (REQ-078); unset/null `allowed_capabilities` passes trivially without opening manifest (REQ-079); M22 parity under `--strict` confirmed for all four M22 failure fixtures (byte-identical stderr); `policy-strict-verify-determinism` eval scenario present; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (6 scenarios)`) (REQ-080)
- [x] Fastify schema body-field extraction verified (REQ-081 through REQ-087): literal `schema.body.properties` extraction produces `source:'fastify_schema_body'`, `additionalProperties:false`, declaration-order keys, and `required` merging (REQ-081); path-param collision resolution confirmed (path param wins over body-declared field with same name) (REQ-082); fall-back for no-body-key, no-properties, and non-literal schema expressions all produce M15 behavior byte-for-byte (REQ-083); Express and NestJS fixtures produce zero `fastify_schema_body` tags confirming M24 is Fastify-only (REQ-084); +0.04 confidence boost for `schema_fields` verified in `scoreConfidence()` source code (REQ-085); repeated manifest generations produce byte-identical property ordering (REQ-086); `fastify-schema-body-extraction-determinism` eval scenario passes 3 determinism runs; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (7 scenarios)`) (REQ-087)
- [x] Static PII field-name redaction hints verified (REQ-088 through REQ-093): `extractPiiFieldHints()` pure function with whole-key normalized match, no framework import, no eval (REQ-088); `capability.redaction.pii_fields` auto-populated in `cmdManifest` — pii-hint-sample POST /auth → ["email","password"], POST /register → ["user_email","first_name","phone_number"], GET /catalog → [] (REQ-089); `PII_CANONICAL_NAMES` frozen Set with 36 normalized names across 9 categories matches SYSTEM_SPEC DEC-599 (REQ-090); `sensitivity_class` stays "unknown" and other redaction fields not auto-populated (REQ-091); Express fixture pii_fields=[] for all capabilities; NestJS produces no fastify_schema_body; no-properties returns [] (REQ-092); `pii-field-hint-extraction-determinism` eval scenario asserts 3-run determinism; `npm test` exits 0 with `Eval regression harness passed (8 scenarios)` (REQ-093)
- [x] Static PII category labels verified (REQ-094 through REQ-100): `PII_CATEGORY_BY_NAME` is the frozen 36-name/9-category lookup and `PII_CANONICAL_NAMES` derives from it (REQ-094); `extractPiiFieldCategories()` is pure, ordered, and throws on set drift (REQ-095); every capability emits `pii_categories` before digest, with `[]` for empty `pii_fields` (REQ-096); pii-hint-sample `/auth`, `/register`, `/profile`, and `/catalog` produce expected one-to-one field/category arrays (REQ-097); confidence, sensitivity, redaction policy defaults, `serve`, and `policy verify --strict` remain unchanged (REQ-098); docs and release notes carry Constraint 18 framing without retention-enforcement overclaim (REQ-099); `pii-category-label-determinism` eval runs 3 repeats and `npm test` exits 0 with `Eval regression harness passed (9 scenarios)` (REQ-100)
- [x] Reviewer-facing PII redaction review report verified (REQ-101 through REQ-108): `redaction` noun and `redaction review` help surface present (REQ-101); frozen nine-category advisory set with em-dash byte-exactness locked (REQ-102); human/JSON report shapes deterministic and top-level manifest fields copied without re-stamp (REQ-103); advisory order and `--capability` filter verified (REQ-104); no-PII, empty-manifest, and stderr-only failure UX verified (REQ-105); read-only invariant and policy-verify parity verified (REQ-106); no governance-default mutation or sensitivity escalation verified (REQ-107); `redaction-review-determinism` eval runs 3 repeats and `npm test` exits 0 with `Eval regression harness passed (10 scenarios)` (REQ-108)

**turn_d16957598390228f M29 re-verification (run_94746c3508844fcb, 2026-04-25, HEAD 2afe119):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (16 scenarios)`. `node bin/tusq.js help` → exit 0, 13 commands. `node bin/tusq.js review --help` → includes `--auth-scheme <scheme>` and `--sensitivity <class>`. `git diff HEAD --stat -- src/ bin/ tests/ website/` → empty (zero source drift). Challenged dev turn (turn_0e8e152e05796fdc) — only `.planning/IMPLEMENTATION_NOTES.md` changed; M29 already on HEAD from prior run. OBJ-001 (medium, non-blocking) carried forward: R6 dead code in automated pipeline. All 124 acceptance criteria (REQ-001–REQ-124) pass. Ship verdict: SHIP. Phase transition to launch (auto_approve policy).

**turn_ee155e3062a2395e M29 re-verification (run_6d12fe85d0e51576, 2026-04-25, HEAD ab436bf):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (16 scenarios)`. `node bin/tusq.js help` → exit 0, 13 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, policy, redaction, version, help). `node bin/tusq.js review --help` → includes `--auth-scheme <scheme>` and `--sensitivity <class>`. `git diff HEAD --stat -- src/ bin/ tests/ website/` → empty (zero source drift). Challenged dev turn (turn_768db52384611260) — only `.planning/IMPLEMENTATION_NOTES.md` changed; M29 already on HEAD from prior runs. OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation correct for manually-edited manifests. All 124 acceptance criteria (REQ-001–REQ-124) pass. Ship verdict: SHIP. Phase transition to launch (auto_approve policy).

**turn_7774b27ecf2a7a31 M29 re-verification (run_ce89ef5bd4b8cca8, 2026-04-25, HEAD fa7853e):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (16 scenarios)`. `node bin/tusq.js help` → exit 0, 13 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, policy, redaction, version, help). `node bin/tusq.js review --help` → includes `--auth-scheme <scheme>` and `--sensitivity <class>`. `git diff ab436bf..fa7853e -- src/ bin/ tests/ website/` → empty (zero source drift since last QA checkpoint). Challenged prior accepted turn (turn_430f9b5d0f850456, role=qa, labeled 'ghost'): no source changes occurred between ab436bf and fa7853e — only .planning launch artifacts and AgentXchain docs changed. OBJ-001 (medium, non-blocking) carried forward: R6 dead code in automated pipeline; implementation correct for manually-edited manifests. All 124 acceptance criteria (REQ-001–REQ-124) pass. Ship verdict: SHIP. Phase transition to launch (auto_approve policy).

**turn_ceaedc5c7d5bbf1a re-verification (run_42732dba3268a739, 2026-04-26, HEAD fcc623a):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (16 scenarios)`. `node bin/tusq.js help` → exit 0, 13 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, policy, redaction, version, help). `git diff HEAD -- src/ bin/ tests/ website/ package.json package-lock.json` → empty (zero source drift). Challenged dev turn (turn_cdcec89ff1d6683d) — analysis-only with only `.planning/IMPLEMENTATION_NOTES.md` changed; static-MCP-descriptor charter (intent_1777173722739_5899) remains unbound candidate in ROADMAP_NEXT_CANDIDATES.md only; dev correctly declined to implement without human binding; PM OBJ-001 (form decision A/B/C) still unresolved; two unbound charters now in backlog. OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation correct for manually-edited manifests. All 124 acceptance criteria (REQ-001–REQ-124) pass. Ship verdict: SHIP. Phase transition to launch (auto_approve policy).

**turn_f49fd22cd74a554c M29 re-verification (run_2ee1a03651d5d485, 2026-04-25, HEAD e5d3dd4):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (16 scenarios)`. `node bin/tusq.js help` → exit 0, 13 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, policy, redaction, version, help). `git diff HEAD --stat -- src/ bin/ tests/ website/` → empty (zero source drift). Prior dev turn (turn_363693afea46c3e7) made zero source changes — only `.planning/IMPLEMENTATION_NOTES.md` modified; embeddable-surface charter remains unbound candidate in ROADMAP_NEXT_CANDIDATES.md only. No new acceptance criteria required — no new scope shipped; REQ-001–REQ-124 remain complete coverage for the V1.10 (M1–M29) shipped boundary. OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation is correct for manually-edited manifests. OBJ-002 (low, non-blocking) carried forward: VISION.md chat/voice/widget/palette surfaces are unimplemented in M1–M29; shipped product delivers compiler/governance half only. All 124 acceptance criteria (REQ-001–REQ-124) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).

**turn_9abf910a0efb4468 M28 stale-checkbox reconciliation re-verification (run_d69cb0392607d170, 2026-04-26, HEAD 31031e3):** `npm test` → exit 0, `Smoke tests passed`, `Eval regression harness passed (16 scenarios)`. `node bin/tusq.js help` → exit 0, 13 commands (init, scan, manifest, compile, serve, review, docs, approve, diff, policy, redaction, version, help). `git diff HEAD --stat -- src/ bin/ tests/ website/ package.json package-lock.json` → empty (zero source drift). Challenged dev turn (turn_76e50fc1cfd4ef0f) — analysis-only with only `.planning/IMPLEMENTATION_NOTES.md` changed; PM turn (turn_641188dc0c4b7616) correctly reconciled 15 M28 ROADMAP checkboxes as a stale-checkbox false positive (M28 fully shipped at V1.9); no new charter bound; two unbound charters (embeddable-surface + static-MCP-descriptor) remain in ROADMAP_NEXT_CANDIDATES.md. OBJ-001 (medium, non-blocking) carried forward: R6 (`auth_required === false` → `auth_scheme: 'none'`) is dead code in the automated pipeline; implementation is correct for manually-edited manifests. OBJ-002 (low, non-blocking): two unbound vision-derived charters in candidate backlog; non-blocking. OBJ-003 (low, non-blocking): vision_scan has produced at least three stale-checkbox false positives; operator-level configuration concern. All 124 acceptance criteria (REQ-001–REQ-124) pass. Ship verdict: SHIP. Phase transition requested: launch (auto_approve policy).
| REQ-240 | M35 CLI surface: `tusq help` lists 19 commands; `auth` command appears between `approve` and `diff` (alphabetical order) | `node bin/tusq.js help` → exit 0; 19-command list includes `auth` between `approve` and `diff` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-241 | M35 default index (no `--scheme` filter) produces all present scheme buckets in closed-enum order `bearer → api_key → session → basic → oauth → none → unknown`; empty buckets absent | Smoke M35(a): fixture with bearer/api_key/none/unknown capabilities → 4 buckets in order; session/basic/oauth absent | PASS | 2026-04-26 (smoke) | PASS |
| REQ-242 | M35 `--scheme <value>` filter (case-sensitive lowercase) returns exactly one matching bucket; `--scheme` for absent (but valid) scheme exits 1 with `Unknown auth scheme:` message | Smoke M35(c): `--scheme bearer` → single bearer bucket; M35(u): absent scheme → exit 1 with `Unknown auth scheme:` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-243 | M35 `--scheme` with uppercase value (e.g., `BEARER`) exits 1 with `Unknown auth scheme:` on stderr and empty stdout | Smoke M35(d): `--scheme BEARER` → exit 1, stderr `Unknown auth scheme: BEARER`, empty stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-244 | M35 missing manifest exits 1 with error on stderr and empty stdout | Smoke M35(e): missing manifest path → exit 1, error on stderr, empty stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-245 | M35 malformed JSON manifest exits 1 with error on stderr and empty stdout | Smoke M35(f): invalid JSON → exit 1, error on stderr, empty stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-246 | M35 manifest missing `capabilities` array exits 1 | Smoke M35(g): manifest without `capabilities` key → exit 1 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-247 | M35 unknown flag exits 1 | Smoke M35 edge: `--unknown-flag` → exit 1 | PASS | 2026-04-26 (smoke) | PASS |
| REQ-248 | M35 determinism: repeated `tusq auth index` runs on the same manifest produce byte-identical output | Smoke M35(i) equivalent: two consecutive runs produce identical stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-249 | M35 read-only manifest invariant: `tusq auth index` MUST NOT mutate the manifest file | `tusq.manifest.json` mtime and content unchanged before/after `tusq auth index` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-250 | M35 `tusq compile` output is byte-identical before and after `tusq auth index` run | Smoke M35(i): compile hash pre == compile hash post | PASS | 2026-04-26 (smoke) | PASS |
| REQ-251 | M35 `tusq surface plan`, `tusq domain index`, `tusq effect index`, `tusq sensitivity index`, `tusq method index` outputs are byte-identical pre and post-M35 auth index run | Smoke M35(j): hash of each command's stdout unchanged after auth index run | PASS | 2026-04-26 (smoke) | PASS |
| REQ-252 | M35 empty-capabilities manifest: exits 0; human output is exactly the documented empty line; JSON has `schemes: []` | Smoke M35(k): empty-caps manifest → exit 0, documented human line, `schemes: []` in JSON | PASS | 2026-04-26 (smoke) | PASS |
| REQ-253 | M35 `--out <path>` writes index to file and emits no stdout on success | Smoke M35(l): `--out /tmp/...` → empty stdout, output file contains ≥3 scheme entries | PASS | 2026-04-26 (smoke) | PASS |
| REQ-254 | M35 `--out <unwritable-path>` exits 1 with empty stdout | Smoke M35(m): unwritable path → exit 1, empty stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-255 | M35 `--out .tusq/` (detection-before-output rejection): exits 1 with correct rejection message and empty stdout | Smoke M35(n): `--out .tusq/auth-index.json` → exit 1, `Cannot write index inside .tusq/` (or equivalent), empty stdout | PASS | 2026-04-26 (smoke) | PASS |
| REQ-256 | M35 capability with `auth_requirements: null` or missing aggregates into `unknown` bucket; `unknown` bucket always appended last after all named-scheme buckets | Smoke M35(o): no-auth-requirements capability → unknown bucket; unknown appears last in `schemes[]` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-257 | M35 `aggregation_key` is exactly one of the two closed values (`scheme` \| `unknown`) for every emitted bucket; named-scheme buckets have `aggregation_key: 'scheme'`; unknown bucket has `aggregation_key: 'unknown'` | Smoke M35(p): every entry's `aggregation_key` in `{'scheme','unknown'}`; bearer → `scheme`; unknown → `unknown` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-258 | M35 empty buckets MUST NOT appear in output; closed 7-value iteration order `bearer → api_key → session → basic → oauth → none → unknown` (unknown always last, named-scheme order frozen) | Smoke M35(q): bearer-only manifest → exactly `[bearer]`; M35(a): session/basic/oauth absent when unused | PASS | 2026-04-26 (smoke) | PASS |
| REQ-259 | M35 per-bucket 8-field entry shape: `auth_scheme`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`; within each bucket, capability names follow manifest declared order (NOT alphabetized) | Smoke M35(b): all 8 fields present; M35(r): bearer bucket capabilities in manifest declared order | PASS | 2026-04-26 (smoke) | PASS |
| REQ-260 | M35 `has_destructive_side_effect` flag correct per bucket: `true` when any capability in the bucket has `side_effect_class: destructive`; `false` for write-only bucket | Smoke M35(s): bearer bucket with destructive capability → `true`; api_key bucket with write-only capability → `false` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-261 | M35 `has_restricted_or_confidential_sensitivity` flag correct per bucket: `true` when any capability has `sensitivity_class: restricted` or `sensitivity_class: confidential`; `false` when all capabilities have lower sensitivity | Smoke M35(t): bearer bucket with restricted capability → `true`; none bucket → `false` | PASS | 2026-04-26 (smoke) | PASS |
| REQ-262 | M35 `tusq auth index --help` includes planning-aid framing: `This is a planning aid, not a runtime authentication enforcer or OAuth/OIDC/SAML/SOC2 compliance certifier.` | Smoke M35 edge: `tusq auth index --help` → exit 0; stdout includes planning-aid framing | PASS | 2026-04-26 (smoke) | PASS |
| REQ-263 | M35 eval regression: `auth-scheme-index-determinism` scenario (26th scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (26 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `auth-scheme-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (26 scenarios)` | PASS | 2026-04-26 (eval + npm test) | PASS |
| REQ-264 | M35 refinement: `AUTH_SCHEME_INDEX_BUCKET_ORDER` is derived from M29 `AUTH_SCHEMES` (not independently declared); `_guardAuthSchemeBucketAlignment` IIFE fires synchronously at module load and throws a descriptive `Error` if `AUTH_SCHEME_INDEX_BUCKET_ORDER ∪ {'unknown'}` diverges from `AUTH_SCHEMES` — ensuring any future M29 classifier extension surfaces as a startup error rather than silent drift | `src/cli.js` line 109: `Object.freeze(AUTH_SCHEMES.filter((s) => s !== 'unknown'))`; IIFE at line 114 computes `expected = new Set([...AUTH_SCHEME_INDEX_BUCKET_ORDER, 'unknown'])` and `actual = new Set(AUTH_SCHEMES)` and throws if sets diverge; `node -e "require('./src/cli.js')"` exits 0 (guard passes on current aligned state); ROADMAP line 629 `[x]` | PASS | 2026-04-26 (module load + code inspection) | PASS |
| REQ-265 | M36 CLI surface: `tusq help` lists 20 commands; `confidence` command appears between `auth` and `diff` (alphabetical order); CLI surface grows from 19 → 20 commands exactly | `node bin/tusq.js help` → exit 0; 20-command list includes `confidence` between `auth` and `diff` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-266 | M36 default index (no `--tier` filter) produces all present tier buckets in closed-enum order `high → medium → low → unknown`; empty buckets absent; `unknown` always last | Smoke M36(a): fixture with high/medium/low/unknown-confidence capabilities → 4 buckets in closed-enum order; empty buckets absent when no capabilities fall in that tier | PASS | 2026-04-27 (smoke) | PASS |
| REQ-267 | M36 `--tier <value>` filter (case-sensitive lowercase) returns exactly one matching bucket; `--tier` for valid but absent tier exits 1 with `Unknown confidence tier:` message and empty stdout (consistent with M31–M35 filter-absent-bucket behavior) | Smoke M36(c): `--tier high` → single high bucket; valid-but-absent tier → exit 1 with `Unknown confidence tier:` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-268 | M36 `--tier` with uppercase value (e.g., `HIGH`) exits 1 with `Unknown confidence tier:` on stderr and empty stdout (case-sensitive enforcement) | Smoke M36(d): `--tier HIGH` → exit 1, stderr `Unknown confidence tier: HIGH`, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-269 | M36 missing manifest exits 1 with `Manifest not found:` on stderr and empty stdout | Smoke M36(e): missing manifest path → exit 1, `Manifest not found:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-270 | M36 malformed JSON manifest exits 1 with `Invalid manifest JSON:` on stderr and empty stdout | Smoke M36(f): invalid JSON → exit 1, `Invalid manifest JSON:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-271 | M36 manifest missing `capabilities` array exits 1 with `Invalid manifest: missing capabilities array` on stderr and empty stdout | Smoke M36(g): manifest without `capabilities` key → exit 1, error on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-272 | M36 unknown flag exits 1 with error on stderr and empty stdout | Smoke M36 edge: `--unknown-flag` → exit 1, error on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-273 | M36 determinism: repeated `tusq confidence index` runs on the same manifest produce byte-identical stdout in both human and `--json` modes; `warnings[]` ordering is stable per manifest declared order | Smoke M36(h2 determinism): two consecutive runs produce identical stdout; eval `confidence-tier-index-determinism` asserts byte-identity across three runs | PASS | 2026-04-27 (smoke + eval) | PASS |
| REQ-274 | M36 read-only manifest invariant: `tusq confidence index` MUST NOT mutate the manifest file; mtime and content unchanged; `confidence_tier` MUST NOT be persisted into `tusq.manifest.json` (non-persistence rule) | Smoke M36(g2 manifest-invariant): mtime compare before/after; M36(h2 non-persistence): post-run manifest does NOT contain `confidence_tier` key on any capability | PASS | 2026-04-27 (smoke) | PASS |
| REQ-275 | M36 `tusq compile` output is byte-identical before and after `tusq confidence index` run | Smoke M36(i): compile hash pre == compile hash post-confidence-index-run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-276 | M36 `tusq surface plan`, `tusq domain index`, `tusq effect index`, `tusq sensitivity index`, `tusq method index`, and `tusq auth index` outputs are byte-identical pre and post-M36 confidence index run | Smoke M36(j): hash of each command's stdout unchanged after confidence index run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-277 | M36 empty-capabilities manifest: exits 0; human output is exactly `No capabilities in manifest — nothing to index.`; JSON has `tiers: []` and `warnings: []` | Smoke M36(k): empty-caps manifest → exit 0, documented human line, `tiers: []` and `warnings: []` in JSON | PASS | 2026-04-27 (smoke) | PASS |
| REQ-278 | M36 `--out <path>` writes index to file and emits no stdout on success; file contains ≥1 tier entries for non-empty manifests | Smoke M36(l): `--out /tmp/...` → empty stdout, output file contains ≥3 tier entries | PASS | 2026-04-27 (smoke) | PASS |
| REQ-279 | M36 `--out <unwritable-path>` exits 1 with `Cannot write to --out path:` on stderr and empty stdout | Smoke M36(m): unwritable path → exit 1, `Cannot write to --out path:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-280 | M36 `--out .tusq/` (detection-before-output rejection): exits 1 with `--out path must not be inside .tusq/` on stderr and empty stdout | Smoke M36(n): `--out .tusq/confidence-index.json` → exit 1, correct rejection message on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-281 | M36 capability with `confidence: null`, missing `confidence` field, non-numeric, `NaN`, `+/-Infinity`, or out-of-`[0,1]` value aggregates into `unknown` bucket; `unknown` bucket appended last after all named-tier buckets; out-of-range/non-numeric (non-null/undefined) produces `Warning:` entry on stderr (human) or `warnings[]` entry (JSON) | Smoke M36(o): null/missing → unknown bucket last; out-of-range `1.5` → unknown + warning on stderr; `NaN` → unknown + missing-or-non-numeric warning | PASS | 2026-04-27 (smoke) | PASS |
| REQ-282 | M36 `aggregation_key` is exactly one of the two closed values (`tier` \| `unknown`) for every emitted bucket; named-tier buckets (`high`/`medium`/`low`) have `aggregation_key: 'tier'`; `unknown` bucket has `aggregation_key: 'unknown'` | Smoke M36(p): every entry's `aggregation_key` ∈ `{'tier','unknown'}`; high bucket → `tier`; unknown bucket → `unknown` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-283 | M36 empty buckets MUST NOT appear in output; closed 4-value iteration order `high → medium → low → unknown` (unknown always last, named-tier order frozen by closed enum — NOT confidence-numeric-descending) | Smoke M36(q): manifest with only high-confidence capabilities → exactly `[high]`; medium/low/unknown absent | PASS | 2026-04-27 (smoke) | PASS |
| REQ-284 | M36 per-bucket 8-field entry shape: `confidence_tier`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`; within each bucket, capability names follow manifest declared order (NOT alphabetized, NOT confidence-descending) | Smoke M36(b): all 8 required fields present on first entry; M36(r): within-bucket capability order matches manifest declared order | PASS | 2026-04-27 (smoke) | PASS |
| REQ-285 | M36 `has_destructive_side_effect` flag correct per bucket: `true` when any capability in the bucket has `side_effect_class: destructive`; `false` otherwise | Smoke M36(s): bucket with destructive capability → `has_destructive_side_effect: true`; bucket with write-only capability → `false` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-286 | M36 `has_restricted_or_confidential_sensitivity` flag correct per bucket: `true` when any capability has `sensitivity_class: restricted` or `sensitivity_class: confidential`; `false` when all capabilities have lower sensitivity | Smoke M36(t): bucket with restricted capability → `has_restricted_or_confidential_sensitivity: true`; bucket with only public → `false` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-287 | M36 `tusq confidence index --help` includes planning-aid framing: `This is a planning aid, not a runtime confidence enforcement engine, evidence-quality scoring engine, or automated re-classifier.`; tier function thresholds displayed; bucket iteration order displayed | `node bin/tusq.js confidence index --help` → exit 0; stdout includes planning-aid framing + tier function + bucket order `high → medium → low → unknown` | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-288 | M36 eval regression: `confidence-tier-index-determinism` scenario (27th scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (27 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `confidence-tier-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (27 scenarios)` | PASS | 2026-04-27 (eval + npm test) | PASS |
| REQ-289 | M36 tier function thresholds correct and boundary-safe: `high` if `confidence >= 0.85` (inclusive); `medium` if `0.6 <= confidence < 0.85`; `low` if `confidence < 0.6` (and `>= 0`); `unknown` if null/missing/non-numeric/NaN/Infinity/out-of-`[0,1]`; boundary values `0.85` (high), `0.84999` (medium), `0.6` (medium), `0.59999` (low), `0` (low), `1` (high) asserted | Smoke M36(v): boundary-value smoke on synthetic fixture confirms all six boundary cases bucket correctly; `classifyConfidenceTier` thresholds immutable at M36 ship | PASS | 2026-04-27 (smoke) | PASS |
| REQ-290 | M37 CLI surface 20 → 21: `pii` inserted alphabetically between `method` and `policy`; `tusq help` enumerates 21 commands; `tusq help \| grep -c '^  [a-z]'` → 21 | `node bin/tusq.js help` → exit 0, 21 commands (init, scan, manifest, compile, serve, review, docs, approve, auth, confidence, diff, domain, effect, method, pii, policy, redaction, sensitivity, surface, version, help); `grep -c` → 21 | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-291 | M37 `tusq pii index` default run produces all present buckets in closed-enum order `none → low → medium → high → unknown`; empty buckets absent; `pii_field_count_tier` field present in every entry | Smoke M37(a): default run on fixture with mixed-tier capabilities → buckets in `none → low → medium → high → unknown` order; zero absent-tier buckets appear | PASS | 2026-04-27 (smoke) | PASS |
| REQ-292 | M37 `--tier <valid-lowercase>` filter returns exactly one matching bucket on exit 0; absent-tier filter exits 1 with `Unknown pii field count tier:` and empty stdout | Smoke M37(c): `--tier none` on fixture with none-tier caps → 1 entry, exit 0; M37(u): `--tier high` on none-only manifest → exit 1, `Unknown pii field count tier: high`, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-293 | M37 `--tier <UPPERCASE>` exits 1 with `Unknown pii field count tier: HIGH` (or similar) on stderr and empty stdout; case-sensitive lowercase-only enforcement | `node bin/tusq.js pii index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown pii field count tier: HIGH`, empty stdout (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-294 | M37 missing `--manifest` path exits 1 with `Manifest not found:` on stderr and empty stdout | Smoke M37(d): `--manifest /does/not/exist.json` → exit 1, `Manifest not found:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-295 | M37 malformed-JSON manifest exits 1 with `Invalid manifest JSON:` on stderr and empty stdout; no partial index emitted | Smoke M37(e): malformed-JSON file → exit 1, `Invalid manifest JSON:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-296 | M37 missing `capabilities` array in manifest exits 1 with `Invalid manifest: missing capabilities array` on stderr and empty stdout | Smoke: missing capabilities array → exit 1, correct error message on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-297 | M37 unknown flag exits 1 with `Unknown flag:` on stderr and empty stdout | Smoke: `tusq pii index --bogus-flag` → exit 1, `Unknown flag:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-298 | M37 determinism: two consecutive runs of `tusq pii index` on the same manifest produce byte-identical stdout in both human and `--json` modes | Smoke M37(f): two runs byte-identical; eval `pii-field-count-tier-index-determinism` verifies three byte-identical runs | PASS | 2026-04-27 (smoke + eval) | PASS |
| REQ-299 | M37 read-only manifest invariant: manifest mtime/content unchanged after `tusq pii index`; `pii_field_count_tier` MUST NOT be written into `tusq.manifest.json` on any capability (non-persistence rule) | Smoke M37(g): manifest mtime unchanged; M37(h2): post-run manifest has zero `pii_field_count_tier` keys on any capability | PASS | 2026-04-27 (smoke) | PASS |
| REQ-300 | M37 `tusq compile` byte-identical before and after `tusq pii index` | Smoke M37(i): compile golden-file output byte-identical pre/post M37 run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-301 | M37 other index commands (`tusq surface plan`, `tusq domain index`, `tusq effect index`, `tusq sensitivity index`, `tusq method index`, `tusq auth index`, `tusq confidence index`) produce byte-identical output pre/post M37 | Smoke M37(k): all six sibling index commands byte-identical post M37 run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-302 | M37 empty-capabilities manifest exits 0: human mode emits `No capabilities in manifest — nothing to index.`; `--json` mode emits `{…, tiers: [], warnings: []}` | Smoke M37(l): empty-capabilities manifest → exit 0; human line confirmed; JSON `tiers: [], warnings: []` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-303 | M37 `--out <path>` writes index to file and emits no stdout on success | Smoke M37(m): `--out /tmp/pii-out.json` → file written, no stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-304 | M37 `--out <unwritable-path>` exits 1 with `Cannot write to --out path:` on stderr and empty stdout | Smoke M37(n): unwritable path → exit 1, correct error on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-305 | M37 `--out .tusq/anything.json` exits 1 with `--out path must not be inside .tusq/` on stderr (detection-before-output rejection) | Smoke M37(o): `--out .tusq/pii-index.json` → exit 1, `--out path must not be inside .tusq/` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-306 | M37 malformed `pii_fields` (null, missing, not-an-array, non-string element, empty-string element) bucketed as `unknown` with `Warning:` on stderr (human) or in `warnings[]` (JSON); `unknown` bucket appended last | Smoke M37(p5): `pii_fields: null` → unknown bucket + warning; M37(p6): `pii_fields: [123, 'email']` non-string element → unknown + warning; `warnings[]` in JSON | PASS | 2026-04-27 (smoke) | PASS |
| REQ-307 | M37 `aggregation_key` is exactly one of the two closed values (`tier` \| `unknown`) for every emitted bucket; `none`/`low`/`medium`/`high` buckets → `tier`; `unknown` bucket → `unknown` | Smoke M37(q): every emitted entry's `aggregation_key` ∈ `{'tier','unknown'}`; high bucket → `tier`; unknown bucket → `unknown` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-308 | M37 empty buckets MUST NOT appear in output; closed 5-value iteration order `none → low → medium → high → unknown` (unknown always last — NOT count-numeric-ascending) | Smoke M37(r): manifest with only none-tier caps → exactly `[none]`; low/medium/high/unknown absent | PASS | 2026-04-27 (smoke) | PASS |
| REQ-309 | M37 per-bucket 8-field entry shape: `pii_field_count_tier`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`; within each bucket, capability names follow manifest declared order (NOT alphabetized, NOT count-descending) | Smoke M37(b): all 8 required fields present; M37(s): within-bucket capability order matches manifest declared order | PASS | 2026-04-27 (smoke) | PASS |
| REQ-310 | M37 `has_destructive_side_effect` flag correct per bucket: `true` when any capability in the bucket has `side_effect_class: destructive`; `false` otherwise | Smoke M37(t): bucket with destructive capability → `has_destructive_side_effect: true`; bucket with write-only → `false` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-311 | M37 `has_restricted_or_confidential_sensitivity` flag correct per bucket: `true` when any capability has `sensitivity_class: restricted` or `sensitivity_class: confidential`; `false` when all have lower sensitivity | Smoke M37(u-analog): bucket with restricted capability → `has_restricted_or_confidential_sensitivity: true`; public-only → `false` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-312 | M37 `tusq pii index --help` includes planning-aid framing: `This is a planning aid, not a runtime PII detector, data-leakage prevention engine, runtime redaction enforcer, or compliance certifier`; tier function thresholds displayed; bucket order `none → low → medium → high → unknown` displayed | `node bin/tusq.js pii index --help` → exit 0; stdout includes planning-aid framing + tier function + bucket order (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-313 | M37 eval regression: `pii-field-count-tier-index-determinism` scenario (28th scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (28 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `pii-field-count-tier-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (28 scenarios)` (independently verified) | PASS | 2026-04-27 (eval + npm test) | PASS |
| REQ-314 | M37 tier function boundary values correct: `length === 0` → `none`; `length === 1` → `low`; `length === 2` → `low`; `length === 3` → `medium`; `length === 5` → `medium`; `length === 6` → `high`; `length === 7` → `high`; thresholds `0/2/5/6` are frozen | Smoke M37(v): boundary-value smoke on synthetic fixture confirms all seven boundary cases bucket correctly; `classifyPiiFieldCountTier` thresholds immutable at M37 ship | PASS | 2026-04-27 (smoke) | PASS |
| REQ-315 | M38 CLI surface grows from 21 → 22 commands; `examples` inserted alphabetically between `effect` and `method` (`ef` < `ex` < `me`); `tusq help` reflects 22 commands; `grep -c '^  [a-z]'` on help output → 22 | `node bin/tusq.js help \| grep -c '^  [a-z]'` → 22; `examples` between `effect` and `method` confirmed (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-316 | M38 default run (no `--tier`) emits all populated tiers in closed-enum order `none → low → medium → high → unknown`; empty tiers absent; within each tier capabilities follow manifest declared order | Smoke M38(b): `tusq examples index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with `tiers[]` array in `none → low → medium → high` order; `warnings: []` | PASS | 2026-04-27 (smoke + CLI inspection) | PASS |
| REQ-317 | M38 `--tier <valid-lowercase>` filters to that single tier; `--tier extreme` (non-enum value) exits 1 with `Unknown examples count tier: extreme` on stderr and empty stdout | Smoke M38(c): valid `--tier low` filter → only low-tier bucket in output; `--tier extreme` → exit 1, stderr error, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-318 | M38 `--tier HIGH` (uppercase) exits 1 with `Unknown examples count tier: HIGH` on stderr and empty stdout (case-sensitive lowercase-only filter enforcement) | `node bin/tusq.js examples index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, `Unknown examples count tier: HIGH` on stderr, empty stdout (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-319 | M38 missing `--manifest` path exits 1 with `Manifest not found: <path>` on stderr and empty stdout | Smoke M38(d): `--manifest /does/not/exist.json` → exit 1, `Manifest not found:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-320 | M38 malformed-JSON manifest exits 1 with `Failed to parse manifest JSON:` on stderr and empty stdout; no partial index emitted | Smoke M38(e): malformed-JSON file → exit 1, `Failed to parse manifest JSON:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-321 | M38 missing `capabilities` array in manifest exits 1 with `Manifest is missing capabilities array` on stderr and empty stdout | Smoke: missing capabilities array → exit 1, correct error message on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-322 | M38 unknown flag exits 1 with `Unknown flag:` on stderr and empty stdout | Smoke: `tusq examples index --bogus-flag` → exit 1, `Unknown flag:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-323 | M38 determinism: three consecutive runs of `tusq examples index` on the same manifest produce byte-identical stdout in both human and `--json` modes | Smoke M38(f): two runs byte-identical; eval `examples-count-tier-index-determinism` (29th scenario) verifies three byte-identical runs | PASS | 2026-04-27 (smoke + eval) | PASS |
| REQ-324 | M38 read-only manifest invariant: manifest mtime/content unchanged after `tusq examples index`; `examples_count_tier` MUST NOT be written into `tusq.manifest.json` on any capability (non-persistence rule) | Smoke M38(g): manifest mtime unchanged; M38(h2): post-run manifest has zero `examples_count_tier` keys on any capability | PASS | 2026-04-27 (smoke) | PASS |
| REQ-325 | M38 `tusq compile` byte-identical before and after `tusq examples index` | Smoke M38(i): compile golden-file output byte-identical pre/post M38 run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-326 | M38 other index commands (`tusq surface plan`, `tusq domain index`, `tusq effect index`, `tusq sensitivity index`, `tusq method index`, `tusq auth index`, `tusq confidence index`, `tusq pii index`) produce byte-identical output pre/post M38 | Smoke M38(k): all sibling index commands byte-identical post M38 run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-327 | M38 empty-capabilities manifest exits 0: human mode emits `No capabilities in manifest — nothing to index.`; `--json` mode emits `{…, tiers: [], warnings: []}` | Smoke M38(l): empty-capabilities manifest → exit 0; human line confirmed; JSON `tiers: [], warnings: []` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-328 | M38 `--out <path>` writes index to file and emits no stdout on success | Smoke M38(m): `--out /tmp/examples-out.json` → file written, no stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-329 | M38 `--out <unwritable-path>` exits 1 with `Cannot write to --out path:` on stderr and empty stdout | Smoke M38(n): unwritable path → exit 1, correct error on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-330 | M38 `--out .tusq/anything.json` exits 1 with `--out path must not be inside .tusq/` on stderr (detection-before-output rejection) | Smoke M38(o): `--out .tusq/examples-index.json` → exit 1, `--out path must not be inside .tusq/` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-331 | M38 malformed `examples` (null, missing, not-an-array, non-object element, null element, array element) bucketed as `unknown` with five frozen reason codes (`examples_field_missing`, `examples_field_not_array`, `examples_array_contains_non_object_element`, `examples_array_contains_null_element`, `examples_array_contains_array_element`); `Warning:` on stderr (human) or in `warnings[]` (JSON); `unknown` bucket appended last | Smoke M38(p1–p5): null examples → unknown + warning; non-array → unknown + warning; non-object element → unknown + warning; null element → unknown + warning; array element → unknown + warning; `warnings[]` in JSON | PASS | 2026-04-27 (smoke) | PASS |
| REQ-332 | M38 `aggregation_key` is exactly one of the two closed values (`tier` \| `unknown`) for every emitted bucket; `none`/`low`/`medium`/`high` buckets → `tier`; `unknown` bucket → `unknown` | Smoke M38(q): every emitted entry's `aggregation_key` ∈ `{'tier','unknown'}`; low bucket → `tier`; unknown bucket → `unknown` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-333 | M38 empty buckets MUST NOT appear in output; closed 5-value iteration order `none → low → medium → high → unknown` (unknown always last — NOT count-numeric-ascending, NOT eval-readiness-ranked) | Smoke M38(r): manifest with only none-tier caps → exactly `[none]`; low/medium/high/unknown absent | PASS | 2026-04-27 (smoke) | PASS |
| REQ-334 | M38 per-bucket 8-field entry shape: `examples_count_tier`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`; within each bucket, capability names follow manifest declared order (NOT alphabetized, NOT count-descending) | Smoke M38(b): all 8 required fields present; M38(s): within-bucket capability order matches manifest declared order | PASS | 2026-04-27 (smoke) | PASS |
| REQ-335 | M38 `has_destructive_side_effect` flag correct per bucket: `true` when any capability in the bucket has `side_effect_class: destructive`; `false` otherwise | Smoke M38(t): bucket with destructive capability → `has_destructive_side_effect: true`; bucket with write-only → `false` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-336 | M38 `has_restricted_or_confidential_sensitivity` flag correct per bucket: `true` when any capability has `sensitivity_class: restricted` or `sensitivity_class: confidential`; `false` when all have lower sensitivity | Smoke M38(u-analog): bucket with restricted capability → `has_restricted_or_confidential_sensitivity: true`; public-only → `false` | PASS | 2026-04-27 (smoke) | PASS |
| REQ-337 | M38 `tusq examples index --help` includes planning-aid framing: `This is a planning aid, not a runtime example executor, schema validator, example generator, or eval-readiness certifier`; tier function thresholds displayed; bucket order `none → low → medium → high → unknown` displayed; exit codes documented | `node bin/tusq.js examples index --help` → exit 0; stdout includes planning-aid framing + tier function + bucket order (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-338 | M38 eval regression: `examples-count-tier-index-determinism` scenario (29th scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (29 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `examples-count-tier-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (29 scenarios)` (independently verified this turn) | PASS | 2026-04-27 (eval + npm test) | PASS |
| REQ-339 | M38 tier function boundary values correct: `length === 0` → `none`; `length === 1` → `low`; `length === 2` → `low`; `length === 3` → `medium`; `length === 5` → `medium`; `length === 6` → `high`; `length === 7` → `high`; thresholds `0/2/5/6` are frozen; null/array-element/non-object-element → `unknown` | Smoke M38(w): boundary-value smoke on synthetic fixture confirms all seven boundary cases bucket correctly; `classifyExamplesCountTier` thresholds immutable at M38 ship (independently verified from `src/cli.js` lines 3722–3735) | PASS | 2026-04-27 (smoke + code inspection) | PASS |
| REQ-340 | M39 CLI surface grows from 22 → 23 commands; `input` inserted alphabetically between `examples` and `method` (`ex` < `in` < `me`); `tusq help` reflects 23 commands; `grep -c '^  [a-z]'` on help output → 23 | `node bin/tusq.js help | grep -c '^  [a-z]'` → 23; `input` between `examples` and `method` confirmed (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-341 | M39 default run (no `--tier`) emits all populated tiers in closed-enum order `none → low → medium → high → unknown`; empty tiers absent; within each tier capabilities follow manifest declared order | Smoke M39(a): default run on fixture with mixed-tier capabilities → buckets in `none → low → medium → high → unknown` order; zero absent-tier buckets appear; `node bin/tusq.js input index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, `none` and `low` buckets present | PASS | 2026-04-27 (smoke + CLI inspection) | PASS |
| REQ-342 | M39 `--tier <valid-lowercase>` filter returns exactly one matching bucket on exit 0; absent-tier filter exits 1 with `Unknown required input field count tier:` and empty stdout | Smoke M39(c): valid `--tier none` filter → 1 entry, exit 0; `node bin/tusq.js input index --manifest tests/fixtures/express-sample/tusq.manifest.json --tier none` → exit 0 with `[none]` bucket | PASS | 2026-04-27 (smoke + CLI inspection) | PASS |
| REQ-343 | M39 `--tier <UPPERCASE>` exits 1 with `Unknown required input field count tier: HIGH` (or similar) on stderr and empty stdout; case-sensitive lowercase-only enforcement | `node bin/tusq.js input index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown required input field count tier: HIGH`, empty stdout (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-344 | M39 missing `--manifest` path exits 1 with `Manifest not found:` on stderr and empty stdout | `node bin/tusq.js input index --manifest /nonexistent.json` → exit 1, `Manifest not found: /nonexistent.json` on stderr, empty stdout (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-345 | M39 malformed-JSON manifest exits 1 with appropriate error on stderr and empty stdout; no partial index emitted | Smoke M39(e): malformed-JSON file → exit 1, JSON parse error on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-346 | M39 missing `capabilities` array in manifest exits 1 with appropriate error on stderr and empty stdout | Smoke: missing capabilities array → exit 1, correct error message on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-347 | M39 unknown flag exits 1 with `Unknown flag:` on stderr and empty stdout | Smoke: `tusq input index --bogus-flag` → exit 1, `Unknown flag:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-348 | M39 determinism: three consecutive runs of `tusq input index` on the same manifest produce byte-identical stdout in both human and `--json` modes | Smoke M39(f): two runs byte-identical; eval `required-input-field-count-tier-index-determinism` (30th scenario) verifies three byte-identical runs | PASS | 2026-04-27 (smoke + eval) | PASS |
| REQ-349 | M39 read-only manifest invariant: manifest mtime/content unchanged after `tusq input index`; `required_input_field_count_tier` MUST NOT be written into `tusq.manifest.json` on any capability (non-persistence rule) | Smoke M39(g/h2): manifest mtime unchanged; post-run manifest has zero `required_input_field_count_tier` keys on any capability | PASS | 2026-04-27 (smoke) | PASS |
| REQ-350 | M39 `tusq compile` byte-identical before and after `tusq input index` | Smoke M39(i): compile golden-file output byte-identical pre/post M39 run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-351 | M39 other index commands (`tusq surface plan`, `tusq domain index`, `tusq effect index`, `tusq sensitivity index`, `tusq method index`, `tusq auth index`, `tusq confidence index`, `tusq pii index`, `tusq examples index`) produce byte-identical output pre/post M39 | Smoke M39(v): sibling index commands byte-identical post M39 run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-352 | M39 empty-capabilities manifest exits 0: human mode emits `No capabilities in manifest — nothing to index.`; `--json` mode emits `{…, tiers: [], warnings: []}` | Smoke M39(w): empty-capabilities manifest → exit 0; human line confirmed; JSON `tiers: [], warnings: []` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-353 | M39 `--out <path>` writes index to file and emits no stdout on success | Smoke M39(m): `--out /tmp/input-out.json` → file written, no stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-354 | M39 `--out <unwritable-path>` exits 1 with `Cannot write to --out path:` on stderr and empty stdout | Smoke M39(n): unwritable path → exit 1, correct error on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-355 | M39 `--out .tusq/anything.json` exits 1 with `--out path must not be inside .tusq/` on stderr (detection-before-output rejection) | Smoke M39(o): `--out .tusq/input-index.json` → exit 1, `--out path must not be inside .tusq/` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-356 | M39 malformed `input_schema` or `required` (input_schema null/missing/not-plain-object/array, required null/missing/not-array, required contains non-string or empty-string element) bucketed as `unknown` with five frozen reason codes (`input_schema_field_missing`, `input_schema_field_not_object`, `required_field_missing`, `required_field_not_array`, `required_array_contains_non_string_or_empty_element`); `Warning:` on stderr (human) or in `warnings[]` (JSON); `unknown` bucket appended last | Smoke M39(x): legacy_sync (missing input_schema) → unknown + warning; bad_cap (empty-string element in required) → unknown + warning; `warnings[]` in JSON; `unknown` bucket appears last | PASS | 2026-04-27 (smoke) | PASS |
| REQ-357 | M39 `aggregation_key` is exactly one of the two closed values (`tier` \| `unknown`) for every emitted bucket; `none`/`low`/`medium`/`high` buckets → `tier`; `unknown` bucket → `unknown` | `node bin/tusq.js input index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → every entry's `aggregation_key` ∈ `{'tier','unknown'}`; `none` bucket → `tier` confirmed | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-358 | M39 empty buckets MUST NOT appear in output; closed 5-value iteration order `none → low → medium → high → unknown` (unknown always last — NOT exposure-risk-ranked, NOT blast-radius-ranked, NOT easy-call-ranked) | `node bin/tusq.js input index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → only `none` and `low` buckets present (medium/high/unknown absent from fixture manifest) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-359 | M39 per-bucket 8-field entry shape: `required_input_field_count_tier`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`; within each bucket, capability names follow manifest declared order (NOT alphabetized, NOT count-descending) | `node bin/tusq.js input index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → first entry has all 8 required fields confirmed | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-360 | M39 `has_destructive_side_effect` flag correct per bucket: `true` when any capability in the bucket has `side_effect_class: destructive`; `false` otherwise | Smoke M39(t-analog): bucket with destructive capability → `has_destructive_side_effect: true`; bucket with write-only → `false`; sample manifest none-bucket → `false` confirmed | PASS | 2026-04-27 (smoke + CLI inspection) | PASS |
| REQ-361 | M39 `has_restricted_or_confidential_sensitivity` flag correct per bucket: `true` when any capability has `sensitivity_class: restricted` or `sensitivity_class: confidential`; `false` when all have lower sensitivity | Smoke M39(u-analog): bucket with restricted capability → `has_restricted_or_confidential_sensitivity: true`; public-only → `false`; sample manifest none-bucket → `false` confirmed | PASS | 2026-04-27 (smoke + CLI inspection) | PASS |
| REQ-362 | M39 `tusq input index --help` includes planning-aid framing: `This is a planning aid, not a runtime input executor, input-schema validator, input generator, or exposure-safety certifier`; tier function thresholds displayed; bucket order `none → low → medium → high → unknown` displayed; exit codes documented | `node bin/tusq.js input index --help` → exit 0; planning-aid framing + tier function + bucket order `none → low → medium → high → unknown` + exit codes all confirmed (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-363 | M39 eval regression: `required-input-field-count-tier-index-determinism` scenario (30th scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (30 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `required-input-field-count-tier-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (30 scenarios)` (independently verified this turn) | PASS | 2026-04-27 (eval + npm test) | PASS |
| REQ-364 | M39 tier function boundary values correct: `length === 0` → `none`; `length === 1` → `low`; `length === 2` → `low`; `length === 3` → `medium`; `length === 5` → `medium`; `length === 6` → `high`; `length === 7` → `high`; thresholds `0/2/5/6` are frozen; null/non-object input_schema/non-array required/non-string or empty-string required element → `unknown` | `classifyRequiredInputFieldCountTier` at `src/cli.js:4080–4107` independently read: null/undefined input_schema → unknown (lines 4082–4084); non-object/array input_schema → unknown (lines 4085–4087); missing required property → unknown (lines 4088–4090); non-array required → unknown (lines 4091–4093); non-string or empty-string element → unknown (lines 4094–4098); length 0 → none (line 4099); length 1-2 → low (line 4100); length 3-5 → medium (line 4101); length >= 6 → high (line 4102); thresholds `0/2/5/6` immutable at M39 ship | PASS | 2026-04-27 (code inspection) | PASS |
| REQ-365 | M40 CLI surface grows from 23 → 24 commands; `output` inserted alphabetically between `method` and `pii` (`me` < `ou` < `pi`); `tusq help` reflects 24 commands; `grep -c '^  [a-z]'` on help output → 24 | `node bin/tusq.js help | grep -c '^  [a-z]'` → 24; `output` between `method` and `pii` confirmed (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-366 | M40 default run (no `--tier`) emits all populated tiers in closed-enum order `none → low → medium → high → unknown`; empty tiers absent; within each tier capabilities follow manifest declared order | `node bin/tusq.js output index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, `low` and `unknown` buckets present (none/medium/high absent from fixture); buckets in `none → low → medium → high → unknown` order (low before unknown) confirmed | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-367 | M40 `--tier <valid-lowercase>` filter returns exactly one matching bucket on exit 0; absent-tier filter exits 1 with `Unknown output schema property count tier:` and empty stdout | `node bin/tusq.js output index --tier low --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, single `low` bucket; absent-tier (`--tier none` on fixture with no none-bucket capabilities) → exit 1 (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-368 | M40 `--tier <UPPERCASE>` exits 1 with `Unknown output schema property count tier: HIGH` (or similar) on stderr and empty stdout; case-sensitive lowercase-only enforcement | `node bin/tusq.js output index --tier HIGH --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown output schema property count tier: HIGH`, empty stdout (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-369 | M40 missing `--manifest` path exits 1 with `Manifest not found:` on stderr and empty stdout | `node bin/tusq.js output index --manifest /nonexistent.json` → exit 1, `Manifest not found: /nonexistent.json` on stderr, empty stdout (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-370 | M40 malformed-JSON manifest exits 1 with appropriate error on stderr and empty stdout; no partial index emitted | Smoke M40(e): malformed-JSON file → exit 1, JSON parse error on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-371 | M40 missing `capabilities` array in manifest exits 1 with appropriate error on stderr and empty stdout | Smoke M40(f): missing capabilities array → exit 1, correct error message on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-372 | M40 unknown flag exits 1 with `Unknown flag:` on stderr and empty stdout | Smoke M40(l): `tusq output index --bogus-flag` → exit 1, `Unknown flag:` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-373 | M40 determinism: three consecutive runs of `tusq output index` on the same manifest produce byte-identical stdout in both human and `--json` modes | Smoke M40(r): two runs byte-identical; eval `output-schema-property-count-tier-index-determinism` (31st scenario) verifies three byte-identical runs; `npm test` exits 0 with `Eval regression harness passed (31 scenarios)` (independently verified) | PASS | 2026-04-27 (smoke + eval) | PASS |
| REQ-374 | M40 read-only manifest invariant: manifest mtime/content unchanged after `tusq output index`; `output_schema_property_count_tier` MUST NOT be written into `tusq.manifest.json` on any capability (non-persistence rule) | Smoke M40(s/t): manifest mtime unchanged; post-run manifest has zero `output_schema_property_count_tier` keys on any capability | PASS | 2026-04-27 (smoke) | PASS |
| REQ-375 | M40 `tusq compile` byte-identical before and after `tusq output index` | Smoke M40(u): compile golden-file output byte-identical pre/post M40 run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-376 | M40 other index commands (`tusq surface plan`, `tusq domain index`, `tusq effect index`, `tusq sensitivity index`, `tusq method index`, `tusq auth index`, `tusq confidence index`, `tusq pii index`, `tusq examples index`, `tusq input index`) produce byte-identical output pre/post M40 | Smoke M40(v): sibling index commands byte-identical post M40 run | PASS | 2026-04-27 (smoke) | PASS |
| REQ-377 | M40 empty-capabilities manifest exits 0: human mode emits `No capabilities in manifest — nothing to index.`; `--json` mode emits `{…, tiers: [], warnings: []}` | Smoke M40(w): empty-capabilities manifest → exit 0; human line confirmed; JSON `tiers: [], warnings: []` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-378 | M40 `--out <path>` writes index to file and emits no stdout on success | Smoke M40(n): `--out /tmp/output-out.json` → file written, no stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-379 | M40 `--out <unwritable-path>` exits 1 with `Cannot write to --out path:` on stderr and empty stdout | Smoke M40(o): unwritable path → exit 1, correct error on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-380 | M40 `--out .tusq/anything.json` exits 1 with `--out path must not be inside .tusq/` on stderr (detection-before-output rejection) | Smoke M40(p): `--out .tusq/output-index.json` → exit 1, `--out path must not be inside .tusq/` on stderr, empty stdout | PASS | 2026-04-27 (smoke) | PASS |
| REQ-381 | M40 malformed `output_schema` or `properties` (output_schema null/missing/not-plain-object/array, properties null/missing/not-plain-object, properties containing non-object descriptor — null/primitive/array/function) bucketed as `unknown` with five frozen reason codes (`output_schema_field_missing`, `output_schema_field_not_object`, `output_schema_properties_field_missing`, `output_schema_properties_field_not_object`, `output_schema_properties_object_contains_non_object_property_descriptor`); `Warning:` on stderr (human) or in `warnings[]` (JSON); `unknown` bucket appended last; type:array schemas correctly bucketed as unknown with reason `output_schema_properties_field_missing` (informative, not a defect) | Smoke M40(x): get_users_users (output_schema_properties_field_missing) → unknown bucket + warning in JSON `warnings[]` confirmed; `--json` → `warnings: [{capability: "get_users_users", reason: "output_schema_properties_field_missing"}]` (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-382 | M40 `aggregation_key` is exactly one of the two closed values (`tier` \| `unknown`) for every emitted bucket; `none`/`low`/`medium`/`high` buckets → `tier`; `unknown` bucket → `unknown` | `node bin/tusq.js output index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → `low` bucket `aggregation_key: "tier"`, `unknown` bucket `aggregation_key: "unknown"` confirmed (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-383 | M40 empty buckets MUST NOT appear in output; closed 5-value iteration order `none → low → medium → high → unknown` (unknown always last — NOT doc-drift-risk-ranked, NOT staleness-ranked, NOT contract-surface-area-ranked) | `node bin/tusq.js output index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → only `low` and `unknown` buckets present (none/medium/high absent from fixture); `low` appears before `unknown` (closed-enum order confirmed) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-384 | M40 per-bucket 8-field entry shape: `output_schema_property_count_tier`, `aggregation_key`, `capability_count`, `capabilities[]`, `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`; within each bucket, capability names follow manifest declared order (NOT alphabetized, NOT count-descending) | `node bin/tusq.js output index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → `low` bucket entry has all 8 required fields: `output_schema_property_count_tier: "low"`, `aggregation_key: "tier"`, `capability_count: 2`, `capabilities: ["get_users_api_v1_users_id", "post_users_users"]`, `approved_count: 1`, `gated_count: 1`, `has_destructive_side_effect: false`, `has_restricted_or_confidential_sensitivity: false` (independently verified) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-385 | M40 `has_destructive_side_effect` flag correct per bucket: `true` when any capability in the bucket has `side_effect_class: destructive`; `false` otherwise | Smoke M40(destructive-analog): bucket with destructive capability → `has_destructive_side_effect: true`; fixture low-bucket `has_destructive_side_effect: false` confirmed | PASS | 2026-04-27 (smoke + CLI inspection) | PASS |
| REQ-386 | M40 `has_restricted_or_confidential_sensitivity` flag correct per bucket: `true` when any capability has `sensitivity_class: restricted` or `sensitivity_class: confidential`; `false` when all have lower sensitivity | Smoke M40(sensitivity-analog): bucket with restricted capability → `has_restricted_or_confidential_sensitivity: true`; fixture low-bucket `has_restricted_or_confidential_sensitivity: false` confirmed | PASS | 2026-04-27 (smoke + CLI inspection) | PASS |
| REQ-387 | M40 `tusq output index --help` includes planning-aid framing: `This is a planning aid, not a runtime output executor, output-schema validator, doc-contradiction detector, output generator, or doc-accuracy certifier`; tier function displayed (`none if Object.keys(output_schema.properties).length === 0; low if 1-2; medium if 3-5; high if >= 6; unknown if output_schema/properties missing or malformed`); type:array informative note displayed; bucket order `none → low → medium → high → unknown` displayed; exit codes documented; NOT doc-drift-risk-ranked disclaimer present | `node bin/tusq.js output index --help` → exit 0; all framing elements confirmed (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-388 | M40 eval regression: `output-schema-property-count-tier-index-determinism` scenario (31st scenario) present in eval harness; `npm test` exits 0 with `Eval regression harness passed (31 scenarios)` | `tests/evals/governed-cli-scenarios.json` id `output-schema-property-count-tier-index-determinism`; `npm test` exits 0 with `Eval regression harness passed (31 scenarios)` (independently verified this turn) | PASS | 2026-04-27 (eval + npm test) | PASS |
| REQ-389 | M40 tier function boundary values correct: `length === 0` → `none`; `length === 1` → `low`; `length === 2` → `low`; `length === 3` → `medium`; `length === 5` → `medium`; `length === 6` → `high`; `length === 7` → `high`; thresholds `0/2/5/6` are frozen; null/non-object output_schema/null-or-non-object properties/properties-with-non-object-descriptor → `unknown`; type:array → `unknown` (properties missing) | `classifyOutputSchemaPropertyCountTier` at `src/cli.js:4444–4468` independently read: null/undefined → unknown (line 4445–4447); non-object/array output_schema → unknown (lines 4448–4450); missing properties → unknown (lines 4451–4453); null/non-object/array properties → unknown (lines 4454–4456); non-object descriptor → unknown (lines 4459–4462); length 0 → none (line 4465); length ≤ 2 → low (line 4466); length ≤ 5 → medium (line 4467); else → high (line 4468); thresholds `0/2/5/6` immutable at M40 ship | PASS | 2026-04-27 (code inspection) | PASS |
| REQ-390 | M41 CLI surface expanded: 24 → 25 commands; `tusq path` and `tusq path index` present in `tusq help` output; `path` inserted alphabetically between `output` and `pii` | `node bin/tusq.js help \| grep -c '^  [a-z]'` → 25; `node bin/tusq.js help` output contains `path` between `output` and `pii` (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-391 | M41 `tusq path index` default closed-enum run: on canonical express fixture produces tiers in order `low` (2 capabilities: get_users_users, post_users_users) then `medium` (1 capability: get_users_api_v1_users_id); `warnings: []`; exit 0 | `node bin/tusq.js path index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with tiers: [{path_segment_count_tier:"low", capability_count:2,...}, {path_segment_count_tier:"medium", capability_count:1,...}], warnings:[] (independently verified this turn) | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-392 | M41 `--tier` lowercase filter: `--tier low` returns only the low bucket; `--tier medium` returns only the medium bucket; `--tier none`, `--tier high`, `--tier unknown` return empty tiers[] or single filtered bucket | Smoke M41(b-f): per-tier filter cases confirmed; `node bin/tusq.js path index --tier low --manifest ... --json` → exit 0, single low bucket (independently verified this turn) | PASS | 2026-04-27 (smoke + CLI) | PASS |
| REQ-393 | M41 `--tier <absent-tier>` exits 1: requesting a tier that is not present in the index (e.g., `--tier high` when no capability reaches 5+ segments) returns exit 0 with empty tiers[] — absent tier is NOT a fatal error; unknown tier literal IS fatal (exit 1) | Smoke M41(e): `--tier high` on express fixture (no capabilities with ≥ 5 segments) → exit 0, tiers: [] confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-394 | M41 `--tier HIGH` (uppercase) exits 1 with `Unknown path segment count tier: HIGH` on stderr, empty stdout (case-sensitive enforcement) | `node bin/tusq.js path index --tier HIGH --manifest ... --json` → exit 1, stderr `Unknown path segment count tier: HIGH` (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-395 | M41 `--tier xyz` (unknown literal) exits 1 with `Unknown path segment count tier: xyz` on stderr | Smoke M41(h): `--tier xyz` → exit 1, stderr `Unknown path segment count tier: xyz` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-396 | M41 `--manifest <missing>` exits 1 with `Manifest file not found: <path>` on stderr, empty stdout | Smoke M41(i): missing manifest path → exit 1, stderr `Manifest file not found:` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-397 | M41 `--manifest <malformed.json>` exits 1 with `Failed to parse manifest JSON: <message>` on stderr, empty stdout | Smoke M41(j): malformed JSON manifest → exit 1, stderr `Failed to parse manifest JSON:` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-398 | M41 `--manifest <no-capabilities>` exits 1 with `Manifest is missing capabilities array` on stderr, empty stdout | Smoke M41(k): manifest missing capabilities array → exit 1, stderr `Manifest is missing capabilities array` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-399 | M41 unknown flag exits 1 with `Unknown flag: <flag>` on stderr, empty stdout | Smoke M41(l): `--unknown-flag` → exit 1, stderr `Unknown flag:` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-400 | M41 `--tier` with no value exits 1 with `--tier requires a value` on stderr | Smoke M41(m): `--tier` with no following value → exit 1, stderr `--tier requires a value` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-401 | M41 `--out <valid path>` writes index JSON to file; stdout empty on success; exit 0 | Smoke M41(n): `--out /tmp/m41-out.json` writes valid JSON; stdout empty; exit 0 confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-402 | M41 `--out .tusq/foo.json` exits 1 with `--out path must not be inside .tusq/` on stderr, empty stdout | Smoke M41(o): `--out .tusq/foo.json` → exit 1, rejection confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-403 | M41 `--out <unwritable parent>` exits 1 with `--out parent directory does not exist or is not writable` on stderr, empty stdout | Smoke M41(p): `--out /nonexistent/dir/foo.json` → exit 1, rejection confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-404 | M41 `--json` output is valid JSON with `tiers[]` array, `warnings: []` (or populated), and all required top-level fields: `manifest_path`, `manifest_version`, `generated_at`, `tiers`, `warnings` | `node bin/tusq.js path index --manifest ... --json` → exit 0, valid JSON shape verified (independently verified this turn); `warnings` always present even when empty for shape stability | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-405 | M41 determinism: three consecutive `tusq path index` runs on same manifest produce byte-identical stdout | Smoke M41(r): three consecutive runs → byte-identical stdout confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-406 | M41 manifest read-only invariant: manifest mtime, SHA-256, and per-capability `capability_digest` unchanged after `tusq path index` run; non-persistence: `path_segment_count_tier` field MUST NOT appear in `tusq.manifest.json` after run | Smoke M41(s,t): mtime/SHA/digest unchanged; `tusq.manifest.json` does not contain `path_segment_count_tier` field confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-407 | M41 compile byte-identity: `tusq compile` output is byte-identical pre and post `tusq path index` run | Smoke M41(u): compile output byte-identical confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-408 | M41 other index commands byte-identity: `tusq surface plan`, `tusq domain index`, `tusq effect index`, `tusq sensitivity index`, `tusq method index`, `tusq auth index`, `tusq confidence index`, `tusq pii index`, `tusq examples index`, `tusq input index`, `tusq output index` all byte-identical pre/post M41 | Smoke M41(v): all 11 peer index commands byte-identical confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-409 | M41 empty-capabilities: manifest with `capabilities: []` → exit 0, human stdout `No capabilities in manifest — nothing to index.`; `--json` stdout `{ manifest_path, manifest_version, generated_at, tiers: [], warnings: [] }` | Smoke M41(w): empty-capabilities manifest → exit 0, correct human and JSON output confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-410 | M41 malformed `path` field: capability with null/missing/non-string/empty-string/no-leading-slash/empty-interior-segment path → bucketed as `unknown`; human stderr `Warning: capability '<name>' has malformed path (<reason>)`; `--json` `warnings[]` entry with capability name and frozen reason code; all five frozen reason codes: `path_field_missing`, `path_field_not_string`, `path_field_empty_string`, `path_field_does_not_start_with_forward_slash`, `path_field_contains_empty_interior_segment` | Smoke M41(x,f): malformed path cases → unknown bucket + warnings entries for all five reason codes confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-411 | M41 closed two-value `aggregation_key` enum: `tier` for known tiers (none/low/medium/high), `unknown` for unknown bucket; no other values possible | `classifyPathSegmentCountTier` returns one of five tier values; `buildPathSegmentCountTierIndex` sets aggregation_key `tier` or `unknown` per bucket; `PATH_SEGMENT_COUNT_TIER_AGGREGATION_KEY_ENUM` frozen Set `{tier, unknown}` verified at `src/cli.js:208` | PASS | 2026-04-27 (code inspection) | PASS |
| REQ-412 | M41 empty buckets absent; closed 5-value iteration order `none → low → medium → high → unknown`: empty buckets do not appear; buckets iterate in closed-enum order, not manifest first-appearance order | `node bin/tusq.js path index --manifest ... --json` → `tiers[]` contains only non-empty buckets; order is `low` then `medium` (none/high/unknown absent because no capabilities match those tiers); smoke M41(a) confirms closed-enum bucket ordering | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-413 | M41 per-bucket 8-field entry shape correct: `path_segment_count_tier`, `aggregation_key`, `capability_count`, `capabilities[]` (manifest declared order), `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`; has_destructive_side_effect: `true` iff any capability in bucket has `side_effect_class === 'destructive'`; has_restricted_or_confidential_sensitivity: `true` iff any has `sensitivity_class === 'restricted'` or `=== 'confidential'` | All 8 fields present in `node bin/tusq.js path index --manifest ... --json` tiers[] entries; fixture low-bucket: approved_count 2, gated_count 0, has_destructive_side_effect false, has_restricted_or_confidential_sensitivity false; medium-bucket: approved_count 0, gated_count 1 confirmed | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-414 | M41 tier function boundary values correct: path `/` → `none` (count 0); `/users` → `low` (count 1); `/users/123` → `low` (count 2); `/api/v1/users` → `medium` (count 3); `/api/v1/users/:id` → `medium` (count 4); `/api/v1/orgs/:id/tasks` → `high` (count 5); `/a/b/c/d/e/f` → `high` (count 6); path-parameter `:id` counts as one segment (no unwrapping); thresholds `0/2/4/5` are frozen; null/undefined/non-string/empty-string/no-leading-slash/empty-interior-segment → `unknown` | `classifyPathSegmentCountTier` at `src/cli.js:4811–4834` independently read: null/undefined → unknown (lines 4812–4814); non-string → unknown (lines 4815–4817); empty string → unknown (lines 4818–4820); no leading '/' → unknown (lines 4821–4823); empty interior segment → unknown (lines 4825–4828); count 0 → none (line 4830); count ≤ 2 → low (line 4831); count ≤ 4 → medium (line 4832); else → high (line 4833); thresholds `0/2/4/5` immutable at M41 ship | PASS | 2026-04-27 (code inspection) | PASS |
| REQ-415 | M42 CLI surface expanded: 25 → 26 commands; `tusq response` and `tusq response index` present in `tusq help`; `response` inserted alphabetically between `redaction` and `sensitivity` (`red` < `res` < `sen` at position 2: `d`(100) < `s`(115) < `s`(115); `response` < `sensitivity` at position 0: `r`(114) < `s`(115)) | `node bin/tusq.js help \| grep -c '^  [a-z]'` → 26; `node bin/tusq.js help` output contains `response` between `redaction` and `sensitivity` (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-416 | M42 default closed-enum run on canonical express fixture produces `types[]` in order `object` (2 capabilities: get_users_api_v1_users_id, post_users_users) then `array` (1 capability: get_users_users); `warnings: []`; exit 0; result field name is `types` (NOT `tiers`) | `node bin/tusq.js response index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with types: [{output_schema_top_level_type:"object", capability_count:2,...}, {output_schema_top_level_type:"array", capability_count:1,...}], warnings:[] (independently verified this turn) | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-417 | M42 `--type object` filter returns only the object bucket (exit 0); `--type array` filter returns only the array bucket (exit 0); absent-type filter (`--type string` when no string capabilities) exits 0 with empty types[] | `node bin/tusq.js response index --type object --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, single object bucket confirmed (independently verified); `--type string` on express fixture → exit 0, types:[] (no string capabilities in fixture) | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-418 | M42 `--type string`, `--type number`, `--type boolean`, `--type null`, `--type unknown` filters: each exits 0 on fixture with empty types[] (no capabilities of those types in express fixture) or non-empty when matched; `--type unknown` on fixture with malformed output_schema capability → exit 0 with unknown bucket populated | Smoke M42(d-h): per-type filter cases confirmed; absent-type filter not fatal | PASS | 2026-04-27 (smoke) | PASS |
| REQ-419 | M42 `--type OBJECT` (uppercase) exits 1 with `Unknown output schema top-level type: OBJECT` on stderr, empty stdout (case-sensitive enforcement); `--type ARRAY`, `--type NULL`, `--type INTEGER` all exit 1 similarly | `node bin/tusq.js response index --type OBJECT --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown output schema top-level type: OBJECT`, empty stdout (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-420 | M42 `--type integer` exits 1 with `Unknown output schema top-level type: integer` on stderr, empty stdout; `'integer'` is NOT a separate bucket in the closed seven-value enum (per JSON Schema spec, integer is a subset of number; integer-subset bucketing is reserved for `M-Output-Type-Integer-Subset-Index-1`) | Smoke M42(j): `--type integer` → exit 1, stderr `Unknown output schema top-level type: integer`; case-sensitive enforcement confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-421 | M42 `--type xyz` (arbitrary unknown literal) exits 1 with `Unknown output schema top-level type: xyz` on stderr, empty stdout | Smoke M42(k): `--type xyz` → exit 1, correct rejection message confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-422 | M42 `--manifest <missing>` exits 1 with `Manifest file not found: <path>` on stderr, empty stdout | Smoke M42(l): missing manifest path → exit 1, stderr `Manifest file not found:` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-423 | M42 `--manifest <malformed.json>` exits 1 with `Failed to parse manifest JSON: <message>` on stderr, empty stdout | Smoke M42(m): malformed JSON manifest → exit 1, stderr `Failed to parse manifest JSON:` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-424 | M42 `--manifest <no-capabilities>` exits 1 with `Manifest is missing capabilities array` on stderr, empty stdout | Smoke M42(n): manifest missing capabilities array → exit 1, stderr `Manifest is missing capabilities array` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-425 | M42 unknown flag exits 1 with `Unknown flag: <flag>` on stderr, empty stdout | Smoke M42(o): `--unknown-flag` → exit 1, stderr `Unknown flag:` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-426 | M42 `--type` with no value exits 1 with `--type requires a value` on stderr | Smoke M42(p): `--type` with no following value → exit 1, stderr `--type requires a value` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-427 | M42 `--out <valid path>` writes index JSON to file; stdout empty on success; exit 0 | Smoke M42(q): `--out /tmp/m42-out.json` writes valid JSON; stdout empty; exit 0 confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-428 | M42 `--out .tusq/foo.json` exits 1 with `--out path must not be inside .tusq/` on stderr, empty stdout (detection-before-output rejection) | Smoke M42(r): `--out .tusq/response-index.json` → exit 1, rejection confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-429 | M42 `--out <unwritable parent>` exits 1 with `--out parent directory does not exist or is not writable` on stderr, empty stdout | Smoke M42(s-analog): `--out /nonexistent/dir/foo.json` → exit 1, rejection confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-430 | M42 `--json` output is valid JSON with `types[]` array (NOT `tiers[]`), `warnings[]` (always present, even when empty), and all required top-level fields: `manifest_path`, `manifest_version`, `generated_at`, `types`, `warnings`; result field name `types` is categorical (not a numeric tier) | `node bin/tusq.js response index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON shape with `types` field (NOT `tiers`) verified (independently verified this turn); `warnings` always present for shape stability | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-431 | M42 determinism: three consecutive `tusq response index` runs on same manifest produce byte-identical stdout in both human and `--json` modes | Smoke M42(t): three consecutive runs → byte-identical stdout confirmed; eval `output-schema-top-level-type-index-determinism` (33rd scenario) also verifies | PASS | 2026-04-27 (smoke + eval) | PASS |
| REQ-432 | M42 manifest read-only invariant: manifest mtime, SHA-256, and per-capability `capability_digest` unchanged after `tusq response index` run; non-persistence: `output_schema_top_level_type` field MUST NOT appear in `tusq.manifest.json` after run | Smoke M42(u): mtime/SHA/digest unchanged; `tusq.manifest.json` does not contain `output_schema_top_level_type` field confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-433 | M42 compile byte-identity: `tusq compile` output is byte-identical before and after `tusq response index` run | Smoke M42(u): compile output byte-identical pre/post M42 run confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-434 | M42 peer index commands byte-identity: `tusq surface plan`, `tusq domain index`, `tusq effect index`, `tusq sensitivity index`, `tusq method index`, `tusq auth index`, `tusq confidence index`, `tusq pii index`, `tusq examples index`, `tusq input index`, `tusq output index`, `tusq path index` all byte-identical pre/post M42 | Smoke M42(v): all 12 peer index commands byte-identical confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-435 | M42 empty-capabilities: manifest with `capabilities: []` → exit 0; human stdout `No capabilities in manifest — nothing to index.`; `--json` stdout `{ manifest_path, manifest_version, generated_at, types: [], warnings: [] }` | Smoke M42(w): empty-capabilities manifest → exit 0, correct human and JSON output confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-436 | M42 malformed `output_schema` or `type` field: null/missing/non-object/array output_schema → unknown + `output_schema_field_missing` or `output_schema_field_not_object`; missing `type` field → unknown + `output_schema_type_field_missing`; non-string `type` (null, number, array) → unknown + `output_schema_type_field_not_string`; string `type` not in spec primitive set (e.g., `'integer'`, `'tuple'`) → unknown + `output_schema_type_field_value_not_in_json_schema_primitive_set`; human mode: stderr `Warning: capability '<name>' has malformed output_schema (<reason>)`; JSON mode: `warnings[]` entry with capability name and frozen reason code; `unknown` bucket appended last | Smoke M42(x): all five frozen reason codes exercised (`output_schema: null` → output_schema_field_missing; `output_schema: 'string'` → output_schema_field_not_object; `output_schema: {}` → output_schema_type_field_missing; `output_schema: {type: 42}` → output_schema_type_field_not_string; `output_schema: {type: 'integer'}` → output_schema_type_field_value_not_in_json_schema_primitive_set); unknown bucket appears last confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-437 | M42 closed two-value `aggregation_key` enum: `type` for known types (object/array/string/number/boolean/null buckets), `unknown` for unknown bucket; NO other values — specifically NOT `tier` (M35–M41 used `tier`; M42 uses `type` because the axis is categorical not numeric) | `node bin/tusq.js response index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → object bucket `aggregation_key: "type"`, array bucket `aggregation_key: "type"` confirmed; `OUTPUT_SCHEMA_TOP_LEVEL_TYPE_AGGREGATION_KEY_ENUM` frozen Set `{type, unknown}` verified at `src/cli.js:225` (independently verified this turn) | PASS | 2026-04-27 (CLI + code inspection) | PASS |
| REQ-438 | M42 empty buckets absent; closed 7-value iteration order `object → array → string → number → boolean → null → unknown`: empty buckets do not appear; buckets iterate in closed-enum order matching JSON Schema 2020-12 spec primitive enumeration order; `unknown` always appended last; explicitly NOT data-contract-completeness-ranked, NOT shape-complexity-ranked, NOT DTO-richness-ranked | `node bin/tusq.js response index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → `types[]` contains only `object` and `array` buckets (string/number/boolean/null/unknown absent from fixture); `object` appears before `array` (closed-enum order confirmed); smoke M42(a) confirms closed-enum bucket ordering | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-439 | M42 `classifyOutputSchemaTopLevelType` boundary conditions: null/undefined output_schema → unknown; non-object (string, number, function) → unknown; array output_schema → unknown; object output_schema with no `type` property → unknown; `type: null` → unknown; `type: 42` (non-string) → unknown; `type: ['object', 'null']` (array-of-types) → unknown; `type: 'integer'` (non-spec-primitive) → unknown; `type: 'object'` → object; `type: 'array'` → array; `type: 'string'` → string; `type: 'number'` → number; `type: 'boolean'` → boolean; `type: 'null'` → null; compositional schemas (`oneOf`/`anyOf`/`allOf` without top-level `type`) → unknown | `classifyOutputSchemaTopLevelType` at `src/cli.js:5174–5193` independently read: null/undefined → unknown (lines 5175–5177); non-object/array → unknown (lines 5178–5180); no `type` property → unknown (lines 5181–5183); null/undefined/non-string `typeVal` → unknown (lines 5184–5187); `OUTPUT_SCHEMA_TOP_LEVEL_TYPE_BUCKET_ORDER.includes(typeVal)` → returns typeVal (line 5189–5191); else → unknown (line 5192); six spec primitives immutable at M42 ship | PASS | 2026-04-27 (code inspection) | PASS |
| REQ-440 | M43 CLI surface expanded: 26 → 27 commands; `tusq request` and `tusq request index` present in `tusq help`; `request` inserted alphabetically between `redaction` and `response` (`red`<`req`<`res` at position 2: `d`(100)<`q`(113)<`s`(115)) | `node bin/tusq.js help \| grep -c '^  [a-z]'` → 27; `node bin/tusq.js help` output contains `request` between `redaction` and `response` (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-441 | M43 default closed-enum run on canonical express fixture produces `sources[]` in order `path` (1 capability: get_users_api_v1_users_id) then `request_body` (1 capability: post_users_users) then `none` (1 capability: get_users_users); `warnings: []`; exit 0; result field name is `sources` (NOT `tiers`, NOT `types`) | `node bin/tusq.js request index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON with sources: [{input_schema_primary_parameter_source:"path", capability_count:1,...}, {input_schema_primary_parameter_source:"request_body", capability_count:1,...}, {input_schema_primary_parameter_source:"none", capability_count:1,...}], warnings:[] (independently verified this turn) | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-442 | M43 `--source path` filter returns only the path bucket (exit 0); `--source request_body` returns only the request_body bucket (exit 0); absent-source filter (`--source query` when no query capabilities) exits 0 with empty sources[] | `node bin/tusq.js request index --source path --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, single path bucket (independently verified); `node bin/tusq.js request index --source request_body ... --json` → exit 0, single request_body bucket (independently verified); `--source query` on express fixture → exit 0, sources:[] | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-443 | M43 `--source query`, `--source header`, `--source mixed`, `--source none`, `--source unknown` filters: each exits 0 with sources[] containing only matching bucket or empty when no match; `--source none` on express fixture → exit 0 with none bucket (get_users_users) | Smoke M43(c-f): per-source filter cases confirmed; absent-source filter not fatal | PASS | 2026-04-27 (smoke) | PASS |
| REQ-444 | M43 `--source REQUEST_BODY` (uppercase) exits 1 with `Unknown input schema primary parameter source: REQUEST_BODY` on stderr, empty stdout (case-sensitive enforcement); `--source PATH`, `--source NONE`, `--source MIXED` all exit 1 similarly | `node bin/tusq.js request index --source REQUEST_BODY --manifest tests/fixtures/express-sample/tusq.manifest.json` → exit 1, stderr `Unknown input schema primary parameter source: REQUEST_BODY`, empty stdout (independently verified this turn) | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-445 | M43 `--source xyz` (arbitrary unknown literal) exits 1 with `Unknown input schema primary parameter source: xyz` on stderr, empty stdout | Smoke M43(h): `--source xyz` → exit 1, correct rejection message confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-446 | M43 `--manifest <missing>` exits 1 with `Manifest file not found: <path>` on stderr, empty stdout | Smoke M43(i): missing manifest path → exit 1, stderr `Manifest file not found:` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-447 | M43 `--manifest <malformed.json>` exits 1 with `Failed to parse manifest JSON: <message>` on stderr, empty stdout | Smoke M43(j): malformed JSON manifest → exit 1, stderr `Failed to parse manifest JSON:` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-448 | M43 `--manifest <no-capabilities>` exits 1 with `Manifest is missing capabilities array` on stderr, empty stdout | Smoke M43(k): manifest missing capabilities array → exit 1, stderr `Manifest is missing capabilities array` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-449 | M43 unknown flag exits 1 with `Unknown flag: <flag>` on stderr, empty stdout | Smoke M43(l): `--unknown-flag` → exit 1, stderr `Unknown flag:` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-450 | M43 `--source` with no value exits 1 with `--source requires a value` on stderr | Smoke M43(m): `--source` with no following value → exit 1, stderr `--source requires a value` confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-451 | M43 `--out <valid path>` writes index JSON to file; stdout empty on success; exit 0 | Smoke M43(n): `--out /tmp/m43-out.json` writes valid JSON; stdout empty; exit 0 confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-452 | M43 `--out .tusq/foo.json` exits 1 with `--out path must not be inside .tusq/` on stderr, empty stdout (detection-before-output rejection) | Smoke M43(o): `--out .tusq/request-index.json` → exit 1, rejection confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-453 | M43 `--json` output is valid JSON with `sources[]` array (NOT `tiers[]`, NOT `types[]`), `warnings[]` (always present, even when empty), and all required top-level fields: `manifest_path`, `manifest_version`, `generated_at`, `sources`, `warnings`; result field name `sources` is plural categorical (not a numeric tier) | `node bin/tusq.js request index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → exit 0, valid JSON shape with `sources` field (NOT `tiers`, NOT `types`) verified (independently verified this turn); `warnings` always present for shape stability | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-454 | M43 determinism: three consecutive `tusq request index` runs on same manifest produce byte-identical stdout in both human and `--json` modes | Smoke M43(q): three consecutive runs → byte-identical stdout confirmed; eval `input-schema-primary-parameter-source-index-determinism` (34th scenario) also verifies | PASS | 2026-04-27 (smoke + eval) | PASS |
| REQ-455 | M43 manifest read-only invariant: manifest mtime, SHA-256, and per-capability `capability_digest` unchanged after `tusq request index` run; non-persistence: `input_schema_primary_parameter_source` field MUST NOT appear in `tusq.manifest.json` after run | Smoke M43(r): mtime/SHA/digest unchanged; `tusq.manifest.json` does not contain `input_schema_primary_parameter_source` field confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-456 | M43 compile byte-identity: `tusq compile` output is byte-identical before and after `tusq request index` run | Smoke M43(r): compile output byte-identical pre/post M43 run confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-457 | M43 peer index commands byte-identity: all 13 peer index commands (surface plan, domain index, effect index, sensitivity index, method index, auth index, confidence index, pii index, examples index, input index, output index, path index, response index) all byte-identical pre/post M43 | Smoke M43(s): all peer index commands byte-identical confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-458 | M43 empty-capabilities: manifest with `capabilities: []` → exit 0; human stdout `No capabilities in manifest — nothing to index.`; `--json` stdout `{ manifest_path, manifest_version, generated_at, sources: [], warnings: [] }` | Smoke M43(t): empty-capabilities manifest → exit 0, correct human and JSON output confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-459 | M43 malformed `input_schema` or property `source` field: null/missing/non-object input_schema → unknown + `input_schema_field_missing` or `input_schema_field_not_object`; missing `properties` field → unknown + `input_schema_properties_field_missing`; non-object `properties` (null, array) → unknown + `input_schema_properties_field_not_object`; any property source missing/non-string/outside closed four-value set (cookie/file/multipart/form-data/etc.) → unknown + `input_schema_property_source_field_missing_or_invalid`; human mode: stderr `Warning: capability '<name>' has malformed input_schema (<reason>)`; JSON mode: `warnings[]` entry with capability name and frozen reason code; `none` bucket does NOT emit a warning (valid named bucket); only `unknown` triggers warnings | Smoke M43(u): malformed input_schema capabilities produce warnings[] entries; none bucket (get_users_users in fixture) produces no warning; all five frozen reason codes confirmed | PASS | 2026-04-27 (smoke) | PASS |
| REQ-460 | M43 closed two-value `aggregation_key` enum: `source` for known sources (path/request_body/query/header/mixed/none buckets), `unknown` for unknown bucket; NO other values — specifically NOT `tier`, NOT `type` (M43 axis is HTTP-anatomy locus class, aggregation key is `source`) | `node bin/tusq.js request index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → path bucket `aggregation_key: "source"`, request_body bucket `aggregation_key: "source"`, none bucket `aggregation_key: "source"` confirmed (independently verified this turn); `INPUT_SCHEMA_PRIMARY_PARAMETER_SOURCE_AGGREGATION_KEY_ENUM` frozen Set `{source, unknown}` verified at `src/cli.js:247` | PASS | 2026-04-27 (CLI + code inspection) | PASS |
| REQ-461 | M43 empty buckets absent; closed 7-value iteration order `path → request_body → query → header → mixed → none → unknown`: empty buckets do not appear; buckets iterate in HTTP anatomy reading order; `unknown` always appended last; explicitly NOT security-blast-radius-ranked, NOT workflow-criticality-ranked | `node bin/tusq.js request index --manifest tests/fixtures/express-sample/tusq.manifest.json --json` → `sources[]` contains only `path`, `request_body`, `none` buckets (query/header/mixed/unknown absent from fixture); `path` appears before `request_body` which appears before `none` (closed-enum order confirmed); smoke M43(a) confirms closed-enum bucket ordering | PASS | 2026-04-27 (CLI + smoke) | PASS |
| REQ-462 | M43 per-bucket 8-field entry shape correct: `input_schema_primary_parameter_source`, `aggregation_key`, `capability_count`, `capabilities[]` (manifest declared order), `approved_count`, `gated_count`, `has_destructive_side_effect`, `has_restricted_or_confidential_sensitivity`; has_destructive_side_effect: `true` iff any capability in bucket has `side_effect_class === 'destructive'`; has_restricted_or_confidential_sensitivity: `true` iff any has `sensitivity_class === 'restricted'` or `=== 'confidential'` | All 8 fields present in `node bin/tusq.js request index --manifest ... --json` sources[] entries; fixture path-bucket: approved_count 0, gated_count 1; request_body-bucket: approved_count 1, gated_count 0; none-bucket: approved_count 1, gated_count 0; all has_destructive_side_effect false and has_restricted_or_confidential_sensitivity false confirmed | PASS | 2026-04-27 (CLI inspection) | PASS |
| REQ-463 | M43 `INPUT_SCHEMA_PROPERTY_SOURCE_VALUE_SET` (closed four-value: path/request_body/query/header) is a separate frozen constant from the seven-value bucket-key enum; cookie/file/multipart/form-data source values all map to `unknown` bucket with reason `input_schema_property_source_field_missing_or_invalid`; array-of-sources also maps to `unknown`; `none` bucket is produced by empty properties object (NOT by missing/malformed input_schema) | `INPUT_SCHEMA_PROPERTY_SOURCE_VALUE_SET` frozen Set `{path, request_body, query, header}` verified at `src/cli.js:259`; smoke M43(u): cookie/unknown source values → unknown bucket + warning confirmed; none bucket represents zero-property capabilities (valid named bucket, no warning) | PASS | 2026-04-27 (code inspection + smoke) | PASS |
| REQ-464 | M43 `classifyInputSchemaPrimaryParameterSource` boundary conditions: null/undefined input_schema → unknown; non-object/Array input_schema → unknown; input_schema with no `properties` key → unknown; properties null/undefined/non-object/Array → unknown; 0 property keys → none (no warning); any property null/undefined/non-object/Array → unknown; any property missing `source` key → unknown; any property source non-string or not in {path,request_body,query,header} → unknown; seenSources.size === 1 → that source literal; seenSources.size > 1 (mixed loci) → mixed; optional-vs-required collapse: every property's source consulted regardless of input_schema.required[] membership | `classifyInputSchemaPrimaryParameterSource` at `src/cli.js:5204–5247` independently read: null/undefined → unknown; non-object/Array → unknown; no `properties` key → unknown; properties null/undefined/non-object/Array → unknown; propKeys.length === 0 → none; per-property null/undefined/non-object/Array → unknown; missing `source` → unknown; non-string or outside closed four-value set → unknown; seenSources.size === 1 → that value; else → mixed; thresholds immutable at M43 ship | PASS | 2026-04-27 (code inspection) | PASS |
