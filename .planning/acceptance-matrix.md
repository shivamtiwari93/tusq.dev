# Acceptance Matrix — tusq.dev v0.1.0

| Req # | Requirement | Acceptance criteria | Test status | Last tested | Status |
|-------|-------------|---------------------|-------------|-------------|--------|
| REQ-001 | `tusq help` prints command list and exits 0 | stdout contains all 6 commands; exit code 0 | PASS | 2026-04-18 | PASS |
| REQ-002 | `tusq version` prints version and exits 0 | stdout is `0.1.0\n`; exit code 0 | PASS | 2026-04-18 | PASS |
| REQ-003 | Unknown command exits 1 with usage | exit code 1; stdout contains "Unknown command" | PASS | 2026-04-18 | PASS |
| REQ-004 | Invalid flag prints command usage and exits 1 | exit code 1; stdout contains "Usage: tusq scan" | PASS | 2026-04-18 | PASS |
| REQ-005 | `tusq init` creates tusq.config.json | file exists after init; idempotent on re-run | PASS | 2026-04-18 | PASS |
| REQ-006 | `tusq scan` without init exits 1 with message | "No tusq config found" message; exit code 1 | PASS | 2026-04-18 | PASS |
| REQ-007 | `tusq scan` detects Express routes | route_count ≥ 2; framework = express | PASS | 2026-04-18 | PASS |
| REQ-008 | `tusq scan` detects Fastify routes | scan completes exit 0; framework = fastify | PASS | 2026-04-18 | PASS |
| REQ-009 | `tusq scan` detects NestJS routes | scan completes exit 0; framework = nestjs | PASS | 2026-04-18 | PASS |
| REQ-010 | `tusq manifest` requires prior scan | "No scan data found" message; exit code 1 | PASS | 2026-04-18 | PASS |
| REQ-011 | `tusq manifest` generates tusq.manifest.json | file created with capabilities array | PASS | 2026-04-18 | PASS |
| REQ-012 | Approval persists across manifest regeneration | approved=true survives re-run of manifest | PASS | 2026-04-18 (smoke) | PASS |
| REQ-013 | `tusq compile --dry-run` exits 0 without writing files | exit code 0; no tusq-tools/ dir created | PASS | 2026-04-18 (smoke) | PASS |
| REQ-014 | `tusq compile` produces tool definition files | tusq-tools/ directory created | PASS | 2026-04-18 (smoke) | PASS |
| REQ-015 | `tusq review` requires manifest | "No manifest found" message; exit code 1 | PASS | 2026-04-18 | PASS |
| REQ-016 | `tusq review` prints capability summary | exit code 0 after manifest exists | PASS | 2026-04-18 (smoke) | PASS |
| REQ-017 | `tusq serve` starts MCP HTTP server | "server listening" on stdout; responds to RPC | PASS | 2026-04-18 (smoke) | PASS |
| REQ-018 | MCP `tools/list` returns capability array | result.tools is non-empty array | PASS | 2026-04-18 (smoke) | PASS |
| REQ-019 | MCP `tools/call` returns schema | result.schema present | PASS | 2026-04-18 (smoke) | PASS |
| REQ-020 | SIGINT shuts down serve cleanly | exit code 0 after SIGINT | PASS | 2026-04-18 (smoke) | PASS |
| REQ-021 | All commands accept `--verbose` flag | no error on --verbose; extra output produced | PASS | 2026-04-18 (smoke) | PASS |
| REQ-022 | Per-command `--help` prints usage | exit code 0; usage line for each sub-command | PASS | 2026-04-18 | PASS |
| REQ-023 | `website/` reflects the migrated live homepage from `websites/` | Docusaurus homepage preserves the legacy hero, three-card explanation band, workflow steps, and V1 surface grid in `website/src/pages/index.tsx` | PASS | 2026-04-19 | PASS |
| REQ-024 | Docusaurus preserves the legacy site's not-found recovery behavior and styling cues | `website/src/pages/404.tsx`, `website/src/pages/index.module.css`, and `website/src/css/custom.css` retain the warm gradient, Fraunces/Space Grotesk typography, paper-card styling, and a path back to home | PASS | 2026-04-19 | PASS |
| REQ-025 | Implementation notes explicitly record website consolidation and gate-unblocking changes | `.planning/IMPLEMENTATION_NOTES.md` contains a literal `## Changes` heading and states that `website/` is canonical while `websites/` is legacy-only cleanup residue | PASS | 2026-04-19 | PASS |
| REQ-026 | Scan output carries provenance back to source (file + line) for each discovered capability | Each route in `.tusq/scan.json` contains `provenance.file` and `provenance.line` fields pointing to the originating source file | PASS | 2026-04-19 | PASS |
| REQ-027 | Manifest preserves capability provenance from scan | Each capability in `tusq.manifest.json` retains `provenance.file` and `provenance.line` from the originating scan | PASS | 2026-04-19 | PASS |
| REQ-028 | Compiled tool definitions carry provenance to the canonical artifact | Each `tusq-tools/<name>.json` file includes `provenance.file` and `provenance.line` linking the tool back to its source route declaration | PASS | 2026-04-19 | PASS |
| REQ-029 | Post-v0.1.0 roadmap page exists and explicitly marks items as non-shipped | `website/docs/roadmap.md` exists, sidebar_position is 8, and file contains the literal string "None of the items below are shipped behavior in `v0.1.0`" | PASS | 2026-04-19 | PASS |
| REQ-030 | Manifest format doc exists, is sidebar-wired, and accurately documents V1 schema limitations | `website/docs/manifest-format.md` exists at sidebar_position 3, is listed in `website/sidebars.ts` items, and contains `## V1 schema limitations` section with the conservative schema shape | PASS | 2026-04-19 | PASS |
| REQ-031 | `sensitivity_class` field is emitted at every pipeline stage and defaults to `unknown` in V1 | `tusq.manifest.json` capabilities each have `sensitivity_class: "unknown"`; compiled `tusq-tools/*.json` files include `sensitivity_class: "unknown"`; MCP `tools/list` and `tools/call` responses include `sensitivity_class: "unknown"`; `website/docs/manifest-format.md` documents all 5 levels and V1 default | PASS | 2026-04-19 | PASS |
| REQ-032 | `auth_hints` field is emitted at every pipeline stage and MCP runtime responses include it as an array | `tusq.manifest.json` capabilities each have `auth_hints` (string[]); compiled `tusq-tools/*.json` files include `auth_hints`; MCP `tools/list` response includes `auth_hints` as an array; MCP `tools/call` response includes `auth_hints` as an array; smoke tests assert `Array.isArray(auth_hints)` for both MCP methods; `website/docs/mcp-server.md` lists `auth_hints` in governance metadata | PASS | 2026-04-20 | PASS |
| REQ-033 | `examples` and `constraints` fields are emitted at manifest, compiled tools, and MCP `tools/call` stages (not scan or `tools/list` — by design per SYSTEM_SPEC) | `tusq.manifest.json` capabilities each have `examples[]` (default describe-only placeholder) and `constraints` object with 5 null/empty defaults; compiled `tusq-tools/*.json` include both fields propagated from manifest; MCP `tools/call` response includes both fields; smoke tests assert default examples/constraints on manifest, assert custom examples/constraints survive manifest→compile propagation and manifest→tools/call propagation; `website/docs/manifest-format.md` documents both fields with V1 limitations; SYSTEM_SPEC.md lines 564-571 explicitly omit `examples` from `tools/list` and scan — confirmed intentional | PASS | 2026-04-19 | PASS |
| REQ-034 | `redaction` field propagates at manifest, compile, and MCP `tools/call` stages; `tools/list` intentionally excludes it; `approved_by`/`approved_at` are manifest-only audit fields that survive regeneration and are explicitly absent from compile output and MCP responses | `tusq.manifest.json` capabilities each have `redaction` with permissive V1 defaults (`pii_fields: []`, `pii_categories: []`, `log_level: "full"`, `mask_in_traces: false`, `retention_days: null`) and `approved_by`/`approved_at` (null by default); custom values survive manifest regeneration except M25/M26-owned `pii_fields`/`pii_categories`, which are re-derived from source-declared schema keys; compiled `tusq-tools/*.json` include `redaction` propagated from manifest but do NOT contain `approved_by`, `approved_at`, `approved`, or `review_needed`; MCP `tools/list` does NOT include `redaction`; MCP `tools/call` includes `redaction` but does NOT include approval metadata; smoke-test assertions cover all boundaries (`node tests/smoke.mjs` → exit 0); `website/docs/manifest-format.md` documents `redaction` and `approved_by`/`approved_at` (manifest-only audit trail); `website/docs/mcp-server.md` states tools/call includes `redaction` and approval metadata is manifest-only | PASS | 2026-04-22 (smoke + docs) | PASS |
| REQ-035 | Version-history fields are emitted in manifest only (`manifest_version`, `previous_manifest_hash`, `capability_digest`) with deterministic hashing and strict non-propagation downstream | `tusq.manifest.json` root includes `manifest_version` (integer counter) and `previous_manifest_hash` (SHA-256 of prior manifest bytes, or `null` when no prior file exists); each capability includes 64-char lowercase hex `capability_digest` computed from content fields only (excluding `approved`, `approved_by`, `approved_at`, `review_needed`, and `capability_digest`); smoke tests verify digest stability on approval-only edits, digest changes on content edits, and assert version-history fields are absent from compiled `tusq-tools/*.json` and MCP `tools/call`; docs updated in `website/docs/manifest-format.md` and `website/docs/mcp-server.md` to declare manifest-only boundary | PASS | 2026-04-20 | PASS |
| REQ-036 | Framework-specific deep extraction: Fastify correctly detects both 2-arg and 3-arg inline forms and deduplicates; NestJS composes controller prefix + method decorator paths and inherits class-level guards to all methods | Fastify scan returns 2 routes for fixture with `fastify.get(path, {schema}, handler)` (3-arg inline) and `fastify.route({method, url, handler})` (route-object); GET route `handler=listOrders` and `input_schema` reflects schema hints from options block; POST route carries `auth_hints` including `requireAuth`; NestJS scan returns 2 routes with composed paths (`/users`, `/users/:id/disable`); GET route `auth_hints` includes `AuthGuard` (inherited from class decorator); POST route `auth_hints` includes both `AuthGuard` (inherited) and `AdminGuard` (method-level); smoke tests assert all 7 framework-specific properties (`node tests/smoke.mjs` → exit 0) | PASS | 2026-04-20 | PASS |
| REQ-037 | First-pass manifest usability (M15): path parameter extraction into `input_schema`, prefix-aware domain inference, rich capability descriptions, and schema-miss confidence penalty so schema-less routes surface for review | Scan of Express fixture with `app.get('/api/v1/users/:id', requireAuth, getUser)` yields `domain="users"` (prefix segments `api`, `v1` skipped); `input_schema.properties.id={type:"string",source:"path"}` and `required=["id"]`; capability description equals `Retrieve user by id - read-only, requires requireAuth (handler: getUser)`; confidence drops below 0.8 for schema-less named-handler routes (-0.10 schema-miss penalty applied in `scoreConfidence`) → `review_needed=true`; smoke tests assert domain, path-parameter properties, rich description substrings, and sub-0.8 confidence (`node tests/smoke.mjs` → exit 0) | PASS | 2026-04-21 | PASS |
| REQ-038 | Manifest review governance improvements: `tusq review --strict` blocks ship on unapproved or low-confidence capabilities; review output summarizes inferred inputs, returns, and source/handler/framework provenance; input/output schemas are inferred (body params for writes, array/object shapes from handler returns); path parameters are tagged with `source: "path"` through manifest and compile | `tusq review --strict` against an unapproved fixture manifest exits 1 with `Review gate failed: …` on stderr; same command with all capabilities approved and not flagged exits 0; `tusq review --verbose` output includes `inputs=body:request_body`, `returns=array<object>`, `source=src/app.ts`, `handler=listUsers`, `framework=express`; manifest POST `/users` carries `input_schema.properties.body.source="request_body"`; manifest GET `/users` carries `output_schema.type="array"`; manifest POST `/users` carries `output_schema.properties.ok.type="boolean"`; manifest capabilities carry `provenance.framework` and `provenance.handler`; NestJS POST `/users/:id/disable` manifest `input_schema.properties.id.source="path"`; all assertions covered by `node tests/smoke.mjs` → exit 0 | PASS | 2026-04-21 | PASS |
| REQ-039 | `tusq diff` compares two explicit manifest files and reports capability change counts | Fixture manifests with added, removed, changed, and unchanged capabilities produce correct summary counts; invalid or missing `--from`/`--to` paths exit 1 with actionable errors | PASS | 2026-04-21 (smoke) | PASS |
| REQ-040 | `tusq diff --json` emits stable structured output for automation | Output parses as JSON and contains `from_version`, `to_version`, `summary`, and `changes[]`; no human prose is mixed into JSON mode | PASS | 2026-04-21 (smoke) | PASS |
| REQ-041 | Changed capabilities are detected by `capability_digest` and include field-level change names | Same-name capabilities with different digests are classified as `changed`; output includes `previous_digest`, `current_digest`, and `fields_changed[]` | PASS | 2026-04-21 (smoke) | PASS |
| REQ-042 | `tusq diff --review-queue` lists capabilities needing human review | Review queue includes added capabilities, changed capabilities, unapproved capabilities, and `review_needed=true` capabilities with governance and provenance metadata when available | PASS | 2026-04-21 (smoke) | PASS |
| REQ-043 | `tusq diff --fail-on-unapproved-changes` works as a CI gate | Command exits 1 when any added or changed capability is unapproved; exits 0 when added or changed capabilities are approved and no review-needed gate remains | PASS | 2026-04-21 (smoke) | PASS |
| REQ-044 | Help and docs expose the diff workflow honestly | `tusq help`, `tusq diff --help`, README, CLI reference, and manifest docs document `tusq diff` without claiming history-store, git integration, or automatic re-approval support | PASS | 2026-04-21 (smoke + file inspection) | PASS |
| REQ-045 | Governed CLI eval scenarios are versioned and repo-local | `tests/evals/governed-cli-scenarios.json` exists, declares schema_version `1.0`, and contains at least two scenarios covering governed workflow and manifest diff regression behavior | PASS | 2026-04-21 (eval) | PASS |
| REQ-046 | Eval runner validates review-gate behavior before compile | `tests/eval-regression.mjs` runs `tusq review --strict`, expects the low-confidence/unapproved sentinel to fail, then approves the manifest and verifies strict review passes before compile | PASS | 2026-04-21 (eval) | PASS |
| REQ-047 | Eval runner validates compiled tool-call metadata boundaries | Eval assertions verify compiled tools preserve auth hints, side-effect class, schema source markers, examples, constraints/redaction defaults, and do not leak manifest-only approval metadata | PASS | 2026-04-21 (eval) | PASS |
| REQ-048 | Eval runner validates manifest diff review-queue and CI gate behavior | Eval assertions create previous/current manifests, verify changed capability field reporting and review-queue inclusion, and require `--fail-on-unapproved-changes` to fail for the changed unapproved capability | PASS | 2026-04-21 (eval) | PASS |
| REQ-049 | Full project test command executes both smoke and eval suites | `npm test` runs `node tests/smoke.mjs && node tests/eval-regression.mjs` and exits 0 with both "Smoke tests passed" and "Eval regression harness passed (2 scenarios)" | PASS | 2026-04-21 (npm test) | PASS |
| REQ-050 | `tusq approve` is exposed in command help and per-command usage | `tusq help` includes `approve`; `tusq approve --help` prints capability-name, `--all`, `--reviewer`, `--manifest`, `--dry-run`, and `--json` usage | PASS | 2026-04-21 (smoke) | PASS |
| REQ-051 | `tusq approve <name>` approves exactly one capability with audit metadata | Smoke coverage selects one unapproved capability, writes `approved: true`, clears `review_needed`, sets `approved_by` from `--reviewer`, and records ISO-like `approved_at` | PASS | 2026-04-21 (smoke) | PASS |
| REQ-052 | `tusq approve --all` intentionally approves all unapproved or review-needed capabilities | Smoke coverage verifies every manifest capability is `approved: true` and `review_needed: false` after `tusq approve --all --reviewer <id>` | PASS | 2026-04-21 (smoke) | PASS |
| REQ-053 | `tusq approve --dry-run --json` is safe for review automation | Smoke coverage parses dry-run JSON and confirms the manifest approval metadata remains unchanged | PASS | 2026-04-21 (smoke) | PASS |
| REQ-054 | `tusq docs` is exposed in command help and per-command usage | `tusq help` includes `docs`; `tusq docs --help` prints `--manifest`, `--out`, and `--verbose` usage; exit code 0 | PASS | 2026-04-22 (smoke) | PASS |
| REQ-055 | `tusq docs` generates Markdown with required structural sections | stdout includes `# Capability Documentation`, `## Manifest`, `## Capabilities`, `Manifest version`, `Approved: yes`, `Side effect class`, `Sensitivity class`, `Auth hints`, `#### Examples`, `#### Constraints`, `#### Redaction`, `#### Provenance` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-056 | `tusq docs --out <path>` writes deterministic Markdown matching stdout | `--out` writes a file whose content is byte-identical to the stdout output from the same manifest; smoke asserts file content equals stdout | PASS | 2026-04-22 (smoke) | PASS |
| REQ-057 | `tusq docs` exits 1 with actionable message when manifest is not found | Missing or non-existent `--manifest` path exits 1 with `Manifest not found: <path>` | PASS | 2026-04-22 (CLI) | PASS |
| REQ-058 | `tusq serve --policy` startup failure UX | Missing policy file exits 1 with `Policy file not found: <path>`; invalid JSON exits 1 with `Invalid policy JSON at: <path>`; unsupported schema_version exits 1 with `Unsupported policy schema_version: <v>`; unknown mode exits 1 with `Unknown policy mode: <mode>`; invalid allowed_capabilities exits 1 with `Invalid allowed_capabilities in policy:` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-059 | `describe-only` policy mode is a no-op on `tools/call` | Starting serve with `mode: "describe-only"` produces same `tools/call` response shape as no policy; no `executed` field present in the response | PASS | 2026-04-22 (smoke) | PASS |
| REQ-060 | `dry-run` mode returns `executed: false` with plan shape and policy echo | `tools/call` under `mode: "dry-run"` returns `executed: false`, `dry_run_plan` with `method`, `path` (with path-param substitution), `path_params`, `plan_hash` (64-char hex), `evaluated_at`, and a `policy` echo with `mode`, `reviewer`, `approved_at` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-061 | `plan_hash` is deterministic across repeated calls | Same tool call arguments produce identical `plan_hash` on repeated invocations; different argument values produce a different hash | PASS | 2026-04-22 (smoke) | PASS |
| REQ-062 | Argument validation failure returns `-32602` with structured errors | Missing required argument returns JSON-RPC error code `-32602` with `data.validation_errors` array containing at least one entry with `path` (e.g. `/id`) and `reason` (e.g. `required field missing`) fields | PASS | 2026-04-22 (smoke) | PASS |
| REQ-063 | `allowed_capabilities` filter blocks non-listed tools under policy | A capability NOT in `allowed_capabilities` returns error code `-32602` with `data.reason: 'capability not permitted under current policy'`; a capability IN the list succeeds with `executed: false` dry-run response | PASS | 2026-04-22 (smoke) | PASS |
| REQ-064 | `tusq policy init` is exposed in command help and default generation produces a valid policy file | `tusq help` includes `policy`; `tusq policy init --help` prints full flag synopsis including `--mode`, `--reviewer`, `--allowed-capabilities`, `--out`, `--force`, `--dry-run`, `--json`, `--verbose`; default generation without flags produces a file with `schema_version:"1.0"`, `mode:"describe-only"`, non-empty reviewer string, ISO-8601 `approved_at`, and no `allowed_capabilities` field | PASS | 2026-04-22 (smoke) | PASS |
| REQ-065 | `tusq policy init` flag surface: `--mode`, `--allowed-capabilities`, and error exits | `--mode dry-run` sets `mode:"dry-run"` in output; `--allowed-capabilities a,b` produces exact trimmed/deduplicated `["a","b"]` array; unknown `--mode live-fire` exits 1 with `Unknown policy mode: live-fire`; empty `--allowed-capabilities ","` exits 1 with `Invalid allowed-capabilities:` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-066 | `tusq policy init` file-conflict protection and `--force` override | Running `tusq policy init --out <existing>` a second time exits 1 with `Policy file already exists:` and `--force` in stderr; `tusq policy init --out <existing> --force` overwrites cleanly and produces a valid `schema_version:"1.0"` file | PASS | 2026-04-22 (smoke) | PASS |
| REQ-067 | `tusq policy init --dry-run` prints valid JSON to stdout and does NOT write the target file | `--dry-run` output parses as JSON with `schema_version:"1.0"`; the `--out` target file is not created when `--dry-run` is set | PASS | 2026-04-22 (smoke) | PASS |
| REQ-068 | Generated policy file passes `loadAndValidatePolicy` round-trip | `tusq policy init --mode dry-run` followed by `tusq serve --policy <generated>` starts successfully (exits `waitForReady` → SIGINT → exit 0); confirms generator output cannot drift from the M20 validator | PASS | 2026-04-22 (smoke) | PASS |
| REQ-069 | Eval harness covers `policy-init-generator-round-trip` scenario | `tests/evals/governed-cli-scenarios.json` contains a `policy-init-generator-round-trip` scenario; `npm test` runs 5 scenarios and exits 0 with `Eval regression harness passed (5 scenarios)` | PASS | 2026-04-22 (eval) | PASS |
| REQ-070 | `tusq policy verify` is exposed in command help and round-trip init→verify succeeds | `tusq policy verify --help` exits 0 and stdout includes `tusq policy verify`; `tusq policy init --mode dry-run --out <path>` followed by `tusq policy verify --policy <path>` exits 0 with `Policy valid:` message including mode and reviewer | PASS | 2026-04-22 (smoke) | PASS |
| REQ-071 | `tusq policy verify --json` success shape | `--json` on a valid policy file exits 0 and stdout parses as JSON with `valid: true`, `path`, and `policy` object containing `schema_version`, `mode`, `reviewer`, `approved_at`, and `allowed_capabilities` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-072 | `tusq policy verify` exits 1 on missing file and `--json` failure shape | Missing policy path exits 1 with `Policy file not found: <path>`; `--json` on a missing file exits 1 and stdout parses as JSON with `valid: false`, `path`, and `error` fields | PASS | 2026-04-22 (smoke) | PASS |
| REQ-073 | `tusq policy verify` exits 1 on all four malformed-policy failure conditions | Malformed JSON exits 1 with `Invalid policy JSON at:`; unsupported `schema_version` exits 1 with `Unsupported policy schema_version:`; unknown `mode` exits 1 with `Unknown policy mode:`; non-array `allowed_capabilities` exits 1 with `Invalid allowed_capabilities in policy:` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-074 | `tusq policy verify` and `tusq serve --policy` produce identical stderr for all four malformed-policy fixtures | For each of the four bad fixtures (bad JSON, bad schema_version, bad mode, bad allowed_capabilities): both `tusq policy verify --policy <fixture>` and `tusq serve --policy <fixture>` exit 1; the error message produced by `verify` is a substring of the error message produced by `serve` (shared `loadAndValidatePolicy()` call site) | PASS | 2026-04-22 (smoke) | PASS |
| REQ-075 | `tusq policy verify --strict` is exposed in help and default (no-`--strict`) verify behavior is byte-for-byte identical to M22 | `tusq policy verify --help` exits 0 and stdout includes `[--strict [--manifest <path>]]`; running `tusq policy verify` without `--strict` on a valid policy exits 0 with `Policy valid:` message and emits no `strict` or `manifest` keywords — confirming M22 code path is unmodified even when a manifest file is present in the working directory | PASS | 2026-04-22 (smoke) | PASS |
| REQ-076 | `--strict` cross-references every `allowed_capabilities` entry against the manifest and exits 1 on not-in-manifest, not-approved, or requires-review | `tusq policy verify --strict --manifest <path>` exits 0 when all allowed capabilities are present, approved, and not `review_needed`; exits 1 with `Strict policy verify failed: allowed capability not found in manifest: <name>` for a missing cap; exits 1 with `Strict policy verify failed: allowed capability not approved: <name>` for an unapproved cap (`approved: false`); exits 1 with `Strict policy verify failed: allowed capability requires review: <name>` for a `review_needed: true` cap | PASS | 2026-04-22 (smoke) | PASS |
| REQ-077 | `--strict` exits 1 with actionable messages for missing manifest, malformed manifest JSON, and `--manifest` without `--strict` | `--strict --manifest <missing>` exits 1 with `Manifest not found: <path>`; `--strict --manifest <bad-json>` exits 1 with `Invalid manifest JSON at: <path>`; `--manifest <path>` without `--strict` exits 1 with `--manifest requires --strict` before any file read (Constraint 11 guard — manifests file is never opened) | PASS | 2026-04-22 (smoke) | PASS |
| REQ-078 | `--strict --json` success shape includes `valid:true`, `strict:true`, `manifest_path`, `manifest_version`, and `approved_allowed_capabilities`; failure shape includes `strict_errors` array | `--strict --json` on a passing policy exits 0 and stdout parses as JSON with `valid:true`, `strict:true`, `manifest_path`, `manifest_version`, and `approved_allowed_capabilities` count; `--strict --json` on a failing policy exits 1 and stdout parses as JSON with `valid:false`, `strict:true`, `strict_errors` array with `{name,reason}` entries; `reason` is one of `not_in_manifest`, `not_approved`, `requires_review` | PASS | 2026-04-22 (smoke) | PASS |
| REQ-079 | `--strict` with unset or null `allowed_capabilities` passes trivially without reading any capability from the manifest | `tusq policy verify --strict --manifest <path>` on a policy with no `allowed_capabilities` field exits 0 with `Policy valid (strict):`; the trivial-pass path does not iterate the manifest capabilities array | PASS | 2026-04-22 (smoke) | PASS |
| REQ-080 | M22 parity is preserved under `--strict` for all four M22 failure fixtures, and `policy-strict-verify-determinism` eval scenario asserts deterministic `strict_errors` ordering across three repeat runs | For each of the four M22 bad-fixture files (bad JSON, bad schema_version, bad mode, bad allowed_capabilities): `tusq policy verify --policy <fixture>` and `tusq policy verify --policy <fixture> --strict` both exit 1 and their stderr messages are byte-identical; `tests/evals/governed-cli-scenarios.json` contains `policy-strict-verify-determinism`; `npm test` exits 0 with `Eval regression harness passed (6 scenarios)` | PASS | 2026-04-22 (smoke + eval) | PASS |
| REQ-081 | Fastify route with a literal `schema: { body: { properties: {...}, required: [...] } }` block produces `input_schema` with declared top-level fields merged in, `additionalProperties: false`, `source: 'fastify_schema_body'`, and property keys in declaration order | POST /items in Fastify fixture yields `input_schema.source='fastify_schema_body'`, `additionalProperties: false`, properties `{name:{type:'string'}, price:{type:'number'}, active:{type:'boolean'}}` with keys in that declaration order, and `required` containing `name` and `price` | PASS | 2026-04-22 (smoke M24-b) | PASS |
| REQ-082 | Path-param collision — when body schema declares a field name matching a path parameter, the path parameter entry wins (`source: 'path'`, `type: 'string'`); body-declared value is silently dropped | PUT /items/:id body declares `id:{type:'number'}` but the path parameter `:id` is authoritative; manifest `input_schema.properties.id` carries `source:'path'` and `type:'string'`; body `name` field (no collision) is present | PASS | 2026-04-22 (smoke M24-d) | PASS |
| REQ-083 | Fastify routes without a literal body-properties block fall back to M15 behavior byte-for-byte — no `fastify_schema_body` source tag, `additionalProperties: true` | GET /orders (schema with `response` only, no `body` key) and POST /orders (schema.body exists but no `properties` key) both produce `additionalProperties: true` with no `source` tag; GET /catalog (schema is a variable reference `sharedSchema`, not a literal object) also falls back to M15 with no extraction | PASS | 2026-04-22 (smoke M24-a, M24-c) | PASS |
| REQ-084 | Express and NestJS fixtures produce zero `fastify_schema_body` source tags; M24 is Fastify-only | Scanning `tests/fixtures/express-sample` after M24 yields no capability with `input_schema.source='fastify_schema_body'`; scanning `tests/fixtures/nest-sample` after M24 yields no route with `input_schema.source='fastify_schema_body'` | PASS | 2026-04-22 (smoke M24-e, M24-f) | PASS |
| REQ-085 | `schema_fields` presence adds +0.04 confidence boost in `scoreConfidence()` on top of the existing `schema_hint` signal (capped at 0.95); no other scoring branches are affected | `src/cli.js` scoreConfidence(): `if (route.schema_fields) { score += 0.04; }` is present after the `schema_hint` branch; routes without `schema_fields` retain byte-identical confidence scores; smoke test confirms no Express/NestJS confidence regression | PASS | 2026-04-22 (code inspection + smoke) | PASS |
| REQ-086 | Repeated `tusq scan` + `tusq manifest` on the same Fastify fixture produce byte-identical `input_schema.properties` ordering for the extracted schema.body route | Two consecutive `tusq manifest` runs on the Fastify fixture produce identical `JSON.stringify(capabilities.find(POST /items).input_schema.properties)` values (smoke M24-g assertion); declaration order (name, price, active) is preserved across both runs | PASS | 2026-04-22 (smoke M24-g) | PASS |
| REQ-087 | `fastify-schema-body-extraction-determinism` eval scenario asserts 3-run determinism; `npm test` exits 0 with `Eval regression harness passed (7 scenarios)` | `tests/evals/governed-cli-scenarios.json` contains `fastify-schema-body-extraction-determinism`; `tests/eval-regression.mjs` includes `runFastifySchemaExtractionScenario()`; 3 scan runs on the Fastify fixture produce identical `source: 'fastify_schema_body'`, `additionalProperties: false`, and `properties` key ordering; `npm test` exits 0 with `Eval regression harness passed (7 scenarios)` | PASS | 2026-04-22 (eval) | PASS |

| REQ-088 | `extractPiiFieldHints(properties)` pure function — whole-key normalized match (`toLowerCase()` + strip `_`/`-`), returns source-literal keys in `Object.keys(properties)` iteration order, returns `[]` for falsy/non-object/array input; defined in `src/cli.js` at line ~2489 with no framework import, no eval, no external PII library (SYSTEM_SPEC Constraint 15) | `src/cli.js` `extractPiiFieldHints` function at line 2489: input validation (`!properties || typeof properties !== 'object' || Array.isArray(properties)`) returns `[]`; normalization is `key.toLowerCase().replace(/[_-]/g, '')` and whole-key Set lookup; source-literal key pushed (not normalized form) | PASS | 2026-04-22 (code inspection) | PASS |
| REQ-089 | `capability.redaction.pii_fields` populated in `cmdManifest` from `extractPiiFieldHints(capability.input_schema?.properties)` on every manifest run; pii-hint-sample POST /auth → `pii_fields=["email","password"]`; POST /register → `pii_fields=["user_email","first_name","phone_number"]` (`account_type` excluded — normalizes to `accounttype`, not in canonical set); GET /catalog → `pii_fields=[]` | `src/cli.js` line 410-413: `capability.redaction.pii_fields = extractPiiFieldHints(capability.input_schema && capability.input_schema.properties)` before `computeCapabilityDigest()`; eval scenario `pii-field-hint-extraction-determinism` asserts all three routes across 3 repeat runs; `npm test` → exit 0 with `Eval regression harness passed (8 scenarios)` | PASS | 2026-04-22 (eval) | PASS |
| REQ-090 | `PII_CANONICAL_NAMES` frozen `Set` enumerated in `src/cli.js` with 36 normalized names across 9 categories: Email (3), Phone (5), Government ID (4), Name (4), Address (3), DOB (3), Payment (7), Secrets (7), Network (1) — matches SYSTEM_SPEC DEC-599 V1.6 canonical list exactly; any expansion is a material governance event requiring its own ROADMAP milestone | `src/cli.js` lines 15–34: `new Set([...])` with all 36 normalized names enumerated; category comments present; V1.6 governance note at line 14 | PASS | 2026-04-22 (code inspection) | PASS |
| REQ-091 | `capability.sensitivity_class` NOT auto-escalated when `pii_fields` is non-empty (stays `"unknown"`); redaction fields `log_level`, `mask_in_traces`, `retention_days` NOT auto-populated by M25 — remain at V1 permissive defaults | Smoke test line 258: `manifest.capabilities.every((capability) => capability.sensitivity_class === 'unknown')` passes for Express fixture (which produces pii_fields=[] for all capabilities); `extractPiiFieldHints` only sets `pii_fields` — `log_level`, `mask_in_traces`, `retention_days` are preserved from `normalizeRedaction()` defaults (not overwritten by M25) | PASS | 2026-04-22 (code inspection + smoke) | PASS |
| REQ-092 | Express fixture capabilities all produce `pii_fields: []` (no PII-named fields in Express sample routes); NestJS fixture M24 scan produces no `fastify_schema_body` source tags; repeated manifest generations produce byte-identical `pii_fields` ordering; capability with no `input_schema.properties` returns `[]` from `extractPiiFieldHints` | Smoke test line 264: Express manifest `capabilities.every((c) => JSON.stringify(c.redaction) === JSON.stringify(defaultRedaction))` where `defaultRedaction.pii_fields = []`; NestJS M24-f smoke at line 692-696 confirms no `fastify_schema_body`; pii-hint-sample GET /catalog (no body) verified by eval scenario | PASS | 2026-04-22 (smoke + eval) | PASS |
| REQ-093 | `pii-field-hint-extraction-determinism` eval scenario present in `tests/evals/governed-cli-scenarios.json`; `runPiiFieldHintScenario()` in `tests/eval-regression.mjs`; 3-run determinism verified across POST /auth, POST /register, GET /catalog in pii-hint-sample fixture; `npm test` exits 0 with `Eval regression harness passed (8 scenarios)` | `tests/evals/governed-cli-scenarios.json`: scenario `pii-field-hint-extraction-determinism` with `repeat_runs: 3` and `expected_routes` covering all 3 pii-hint-sample routes; `tests/eval-regression.mjs` line 534: dispatches to `runPiiFieldHintScenario()`; `npm test` → exit 0 with `Eval regression harness passed (8 scenarios)` (independently re-run) | PASS | 2026-04-22 (eval) | PASS |

| REQ-094 | `PII_CATEGORY_BY_NAME` frozen lookup is the M26 single source of truth: every M25 normalized canonical name maps to exactly one of 9 category keys (`email`, `phone`, `government_id`, `name`, `address`, `date_of_birth`, `payment`, `secrets`, `network`); `PII_CANONICAL_NAMES` is derived from this lookup so M25/M26 freeze together | `src/cli.js` lines 13-62 define `PII_CATEGORY_BY_NAME = Object.freeze({...})` with all 36 normalized names and derive `PII_CANONICAL_NAMES = new Set(Object.keys(PII_CATEGORY_BY_NAME))`; no separate duplicate name list can drift from categories; no new dependency or external dictionary import exists | PASS | 2026-04-22 (code inspection) | PASS |
| REQ-095 | `extractPiiFieldCategories(piiFields)` pure function returns a parallel category array in `pii_fields` order, returns `[]` for absent/empty/non-array inputs, applies the same normalization rule as M25, and throws synchronously if a field lacks a known category | `src/cli.js` lines 2532-2550: guard returns `[]`; iteration uses `for...of` over `piiFields`; normalization is `field.toLowerCase().replace(/[_-]/g, '')`; lookup uses `PII_CATEGORY_BY_NAME[normalized]`; missing category throws `CliError`; function has no I/O, no source scan, no eval, and no library import | PASS | 2026-04-22 (code inspection) | PASS |
| REQ-096 | Every generated capability emits `redaction.pii_categories` immediately after `pii_fields` and before `capability_digest`; empty `pii_fields` yields `pii_categories: []` (array, never absent); `defaultRedaction()` and `normalizeRedaction()` preserve the additive field shape | `src/cli.js` lines 438-443 populate `pii_fields`, then `pii_categories`, then compute digest; `defaultRedaction()` lines 2553-2559 includes `pii_categories: []`; `normalizeRedaction()` preserves valid existing `pii_categories` arrays before M26 re-derives generated capability categories; smoke default redaction assertion lines 229-235 expects `pii_categories: []` on Express capabilities | PASS | 2026-04-22 (code inspection + smoke) | PASS |
| REQ-097 | Category labels match field hints one-to-one in realistic routes: POST /auth → `pii_fields=["email","password"]`, `pii_categories=["email","secrets"]`; POST /register → `["user_email","first_name","phone_number"]`, `["email","name","phone"]`; POST /profile → `["user_email","ssn","credit_card"]`, `["email","government_id","payment"]`; GET /catalog → `[]`, `[]` | `tests/fixtures/pii-hint-sample/src/server.ts` declares the M26 mixed-category `/profile` route; `tests/evals/governed-cli-scenarios.json` lines 183-233 encodes expected fields/categories for all four routes; `tests/eval-regression.mjs` lines 542-570 asserts arrays, one-to-one length, `sensitivity_class`, and redaction policy defaults; `npm test` exits 0 with `Eval regression harness passed (9 scenarios)` | PASS | 2026-04-22 (eval) | PASS |
| REQ-098 | M26 does not change score/confidence, `sensitivity_class`, `log_level`, `mask_in_traces`, `retention_days`, `tusq serve`, or `tusq policy verify --strict`; category labels are descriptive metadata only | `scoreConfidence()` still only has the pre-existing M24 `schema_fields` branch (`src/cli.js` lines 2380-2403), with no PII/category scoring branch; eval category scenario asserts `sensitivity_class === "unknown"` and default redaction policy fields for every expected route; M23 strict smoke coverage still passes under `npm test`, confirming strict verification remains approval-alignment only and does not inspect redaction categories | PASS | 2026-04-22 (code inspection + smoke + eval) | PASS |
| REQ-099 | Documentation covers the M26 manifest shape and framing boundary without overclaiming retention enforcement, automated redaction, or regulatory compliance | `website/docs/manifest-format.md` lines 292-333 documents `pii_categories` one-to-one correspondence, empty-array behavior, nine category keys, no value/code/dictionary inspection, no `sensitivity_class` or redaction-policy auto-population, no serve/strict enforcement, and no GDPR/HIPAA/PCI compliance claim; `README.md` line 207 mentions parallel `pii_categories` labels for reviewer-led masking/retention decisions; `RELEASE_NOTES.md` includes the M26 V1.7 section | PASS | 2026-04-22 (docs inspection) | PASS |
| REQ-100 | `pii-category-label-determinism` eval scenario is present and deterministic across 3 repeat runs; full project verification runs 9 scenarios | `tests/evals/governed-cli-scenarios.json` contains scenario `pii-category-label-determinism` with `repeat_runs: 3`; `tests/eval-regression.mjs` dispatches to `runPiiCategoryLabelScenario()` at lines 610-611; `npm test` exits 0 with `Smoke tests passed` and `Eval regression harness passed (9 scenarios)` | PASS | 2026-04-22 (npm test) | PASS |

| REQ-101 | `tusq redaction` is exposed as one new CLI noun with exactly one subcommand, `review`; `tusq redaction review --help` documents only `--manifest`, `--capability`, and `--json` plus reviewer-aid framing | `node bin/tusq.js help` exits 0 and lists `redaction`; `node bin/tusq.js redaction review --help` exits 0 and prints `Usage: tusq redaction review [--manifest <path>] [--capability <name>] [--json]`, the three flags, exit codes, and `This is a reviewer aid, not a runtime enforcement gate.` | PASS | 2026-04-22 (CLI) | PASS |
| REQ-102 | `PII_REVIEW_ADVISORY_BY_CATEGORY` is the frozen M27 nine-category advisory source of truth; every M26 category has exactly one advisory string using em-dash U+2014 and reviewer-directed wording | `src/cli.js` defines `PII_REVIEW_ADVISORY_BY_CATEGORY = Object.freeze({...})` with `email`, `phone`, `government_id`, `name`, `address`, `date_of_birth`, `payment`, `secrets`, and `network`; `assertPiiReviewAdvisorySetComplete()` guards missing entries; smoke M27 advisory byte-exactness checks assert every advisory contains the UTF-8 em-dash byte sequence and matches the expected text | PASS | 2026-04-22 (code inspection + smoke) | PASS |
| REQ-103 | `tusq redaction review` emits deterministic per-capability reports in human and `--json` modes, copying `manifest_path`, `manifest_version`, `generated_at`, `approved`, `sensitivity_class`, `pii_fields`, and `pii_categories` without re-stamping wall-clock data | `buildRedactionReviewReport()` copies top-level manifest fields with `null` fallback; `formatRedactionReviewReport()` uses `unknown` fallback only for human display; smoke M27(g) asserts byte-identical human and JSON stdout across repeated runs; `npm test` exits 0 with `Smoke tests passed` | PASS | 2026-04-22 (smoke + code inspection) | PASS |
| REQ-104 | Advisory rows are one per distinct category per capability in first-appearance order, and `--capability <name>` filters to one exact capability without changing report shape | `buildPiiReviewAdvisories()` uses a `Set` and iterates `pii_categories` in order; smoke M27(k) verifies mixed-category order for email/government_id/payment; smoke M27(c) verifies `--capability` returns one exact capability; eval scenario `redaction-review-determinism` asserts advisory order for `/auth`, `/profile`, and `/catalog` | PASS | 2026-04-22 (smoke + eval) | PASS |
| REQ-105 | No-PII and empty-manifest cases are explicit, successful reviewer signals; malformed/missing inputs fail with stderr-only output and empty stdout | Capabilities with `pii_fields: []` render `No canonical PII field-name matches — reviewer action: none required from M27.` and have empty arrays/advisories; manifest with `capabilities: []` exits 0 with exactly `No capabilities in manifest — nothing to review.\n` in human mode and `{manifest_path, manifest_version, generated_at, capabilities: []}` in JSON mode; missing manifest, malformed JSON, missing/non-array `capabilities`, unknown capability, and unknown flag all exit 1 with stderr-only messages and `stdout === ""` under smoke coverage | PASS | 2026-04-22 (smoke) | PASS |
| REQ-106 | `tusq redaction review` is strictly read-only: it does not mutate `tusq.manifest.json`, does not write policy/tool/scan artifacts, and does not change `tusq policy verify` default or `--strict` behavior | Smoke M27(h) captures manifest content and mtime before/after a review run and asserts both unchanged; smoke M27 policy-verify check compares default and strict policy verify stdout/exit code before and after `redaction review`; `cmdRedactionReview()` only reads the manifest and writes stdout/stderr | PASS | 2026-04-22 (smoke + code inspection) | PASS |
| REQ-107 | M27 does not mutate or infer governance defaults: `sensitivity_class`, `retention_days`, `log_level`, `mask_in_traces`, approval metadata, and `capability_digest` remain manifest-owned inputs, not redaction-review outputs | `buildRedactionReviewReport()` echoes `sensitivity_class` verbatim and emits no retention/log/mask fields; smoke M27(i) asserts report sensitivity equals the manifest value; `cmdRedactionReview()` never calls scanner, manifest generation, approve, compile, policy write, or digest code; SYSTEM_SPEC Constraint 20 remains satisfied | PASS | 2026-04-22 (smoke + code inspection) | PASS |
| REQ-108 | `redaction-review-determinism` eval scenario is present and full project verification now runs 10 scenarios | `tests/evals/governed-cli-scenarios.json` contains `redaction-review-determinism` with `repeat_runs: 3`; `tests/eval-regression.mjs` dispatches to `runRedactionReviewDeterminismScenario()`; `npm test` exits 0 with `Smoke tests passed` and `Eval regression harness passed (10 scenarios)` | PASS | 2026-04-22 (npm test) | PASS |

**M26 recovered QA verification:** `npm test` was re-run after the runtime recovery and exited 0 with `Smoke tests passed` and `Eval regression harness passed (9 scenarios)`. No M26 acceptance item remains open.

**M27 QA verification:** `npm test` was re-run on HEAD d242727 and exited 0 with `Smoke tests passed` and `Eval regression harness passed (10 scenarios)`. `node bin/tusq.js help` and `node bin/tusq.js redaction review --help` both exited 0 and confirmed the redaction noun/help surface.

## Checklist

- [x] Command help audited for every user-facing command (init, scan, manifest, compile, serve, review, version, help)
- [x] Invocation path checked: `node bin/tusq.js` works from repo root
- [x] Failure-mode UX reviewed for invalid flags and missing inputs (REQ-003, REQ-004, REQ-006, REQ-010, REQ-015)
- [x] Full smoke test suite executed and passed (`node tests/smoke.mjs` → exit 0) — re-verified on attempt 2 against post-checkpoint HEAD
- [x] Framework coverage verified: Express, Fastify, and NestJS all scanned successfully (local describe-only MCP endpoint verified via `tusq serve` smoke tests)
- [x] Live-site consolidation explicitly covered in QA evidence: direct file inspection confirms the legacy `websites/` hero structure, recovery path, and styling cues now live in `website/`, and REQ-023 through REQ-025 record canonical `website/` ownership
- [x] Acceptance contract explicitly closed: `website/` is the canonical website surface, `.planning/IMPLEMENTATION_NOTES.md` includes a literal `## Changes` heading, and no ship decision depends on `websites/` remaining active
- [x] Vision goal "capabilities with provenance back to source" verified: scan.json, tusq.manifest.json, and tusq-tools/*.json all carry `provenance.{file,line}` tracing each capability to its originating source declaration (REQ-026 through REQ-028)
- [x] Human approval package reviewed for market-facing truthfulness: `.planning/*` and `website/` launch copy stay anchored to the defendable v0.1.0 surface and do not treat broader README roadmap items as shipped scope
- [x] Off-site launch metadata aligned: `website/docusaurus.config.ts` now uses a tusq-specific social share card so social previews no longer undercut the v0.1.0 launch framing with default Docusaurus branding
- [x] Post-v0.1.0 roadmap page added with explicit non-shipped boundary statement (REQ-029); roadmap is wired into `Help` group in `website/sidebars.ts` and linked from homepage CTA
- [x] Manifest format doc added (REQ-030): `website/docs/manifest-format.md` accurately documents all V1 capability fields, the conservative schema shape, and the approval flow; wired in sidebars.ts Reference group at position 3 with no conflicts
- [x] `sensitivity_class` field verified end-to-end (REQ-031): `classifySensitivity()` and `normalizeSensitivityClass()` implemented in `src/cli.js`; field present in manifest, compiled tools, MCP `tools/list`, and MCP `tools/call`; all four smoke-test assertions pass (`node tests/smoke.mjs` → exit 0); documented with 5 levels and V1-always-unknown note in `website/docs/manifest-format.md`
- [x] `auth_hints` field verified end-to-end (REQ-032): `inferAuthHints()` and `dedupe()` implemented in `src/cli.js`; field present at all 4 pipeline stages (scan.json, tusq.manifest.json, tusq-tools/*.json, MCP runtime); two new smoke-test assertions check `Array.isArray(auth_hints)` on both `tools/list` and `tools/call` responses (`node tests/smoke.mjs` → exit 0); `website/docs/mcp-server.md` updated to list `auth_hints` alongside `side_effect_class` and `sensitivity_class` in governance metadata
- [x] `examples` and `constraints` fields verified end-to-end (REQ-033): `normalizeExamples()`, `defaultExamples()`, `normalizeConstraints()`, and `defaultConstraints()` implemented in `src/cli.js`; fields present at manifest, tusq-tools/*.json, and MCP `tools/call` stages per SYSTEM_SPEC design (scan and `tools/list` intentionally omit them — SYSTEM_SPEC.md lines 564-571 confirm this); smoke tests cover default values on manifest and custom-value propagation through compile and `tools/call` (`node tests/smoke.mjs` → exit 0); `website/docs/manifest-format.md` documents both fields with V1 limitations
- [x] `redaction` and approval audit fields verified end-to-end (REQ-034): `defaultRedaction()`, `normalizeRedaction()`, `normalizeApprovedBy()`, and `normalizeApprovedAt()` implemented in `src/cli.js`; `redaction` present at manifest, tusq-tools/*.json, and MCP `tools/call` stages; `tools/list` intentionally excludes `redaction` (smoke test asserts absence); `approved_by`/`approved_at` are manifest-only — compile output and MCP responses explicitly exclude them (smoke tests assert absence at both stages); 7 smoke assertions cover all boundaries (`node tests/smoke.mjs` → exit 0); `website/docs/manifest-format.md` and `website/docs/mcp-server.md` accurately document the redaction field and manifest-only boundary for approval metadata
- [x] Version-history fields verified end-to-end (REQ-035): `manifest_version` and `previous_manifest_hash` implemented at manifest root in `src/cli.js`; `capability_digest` implemented per capability using stable sorted-key JSON + SHA-256 over content fields only; smoke tests verify increment/hash semantics, digest determinism, digest invariance for approval-only edits, digest changes for content edits, and explicit absence of version-history metadata from compiled tools and MCP responses (`node tests/smoke.mjs` → exit 0); docs updated to mark these fields as manifest-only (`website/docs/manifest-format.md`, `website/docs/mcp-server.md`)
- [x] Framework-specific deep extraction verified (REQ-036): Fastify 3-argument inline bug (`fastify.get(path, {options}, handler)`) discovered and fixed in `src/cli.js`; previously the GET route from the Fastify fixture was silently dropped; fix adds `inlineWithOptionsPattern` multiline regex handling that pattern; NestJS guard inheritance and path composition confirmed correct via live scan; 7 new smoke-test assertions covering Fastify route count, named handler, schema-inferred input_schema, auth_hints on POST, NestJS route count, path composition, guard inheritance, and additive guard on POST all pass (`node tests/smoke.mjs` → exit 0)
- [x] First-pass manifest usability verified (REQ-037): `extractPathParameters`, `buildInputSchema`, `extractDomainSegments` (with `DOMAIN_PREFIX_SEGMENTS` skip list), `describeCapability`, `describeVerb`, `describeSideEffect`, `singularizeWord`, and `describePathQualifier` implemented in `src/cli.js`; `scoreConfidence` applies a -0.10 penalty when `schema_hint` is absent and clamps to `[0, 0.95]`; Express fixture extended with `GET /api/v1/users/:id` so smoke tests observe prefix-aware domain inference (`domain="users"` despite `api/v1` prefix), path parameter extraction into `input_schema.properties`, the rich description with handler/auth context, and sub-0.8 confidence that flips `review_needed=true`; `node tests/smoke.mjs` → exit 0
- [x] Manifest review governance improvements verified (REQ-038): `enforceStrictReviewIfRequested` gates `tusq review --strict` with exit 1 and `Review gate failed:` message when any capability is unapproved or low-confidence; `summarizeInputSchemaForReview`, `summarizeOutputSchemaForReview`, and `summarizeProvenanceForReview` render inputs/returns/source+handler+framework on the review summary line; input_schema inference adds `body` (`source: "request_body"`) for write methods without other inputs; output_schema inference infers `array<object>` or object-with-properties from handler return/json usage; path parameters propagate with `source: "path"` through manifest and compile (including NestJS POST `/users/:id/disable`); provenance now carries `framework` and `handler` through the pipeline; smoke tests assert strict-mode failure and success paths, review-output substrings, body/array/property inference, and path-parameter source marker — all pass (`node tests/smoke.mjs` → exit 0)
- [x] Manifest diff and review queue verified (REQ-039 through REQ-044): `tusq diff` compares explicit manifest files, reports added/removed/changed/unchanged counts, emits JSON, detects changed capabilities by `capability_digest`, reports `fields_changed`, generates review queues, and gates unapproved or review-needed changed capabilities with `--fail-on-unapproved-changes`; smoke tests cover human output, JSON output, gate failure, and gate success (`node tests/smoke.mjs` → exit 0); docs updated in README, CLI reference, and manifest format docs without claiming history-store, git, or automatic re-approval support
- [x] Governed CLI eval/regression harness verified (REQ-045 through REQ-049): `tests/evals/governed-cli-scenarios.json` defines two repo-local scenarios; `tests/eval-regression.mjs` validates strict review gates, compile metadata boundaries, diff review queues, and CI failure behavior; `npm test` runs both smoke and eval suites and exits 0
- [x] Governed manifest approval CLI verified (REQ-050 through REQ-053): `tusq approve` is in help, supports single-capability and `--all` approval, records `approved_by`/`approved_at`, clears `review_needed`, supports dry-run JSON, and is covered by `npm test` (`Smoke tests passed`; `Eval regression harness passed (2 scenarios)`)
- [x] Repo-local capability documentation generator verified (REQ-054 through REQ-057): `tusq docs` is in help, generates Markdown with manifest metadata and per-capability sections, writes deterministic `--out` file byte-identical to stdout, exits 1 with actionable message for missing manifests; covered by `npm test` (`Smoke tests passed`)
- [x] Opt-in execution policy verified (REQ-058 through REQ-063): `tusq serve --policy` startup failures correctly exit 1 with actionable messages for all 5 error conditions; describe-only policy is a no-op; dry-run mode returns `executed: false` with `dry_run_plan` shape, path param substitution, 64-char `plan_hash`, and policy echo; `plan_hash` is deterministic (same args → same hash, different args → different hash); argument validation returns `-32602` with `data.validation_errors` array; `allowed_capabilities` filter enforced with `-32602` for non-listed capabilities; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (4 scenarios)`)
- [x] Policy scaffold generator verified (REQ-064 through REQ-069): `tusq policy init` is in 12-command help surface; default generation produces `schema_version:1.0`, `mode:describe-only`, ISO-8601 `approved_at`, no `allowed_capabilities`; `--mode dry-run`, `--allowed-capabilities`, `--force`, `--dry-run` flags all verified; unknown mode exits 1; empty capabilities exits 1; file-conflict protection exits 1 with `--force` hint; `--dry-run` prints JSON to stdout without writing file; round-trip validator confirmation via `tusq serve --policy <generated>`; `policy-init-generator-round-trip` eval scenario present and passing; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (5 scenarios)`)
- [x] Policy verify command verified (REQ-070 through REQ-074): `tusq policy verify --help` exits 0; help + round-trip (init→verify) exits 0; `--json` success shape (`valid:true`, `path`, `policy` echo) verified; missing-file exit 1 + `--json` failure shape (`valid:false`, `path`, `error`) verified; all four malformed-policy conditions (bad JSON, bad schema_version, bad mode, bad allowed_capabilities) each exit 1 with the correct message; parity confirmed — `verify` and `serve --policy` share `loadAndValidatePolicy()` and produce identical error messages for all four fixtures; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (5 scenarios)`)
- [x] Policy strict verify verified (REQ-075 through REQ-080): `tusq policy verify --strict [--manifest <path>]` flag surface present in `--help`; default (no-`--strict`) code path is byte-identical to M22 even with manifest present in working directory (REQ-075); `--strict` cross-references every `allowed_capabilities` entry against `tusq.manifest.json` and exits 1 for `not_in_manifest`, `not_approved`, and `requires_review` reasons (REQ-076); `--strict` exits 1 with actionable messages for missing manifest, malformed manifest JSON, and Constraint 11 guard (`--manifest requires --strict` before any file read) (REQ-077); `--strict --json` success shape verified (`valid:true`, `strict:true`, `manifest_path`, `manifest_version`, `approved_allowed_capabilities`); failure shape includes `strict_errors` array with `{name,reason}` entries (REQ-078); unset/null `allowed_capabilities` passes trivially without opening manifest (REQ-079); M22 parity under `--strict` confirmed for all four M22 failure fixtures (byte-identical stderr); `policy-strict-verify-determinism` eval scenario present; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (6 scenarios)`) (REQ-080)
- [x] Fastify schema body-field extraction verified (REQ-081 through REQ-087): literal `schema.body.properties` extraction produces `source:'fastify_schema_body'`, `additionalProperties:false`, declaration-order keys, and `required` merging (REQ-081); path-param collision resolution confirmed (path param wins over body-declared field with same name) (REQ-082); fall-back for no-body-key, no-properties, and non-literal schema expressions all produce M15 behavior byte-for-byte (REQ-083); Express and NestJS fixtures produce zero `fastify_schema_body` tags confirming M24 is Fastify-only (REQ-084); +0.04 confidence boost for `schema_fields` verified in `scoreConfidence()` source code (REQ-085); repeated manifest generations produce byte-identical property ordering (REQ-086); `fastify-schema-body-extraction-determinism` eval scenario passes 3 determinism runs; covered by `npm test` (`Smoke tests passed`, `Eval regression harness passed (7 scenarios)`) (REQ-087)
- [x] Static PII field-name redaction hints verified (REQ-088 through REQ-093): `extractPiiFieldHints()` pure function with whole-key normalized match, no framework import, no eval (REQ-088); `capability.redaction.pii_fields` auto-populated in `cmdManifest` — pii-hint-sample POST /auth → ["email","password"], POST /register → ["user_email","first_name","phone_number"], GET /catalog → [] (REQ-089); `PII_CANONICAL_NAMES` frozen Set with 36 normalized names across 9 categories matches SYSTEM_SPEC DEC-599 (REQ-090); `sensitivity_class` stays "unknown" and other redaction fields not auto-populated (REQ-091); Express fixture pii_fields=[] for all capabilities; NestJS produces no fastify_schema_body; no-properties returns [] (REQ-092); `pii-field-hint-extraction-determinism` eval scenario asserts 3-run determinism; `npm test` exits 0 with `Eval regression harness passed (8 scenarios)` (REQ-093)
- [x] Static PII category labels verified (REQ-094 through REQ-100): `PII_CATEGORY_BY_NAME` is the frozen 36-name/9-category lookup and `PII_CANONICAL_NAMES` derives from it (REQ-094); `extractPiiFieldCategories()` is pure, ordered, and throws on set drift (REQ-095); every capability emits `pii_categories` before digest, with `[]` for empty `pii_fields` (REQ-096); pii-hint-sample `/auth`, `/register`, `/profile`, and `/catalog` produce expected one-to-one field/category arrays (REQ-097); confidence, sensitivity, redaction policy defaults, `serve`, and `policy verify --strict` remain unchanged (REQ-098); docs and release notes carry Constraint 18 framing without retention-enforcement overclaim (REQ-099); `pii-category-label-determinism` eval runs 3 repeats and `npm test` exits 0 with `Eval regression harness passed (9 scenarios)` (REQ-100)
- [x] Reviewer-facing PII redaction review report verified (REQ-101 through REQ-108): `redaction` noun and `redaction review` help surface present (REQ-101); frozen nine-category advisory set with em-dash byte-exactness locked (REQ-102); human/JSON report shapes deterministic and top-level manifest fields copied without re-stamp (REQ-103); advisory order and `--capability` filter verified (REQ-104); no-PII, empty-manifest, and stderr-only failure UX verified (REQ-105); read-only invariant and policy-verify parity verified (REQ-106); no governance-default mutation or sensitivity escalation verified (REQ-107); `redaction-review-determinism` eval runs 3 repeats and `npm test` exits 0 with `Eval regression harness passed (10 scenarios)` (REQ-108)
