# Launch Plan

## Challenge To Prior Framing

- The previous launch framing was directionally accurate but still too abstract at the top: it named tusq.dev as a "capability compiler" before it made the buyer pain operational
- The previous pass also overloaded the first screen with governance detail before stating the proof path a buyer can verify in one session
- The previous pass also described a six-step workflow that pre-dated M16 and left `tusq diff` out of the launch narrative entirely, even though the command is shipped and CI-ready on HEAD
- A subsequent pass still wrote the approval step as "edit the manifest by hand" even after `tusq approve` shipped in M18 with explicit `--reviewer`, `--all`, `--dry-run`, and `--json` flags; launch copy must call that first-class command by name so the approval trail reads as a governed operator action, not a text-editor ritual
- The most recent pass still listed a 10-command CLI surface even after `tusq docs` shipped in M19 as an offline, deterministic Markdown capability-documentation generator; launch copy must include the 11th command and frame `tusq docs` as the shareable review artifact that complements the manifest and describe-only MCP surface
- The most recent launch pass also still framed `tusq serve` as a single describe-only response shape even after M20 shipped the `--policy` flag, the `.tusq/execution-policy.json` governance artifact, and the opt-in dry-run `tools/call` response (with `executed: false`, policy echo, and a deterministic `plan_hash`); launch copy must explain that dry-run is opt-in, that the approval gate is unchanged, and that no live execution was added
- The prior launch pass also left the governance-artifact scaffolding step implicit — it told reviewers to "author `.tusq/execution-policy.json`" by hand even after M21 shipped `tusq policy init` as the 12th command with the flag set `--mode`, `--reviewer`, `--allowed-capabilities`, `--out`, `--force`, `--dry-run`, `--json`, `--verbose`; launch copy must call the command by name, state that default `mode` is `"describe-only"`, and be explicit that the generated file is validator-round-trip-safe against the M20 `tusq serve --policy` loader
- The prior launch pass stopped at the `policy init` subcommand even after M22 shipped `tusq policy verify` as the second subcommand under the `policy` noun — a no-server CI pre-flight that reuses the M20 validator byte-for-byte, exits `0` on validator-parity PASS and `1` with the server's own error message on failure, and supports `--json` for machine-readable CI output; launch copy must name `tusq policy verify` as the CI pre-flight surface, state that it starts no server and performs no network I/O, and be explicit that a PASS is a provable guarantee that `tusq serve --policy` will accept the same file
- The prior launch pass stopped at `tusq policy verify`'s validator-parity check even after M23 shipped the opt-in `--strict [--manifest <path>]` flag pair that layers a manifest-aware cross-reference on top of the M22 validator; launch copy must name `--strict`, describe the three strict failure reasons (`not_in_manifest`, `not_approved`, `requires_review`), and be explicit that (a) default `policy verify` behavior is byte-for-byte identical to M22 and never reads the manifest, (b) `--manifest` without `--strict` exits `1` with `--manifest requires --strict` before any file read, (c) a strict PASS is a verify-time policy/manifest alignment statement only, not a runtime/execution/freshness/reachability claim, and (d) `tusq serve --policy` does not enforce strict checks and continues to silently drop unlisted or unapproved capabilities from `tools/list`
- The prior launch pass framed the scan step as producing a permissive body placeholder for every route, even after M24 shipped opt-in-by-source-pattern Fastify schema body-field extraction inside `tusq scan`; launch copy must name the new `source: "fastify_schema_body"` value, note that Fastify routes with a literal `schema: { body: { properties, required } }` block now emit declared top-level field shapes in declaration order (with `additionalProperties: false`), and be explicit that (a) no new command or flag was added and the extraction is active by default on source-literal pattern match, (b) non-literal or unbalanced Fastify schemas and all Express/NestJS routes fall back byte-for-byte to the pre-M24 M15 placeholder, (c) path-parameter names win over body-declared field names of the same key, (d) an M24-driven `input_schema` change flips the M13 `capability_digest` and requires fresh `tusq approve` re-approval before `tools/list` re-surfaces the capability, and (e) `fastify_schema_body` means "the declared body schema as it appears literally in source" — NOT "validator-backed," "runtime-enforced by Fastify," or "ajv-validated" (SYSTEM_SPEC Constraint 14)
- The prior launch pass framed `redaction.pii_fields` as a manually-edited array, even after M25 shipped static, name-only PII field-name redaction hints inside `tusq manifest`; launch copy must name M25 as the V1.6 increment, note that `redaction.pii_fields` is now auto-populated by matching `input_schema.properties` keys against the frozen 36-name `PII_CANONICAL_NAMES` set, and be explicit that (a) no new command or flag was added — M25 runs by default inside existing `tusq manifest` and applies uniformly across Express, Fastify, and NestJS capabilities, (b) matching is whole-key only after `toLowerCase()` + strip `_`/`-` normalization (so `user_email` matches but `email_template_id` does NOT — no substring, tail-match, regex, or value inspection), (c) M25 does NOT auto-escalate `capability.sensitivity_class` (stays `"unknown"`) and does NOT auto-populate `log_level`, `mask_in_traces`, or `retention_days` (stay `null`), (d) capabilities with no canonical PII keys produce byte-identical manifests pre- and post-M25, (e) populating `pii_fields` on a previously empty capability flips the M13 `capability_digest` and requires fresh `tusq approve` re-approval before `tools/list` re-surfaces the capability, (f) the V1.6 canonical list is frozen — any expansion is a material governance event under its own future ROADMAP milestone, and (g) the framing is "well-known PII field-name hints derived statically from source-declared field keys" — NOT "PII detection," "PII validation," "GDPR/HIPAA/PCI-compliant," or "automated redaction" (SYSTEM_SPEC Constraint 16)
- The previous QA turn verified M26 static PII category labels, but QA readiness is not launch readiness: launch copy must name M26 as the V1.7 increment, state that `redaction.pii_categories` is now emitted for every capability as a parallel array to M25's `redaction.pii_fields`, and be explicit that (a) category order and length exactly match `pii_fields`, (b) empty `pii_fields` emits `pii_categories: []` and the field is never absent on generated capabilities, (c) the frozen category keys are `email`, `phone`, `government_id`, `name`, `address`, `date_of_birth`, `payment`, `secrets`, and `network`, (d) the mapping is powered by `PII_CATEGORY_BY_NAME` and `PII_CANONICAL_NAMES` derives from it so M25/M26 freeze together, (e) M26 adds no command, flag, dependency, external dictionary, source scan, value inspection, source-text regex, network I/O, runtime validator, or compliance library, (f) M26 does not auto-escalate `sensitivity_class`, does not auto-populate `log_level`/`mask_in_traces`/`retention_days`, and does not change `scoreConfidence()`, `tusq serve`, or `tusq policy verify --strict`, (g) every existing approved capability sees a one-time M13 digest flip on first post-M26 manifest regeneration because the `redaction` shape now includes `pii_categories`, and (h) category labels are reviewer evidence for manual retention/logging/masking decisions — NOT auto-retention, automated redaction, retention-policy enforcement, or GDPR/HIPAA/PCI compliance (SYSTEM_SPEC Constraint 18)
- The previous launch pass stopped at M28's `sensitivity_class` reviewer-aid even after M29 (V1.10) shipped Static Auth Requirements Inference from Manifest Evidence as the next governance increment; launch copy must name M29 as V1.10, name the new manifest field `capability.auth_requirements` (a structured record per capability with `auth_scheme` from the closed seven-value enum `{bearer, api_key, session, basic, oauth, none, unknown}`, an order-preserving case-sensitively deduplicated `auth_scopes` array, an order-preserving case-sensitively deduplicated `auth_roles` array, and an `evidence_source` label from the closed five-value enum `{middleware_name, route_prefix, auth_required_flag, sensitivity_class_propagation, none}`), name the only new operator-visible CLI surface change (the optional `--auth-scheme <scheme>` filter on the existing `tusq review` command, mutually compatible with M28's `--sensitivity <class>` filter via AND-style intersection), and be explicit that (a) the 13-command CLI surface is preserved EXACTLY — no new top-level noun, no new subcommand, no new environment-variable gate, and NO new dependency added to `package.json` (no `passport`, no `jsonwebtoken`, no `oauth2-server`) — (b) `auth_scheme` is computed by a pure deterministic function (`classifyAuthRequirements` in `src/cli.js`) inside `tusq manifest` over the already-built record (`auth_hints` middleware names, `auth_required` flag, route path) using a frozen six-rule first-match-wins table — R1 `/bearer|jwt|access[_-]?token/i` → `bearer`, R2 `/api[_-]?key|x-api-key/i` → `api_key`, R3 `/session|cookie|passport-local/i` → `session`, R4 `/basic[_-]?auth/i` → `basic`, R5 `/oauth|oidc|openid/i` → `oauth`, R6 `auth_required === false` AND non-admin route → `none`, default → `unknown` — with zero source-file reads, zero value inspection, zero network I/O, and no AAA/OAuth/OIDC/SAML library imports, (c) capabilities with zero static auth evidence MUST receive `auth_scheme: "unknown"` explicitly via the AC-4 zero-evidence guard that fires BEFORE R1 (SYSTEM_SPEC Constraint 22) — they are NEVER silently labeled `"none"`; `"none"` is reachable only from explicit R6 evidence, (d) `auth_requirements` is included in the M13 `capability_digest` content hash, so first post-M29 manifest regeneration flips digests on every capability and resets `approved` to `false`, pulling the capability from `tools/list` until a reviewer re-approves it via `tusq approve` — RELEASE_NOTES MUST warn operators of the expected re-approval sweep, (e) `tusq compile` output is byte-identical before and after M29 adoption (AC-7 compile-output-invariant) — `auth_requirements` is a manifest-and-review-surface field, not a compiled-tool field, so `tusq-tools/*.json` does NOT include it, (f) `tusq serve` MCP `tools/list`/`tools/call`/`dry_run_plan` shapes are byte-for-byte unchanged — `auth_requirements` does NOT appear in any MCP response, `tusq approve` gating is unchanged, `tusq policy verify` (including `--strict`) is unchanged, and `tusq redaction review` is unchanged, (g) `--auth-scheme <scheme>` does NOT change `tusq review` exit-code semantics (exit 0 still means no blocking concerns; exit 1 still means unapproved/low-confidence capabilities were found, the filter does not hide those); supplying a value outside the seven-element enum exits 1 with an actionable message before any output, and combining `--auth-scheme` with `--sensitivity` is AND-style intersection, (h) `auth_scopes` and `auth_roles` are extracted from middleware annotation literals via the frozen regexes `/scopes?:\s*\[([^\]]+)\]/` and `/role[s]?:\s*\[([^\]]+)\]/` — both arrays are always present (NEVER `null`), order-preserving, and case-sensitively deduplicated, returning `[]` on no match, (i) the rule table is FROZEN — any change to R1–R6 (regex patterns, scheme assignments, precedence order, default class) is a material governance event that MUST land under its own future ROADMAP milestone with fresh re-approval expectations and a RELEASE_NOTES entry; the eval regression harness is now 16 scenarios (was 13 pre-M29) and locks R1 bearer precedence, the zero-evidence → `unknown` guard, and order-preserving scope/role extraction at merge time, (j) V1.10 ships with one known and disclosed limitation: the `auth_required` field is NOT set by the M9/M15 scanner during `tusq scan` or `tusq manifest` generation, so R6 (`auth_required === false` → `auth_scheme: "none"`) is reachable in V1.10 only via manually-edited manifests — the implementation is correct for manual edits and a future milestone may populate `auth_required` from scanner evidence; this limitation is non-blocking for V1.10 ship, and (k) the framing is "reviewer-aid label derived from already-shipped manifest evidence" (SYSTEM_SPEC Constraint 22) — NOT "runtime authentication," "runtime authorization enforcement," "automated AAA enforcement," "OAuth/OIDC/SAML/SOC2/ISO27001 compliance certification," "token signature validation," or "scope grant validation"; `auth_scheme: "bearer"` means a middleware name matched the R1 regex, not that tusq verifies bearer tokens — reviewers remain responsible for actual AAA decisions
- The previous launch pass stopped at M27's read-only `tusq redaction review` reviewer-aid even after M28 (V1.9) shipped Static Sensitivity Class Inference from Manifest Evidence as the next governance increment; launch copy must name M28 as V1.9, name the new manifest field `capability.sensitivity_class` (closed five-value enum `{public, internal, confidential, restricted, unknown}`), name the only new operator-visible CLI surface change (the optional `--sensitivity <class>` filter on the existing `tusq review` command), and be explicit that (a) the 13-command CLI surface is preserved EXACTLY — no new top-level noun, no new subcommand, no new environment-variable gate — (b) `sensitivity_class` is computed by a pure deterministic function (`classifySensitivity` in `src/cli.js`) inside `tusq manifest` over the already-built record (verb, route, parameters, `redaction.pii_categories`, M13 preserve flag, write/auth markers) using a frozen six-rule first-match-wins table — R1 preserve→restricted, R2 admin/destructive verb→restricted, R3 any `pii_categories` entry→confidential, R4 write+financial route context→confidential, R5 `auth_required` or narrow-write→internal, R6 default→public — with zero source-file reads, zero value inspection, zero network I/O, and no compliance/policy library imports, (c) capabilities with zero static evidence MUST receive `sensitivity_class: "unknown"` explicitly (the AC-4 zero-evidence guard; SYSTEM_SPEC Constraint 21) — they are NEVER silently labeled `"public"`, (d) `sensitivity_class` is included in the M13 `capability_digest` content hash, so first post-M28 manifest regeneration flips digests on every capability that was previously `"unknown"` and now receives a computed value, resetting `approved` to `false` and pulling the capability from `tools/list` until a reviewer re-approves it via `tusq approve` — RELEASE_NOTES MUST warn operators of the expected re-approval sweep, (e) `tusq compile` output is byte-identical regardless of `sensitivity_class` value (AC-9 compile-output-invariant) — the field surfaces only in `tusq review`, `tusq docs`, and `tusq diff` per-capability rendering, (f) `tusq approve` gate logic is unchanged, `tusq serve` MCP `tools/list` / `tools/call` / `dry_run_plan` shapes are unchanged, and `tusq policy verify` (including `--strict`) is unchanged, (g) `--sensitivity <class>` on `tusq review` does NOT change exit-code semantics (exit 0 still means no blocking concerns; exit 1 still means unapproved/low-confidence capabilities were found, the filter does not hide those); supplying a value outside the five-element enum exits 1 with an actionable message before any output, (h) the rule table is FROZEN — any change to R1-R6 (verb sets, financial-context regex, precedence order, default class) is a material governance event that MUST land under its own future ROADMAP milestone with fresh re-approval expectations and a RELEASE_NOTES entry; the eval regression harness is now 13 scenarios (was 10 pre-M28) and locks R1>R3 precedence, R4 write+financial → confidential, and zero-evidence → unknown at merge time, and (i) the framing is "reviewer-aid label derived from already-shipped manifest evidence" (SYSTEM_SPEC Constraint 21) — NOT "runtime data-access control," "automated PII enforcement," "automated compliance certification," "GDPR/HIPAA/PCI/SOC2 attestation," or any phrase implying tusq enforces, certifies, or redacts data at runtime; reviewers remain responsible for actual access control, retention, masking, and compliance posture
- The previous launch pass stopped at M26 even after M27 (V1.8) shipped `tusq redaction review` as a brand-new top-level `redaction` CLI noun (the 13th shipped entry point) with the single `review` subcommand (flags `--manifest`, `--capability`, `--json`); launch copy must name M27 as the V1.8 increment, state that `tusq redaction review` is a read-only reviewer aid that aggregates the manifest's M25 `redaction.pii_fields` and M26 `redaction.pii_categories` per capability and attaches one frozen advisory string per distinct category from the new nine-entry `PII_REVIEW_ADVISORY_BY_CATEGORY` lookup in `src/cli.js`, and be explicit that (a) `redaction` is a new CLI noun parallel to `policy`, not a flag on the existing `tusq review` approval-gate command, (b) M27 is strictly read-only — it does NOT mutate the manifest, does NOT write any `.tusq/` artifact, does NOT change `capability_digest`, and does NOT change the observable behavior of `tusq scan`, `tusq manifest`, `tusq compile`, `tusq serve`, `tusq approve`, `tusq diff`, `tusq docs`, or `tusq policy verify` (including `--strict`), (c) M27 causes ZERO `capability_digest` flips on first post-M27 run and is observationally invisible in `tusq diff`, (d) `capabilities: []` is a valid scaffold state and exits 0 with the exact human line `No capabilities in manifest — nothing to review.` and a `{manifest_path, manifest_version, generated_at, capabilities: []}` envelope under `--json` — distinct from the `Invalid manifest: missing capabilities array` error path, (e) missing top-level `manifest_version`/`generated_at` become `null` under `--json` and the literal string `unknown` in human output and are NEVER re-stamped with wall-clock time, (f) stdout is empty on every exit-1 path and error text goes to stderr only, (g) advisories appear one per DISTINCT category in first-appearance order so repeated categories inside a single capability do not repeat advisories, (h) the frozen V1.8 advisory strings use em-dash U+2014 exactly — ASCII hyphen-minus and en-dash are forbidden, byte-level drift is locked by a smoke fixture and is a material governance event, (i) the advisory set is total and disjoint across the nine M26 categories and is frozen for V1.8 — any addition or wording change must land under its own future ROADMAP milestone with its own RELEASE_NOTES entry, and (j) the framing is "a reviewer aid that summarizes existing static evidence alongside frozen reminders" (SYSTEM_SPEC Constraints 19 and 20) — NOT "runtime PII enforcement," "automated PII compliance," "automated redaction policy," or GDPR/HIPAA/PCI compliance; running `tusq redaction review` does not make any capability safer at runtime and is not a substitute for reviewer judgment
- For launch, the first screen should answer four questions in order: who this is for, what problem it solves, what proof exists today, and where V1 stops
- Governance detail stays important, but it should support the operator story rather than replace it
- Re-review after change is part of the operator story: `tusq diff` turns manifest-version comparison into a review queue so governed exposure stays governed as the codebase evolves

## Launch Goals

1. Launch tusq.dev `v0.1.0` with a clear, defensible promise: existing Node.js SaaS teams can turn product behavior they already run into a reviewed manifest and describe-only MCP surface in one CLI workflow.
2. Attract the right first adopters: engineering and platform teams on Express, Fastify, or NestJS who are already exploring agent tooling and MCP.
3. Prevent category confusion by separating the shipped OSS CLI from the broader tusq.dev vision and tusq.cloud roadmap.
4. Create enough clarity that early users know exactly what to try first, what to trust, and what is intentionally deferred.

## Launch Narrative

### Core story

- tusq.dev v0.1.0 is for incumbent SaaS teams that already have product logic in a supported Node.js backend and need a reviewed way to make that logic inspectable by AI systems
- The defensible proof is concrete and skimmable: scan the repo, inspect the manifest, approve capabilities manually, compile approved tool JSON, and inspect the resulting describe-only MCP surface
- The launch should sell the operator control model as much as the automation model: reviewed manifest, explicit approval, optional approval trail, preserved provenance, inspectable governance metadata, inspectable redaction policy, and inspectable output

### First user action

- Direct early adopters to run tusq.dev locally against a real Express, Fastify, or NestJS repo
- Ask them to inspect `tusq.manifest.json` first, with special attention to provenance, `side_effect_class`, `sensitivity_class`, `auth_hints`, approval fields, and `redaction`, then approve one or two capabilities, compile, and confirm the describe-only MCP `examples` and `constraints` match expectations
- Treat scanner misses, low-confidence routes, and awkward manifest edits as feedback fuel for the next release
- Do not optimize the launch flow around `tusq serve` alone; the key product moment is seeing existing product behavior become a reviewable manifest before exposure

## Message Hierarchy

1. Lead with the audience filter: existing Express, Fastify, or NestJS SaaS teams with real product logic already in production or active development.
2. State the operator problem in one line: the product logic already exists, but there is no reviewed path to make it AI-visible.
3. State the proof sequence in one line: supported repo -> reviewed manifest -> approved tool JSON -> describe-only MCP.
4. State the boundary immediately after the proof: no live execution, no hosted control plane, no non-Node support in V1.
5. End with the repo-local CTA and an explicit ask for edge-case feedback.

## Required Assets

### Must publish at launch

- A launch announcement that names the release as `tusq.dev v0.1.0`
- Messaging that includes the M29 (V1.10) Static Auth Requirements Inference increment: every capability now carries an `auth_requirements` record with `auth_scheme` from the closed enum `{bearer, api_key, session, basic, oauth, none, unknown}`, an order-preserving `auth_scopes` array, an order-preserving `auth_roles` array, and an `evidence_source` label from the closed enum `{middleware_name, route_prefix, auth_required_flag, sensitivity_class_propagation, none}`, computed by a pure deterministic function over manifest evidence using a frozen six-rule first-match-wins table (R1 bearer/jwt/access-token regex → bearer, R2 api-key regex → api_key, R3 session/cookie/passport-local regex → session, R4 basic-auth regex → basic, R5 oauth/oidc/openid regex → oauth, R6 `auth_required === false` AND non-admin route → none, default → unknown), with `"unknown"` returned explicitly for zero-evidence capabilities (NEVER silently `"none"`); the only new CLI surface is the optional `--auth-scheme <scheme>` filter on the existing `tusq review` command (no new noun, 13-command surface preserved exactly, no new dependency in `package.json`), mutually compatible with M28's `--sensitivity <class>` via AND-style intersection; the field is included in the M13 `capability_digest` so first post-M29 manifest regeneration flips digests and forces re-approval before `tools/list` re-surfaces affected capabilities; `tusq compile` output, `tusq approve` gating, `tusq serve` MCP shapes, `tusq policy verify`, and `tusq redaction review` are byte-for-byte unchanged (AC-7 invariant); the eval regression harness is now 16 scenarios (was 13); V1.10 ships with the disclosed limitation that the scanner does not set `auth_required`, making R6 reachable only via manual manifest edits; the framing is "reviewer-aid label derived from already-shipped manifest evidence" (SYSTEM_SPEC Constraint 22) — not runtime AAA enforcement, not OAuth/OIDC/SAML/SOC2/ISO27001 attestation, not token signature validation
- Messaging that includes the M28 (V1.9) Static Sensitivity Class Inference increment: every capability now carries a `sensitivity_class` field (closed enum `{public, internal, confidential, restricted, unknown}`) computed by a pure deterministic function over manifest evidence using a frozen six-rule first-match-wins table (R1 preserve→restricted, R2 admin/destructive verb→restricted, R3 PII category→confidential, R4 write+financial→confidential, R5 auth_or_narrow_write→internal, R6 default→public), with `"unknown"` returned explicitly for zero-evidence capabilities; the only new CLI surface is the optional `--sensitivity <class>` filter on the existing `tusq review` command (no new noun, 13-command surface preserved exactly); the field is included in the M13 `capability_digest` so first post-M28 manifest regeneration flips digests and forces re-approval before `tools/list` re-surfaces affected capabilities; `tusq compile` output, `tusq approve` gating, and `tusq serve` MCP shapes are byte-for-byte unchanged (AC-9 invariant); the framing is "reviewer-aid label derived from already-shipped manifest evidence" (SYSTEM_SPEC Constraint 21) — not runtime enforcement, not automated compliance, not GDPR/HIPAA/PCI/SOC2 attestation
- Messaging that stays inside the verified product boundary: scan (including M24 source-literal Fastify body-field extraction that emits `input_schema.source: "fastify_schema_body"` with `additionalProperties: false` and declared field ordering when a Fastify route declares a literal `schema: { body: { properties, required } }`, and falls back byte-for-byte to the pre-M24 placeholder otherwise), manifest (including M25 static, name-only PII field-name redaction hints — `redaction.pii_fields` is auto-populated by matching `input_schema.properties` keys whole-key against the frozen 36-name `PII_CANONICAL_NAMES` set after `toLowerCase()` + strip `_`/`-` normalization, in source-declaration order with original casing preserved; no new flag, no `sensitivity_class` escalation, no `log_level`/`mask_in_traces`/`retention_days` auto-population, byte-identical manifest for capabilities without canonical PII keys; and M26 static PII category labels — `redaction.pii_categories` is emitted on every capability as a parallel array to `pii_fields`, using the frozen nine-key `PII_CATEGORY_BY_NAME` mapping, with `[]` for non-PII capabilities, no policy default mutation, and a broad one-time digest flip requiring re-approval under M13), review, approve (with reviewer identity and timestamp), compile, describe-only MCP serve, opt-in dry-run plan emission via `tusq serve --policy` with a reviewer-signed `.tusq/execution-policy.json`, one-step governance-artifact scaffolding via `tusq policy init` (safe default `mode: "describe-only"`), no-server CI pre-flight validation via `tusq policy verify` with validator parity against `tusq serve --policy`, opt-in manifest-aware strict verification via `tusq policy verify --strict --manifest <path>` that cross-references `allowed_capabilities` against the manifest's approval-gated set, diff-based drift re-review, offline Markdown capability documentation via `tusq docs`, and the M27 (V1.8) read-only reviewer-facing PII redaction review surface via `tusq redaction review [--manifest <path>] [--capability <name>] [--json]` that aggregates the manifest's existing M25 `redaction.pii_fields` and M26 `redaction.pii_categories` per capability and attaches one frozen advisory string per distinct category from the nine-entry `PII_REVIEW_ADVISORY_BY_CATEGORY` lookup — strictly read-only (no manifest mutation, no `.tusq/` artifact write, no `capability_digest` change, no command-behavior change elsewhere), deterministic across runs, `capabilities: []` is a valid exit-0 scaffold state (distinct from the `Invalid manifest: missing capabilities array` error path), stdout is empty on every failure, advisories appear one per distinct category in first-appearance order, the frozen V1.8 advisory strings use em-dash U+2014 byte-exactly, and the framing is "reviewer aid, not runtime enforcement" (SYSTEM_SPEC Constraints 19 and 20)
- A first-screen line in README and homepage hero that makes the buyer problem explicit before the category label
- A first-screen line in README and homepage hero that also states the proof path before any metadata inventory appears
- A simple terminal-first workflow example:
  `tusq init` → `tusq scan .` → `tusq manifest` → `tusq approve --all --reviewer you@example.com` → `tusq compile` → `tusq serve`
- An adoption-artifact example for share-outs: `tusq docs --out docs/capabilities.md` to generate a deterministic Markdown review packet from the current manifest
- An optional dry-run example for reviewers who want argument validation and a plan preview without adding live execution: scaffold the policy in one step with `tusq policy init --mode dry-run --reviewer you@example.com`, verify the file with `tusq policy verify .tusq/execution-policy.json --json` (exit `0` proves `tusq serve --policy` will accept the same file), optionally add `--strict --manifest tusq.manifest.json` to cross-reference `allowed_capabilities` against the manifest's approval-gated set before serve is invoked, then start `tusq serve --policy .tusq/execution-policy.json`; subsequent `tools/call` responses return `executed: false`, a policy echo, and a `dry_run_plan` object with a deterministic `plan_hash`. Reviewers who want to preview the policy without writing it can add `--dry-run` to `policy init`; reviewers who want to keep the runtime surface unchanged can omit `--mode` entirely and accept the safe default `mode: "describe-only"`. `tusq policy verify` starts no server and is safe to run as a CI pre-flight before `tusq serve --policy` is invoked, and `--strict` is opt-in — the default path never reads the manifest, and `--manifest` without `--strict` exits `1` with `--manifest requires --strict` before any file is read
- A follow-on CI-ready example for repeat runs:
  regenerate manifest → `tusq diff --from <old> --to <new> --review-queue` → re-approve drifted capabilities with `tusq approve <capability> --reviewer <id>` → `tusq diff --fail-on-unapproved-changes` in CI
- Release notes that spell out supported frameworks and deferred items
- Website or landing-page copy that reflects the actual V1 surface instead of the full long-term vision
- One sentence in every primary asset that explains what "governed" means in practice: inspect provenance, approval state, optional approval trail, mutation class, auth hints, sensitivity markers, redaction policy, and the describe-only usage context exposed through `examples` and `constraints`

### Strongly recommended supporting assets

- A short terminal demo or GIF using one of the fixture apps
- One annotated manifest example showing approvals, provenance, `side_effect_class`, `sensitivity_class`, `auth_hints`, and the `examples` / `constraints` values that flow into `tools/call`
- A concise FAQ covering "Does it execute tools?" and "Which frameworks are supported?"

## Dependencies

### Launch blockers to confirm before broad promotion

- Do not rely on `npm install -g tusq` in launch copy until package distribution is confirmed; the in-repo `package.json` is currently marked `private: true`
- Treat the top of `README.md` as a launch asset, not just internal documentation; many early users will meet the product there before they reach the website
- Ensure the README, homepage, announcement, and release notes all describe the same V1 boundary
- Ensure the README, homepage, announcement, and release notes describe "governed" the same way instead of collapsing it into a generic trust claim
- Ensure every primary asset answers "who is this for?" before it answers "what commands do I run?"
- Use a demo flow that stays inside verified product behavior: local CLI, local MCP, describe-only `tools/call`
- Have one reproducible sample project or fixture ready for screenshots and terminal capture

### Functional dependencies already satisfied

- QA has re-verified the CLI workflow and smoke suite against current HEAD
- Required launch-phase planning artifacts now exist with concrete content
- The release narrative can anchor on the verified `0.1.0` command surface and supported frameworks

## Risks

1. **Overclaim risk**
   The repo vision and older copy imply runtime learning, live execution, embedded surfaces, and broader capability composition. Launch copy must not inherit those promises.
2. **Expectation mismatch on MCP**
   Some developers will assume `tools/call` executes actions. The announcement and FAQ need to repeat that V1 `tusq serve` is describe-only by default and returns schema, examples, and constraints only. Under the opt-in `--policy` flag with `mode: "dry-run"`, `tools/call` still never executes — responses carry `executed: false`, a policy echo, and a validated `dry_run_plan`; copy must not describe dry-run as "live execution with a safety net." `tusq policy init` likewise defaults to `mode: "describe-only"` and only writes `mode: "dry-run"` when the reviewer passes `--mode dry-run` explicitly; copy must not imply scaffolding a policy flips the server into execution or out of the approval gate. `tusq policy verify` is a static validator invocation only — it starts no server, opens no port, and performs no network I/O; copy must not describe it as a runtime health check, a live probe, or a manifest validator, and a PASS proves validator parity with `tusq serve --policy`, not any runtime or execution property. `tusq policy verify --strict --manifest <path>` is an opt-in manifest-aware alignment check layered on top of that validator; copy must not describe a strict PASS as runtime safety, execution pre-flight, manifest freshness, or reachability, must not imply `--strict` is inferred from a manifest's presence (`--manifest` without `--strict` exits `1` with `--manifest requires --strict` before any file is read), and must not imply `tusq serve --policy` enforces strict checks — per M20, serve continues to silently drop unlisted or unapproved capabilities from `tools/list`. The M24 Fastify schema body-field extraction is similarly a verify-time source description, not a runtime guarantee: `source: "fastify_schema_body"` means "the declared body schema as it appears literally in the route file," not "validator-backed," "runtime-enforced by Fastify," or "ajv-validated," per SYSTEM_SPEC Constraint 14; copy must not imply the manifest reflects what Fastify actually enforces at request time (formats, custom keywords, pre-validation hooks remain invisible to the static extractor) and must not imply M24 adds a new command or flag — the extraction is active by default on source-literal pattern match inside the existing `tusq scan` command, and any non-literal `schema:`, missing `body:`, missing `properties:`, or unbalanced brace drops the `schema_fields` record and falls back byte-for-byte to the pre-M24 placeholder. The M25 PII field-name redaction hints are similarly a verify-time name-match statement, not a runtime PII-handling guarantee: a `redaction.pii_fields` entry means "the field key matches a well-known PII canonical name," not "the field carries PII at runtime," and copy must not describe M25 as "PII detection," "PII validation," "PII-validated," "GDPR-compliant," "HIPAA-compliant," "PCI-compliant," or "automated redaction" per SYSTEM_SPEC Constraint 16; copy must not imply M25 auto-escalates `capability.sensitivity_class` (it stays `"unknown"` in V1.6), auto-populates `log_level`/`mask_in_traces`/`retention_days` (they stay `null` in V1.6), or adds a new command or flag — the extraction runs by default inside existing `tusq manifest`. Copy must not imply matching is substring or tail-aware — `email_template_id` does NOT match `email`. Copy must not announce or hint at PII names outside the published, frozen V1.6 canonical 36-name set; any expansion is a future ROADMAP milestone. The M26 PII category labels are likewise descriptive manifest metadata only: `redaction.pii_categories` means "each M25 field-name hint has this canonical category label," not "runtime PII validation," "auto-retention," "retention-policy enforcement," "automated redaction," or any GDPR/HIPAA/PCI compliance claim. Copy must not imply categories auto-populate `retention_days`, `log_level`, or `mask_in_traces`, auto-escalate `sensitivity_class`, change confidence scoring, filter `tools/list`, or alter `tusq policy verify --strict`. Copy must state the one-time M13 digest flip plainly: every existing capability can require re-approval after M26 because even non-PII capabilities gain `pii_categories: []`. The M27 PII redaction review report is a reviewer aid, not runtime enforcement: `tusq redaction review` summarizes existing static evidence (M25 `pii_fields` + M26 `pii_categories`) alongside frozen per-category advisory strings, and copy must not describe it as "runtime PII enforcement," "automated PII compliance," "automated redaction policy," "GDPR-compliant," "HIPAA-compliant," "PCI-compliant," "PII validation," or a substitute for reviewer judgment (SYSTEM_SPEC Constraints 19 and 20). Copy must not imply M27 mutates the manifest, writes any `.tusq/` artifact, flips `capability_digest`, re-stamps `manifest_version`/`generated_at` with wall-clock time, auto-approves or auto-escalates anything, or changes the behavior of any other command — on first post-M27 run the manifest is unchanged and `tusq diff` surfaces nothing. Copy must not frame an empty-capabilities manifest (`capabilities: []`) as an error — it is a valid scaffold state that exits 0 with `No capabilities in manifest — nothing to review.` in human mode and `{manifest_path, manifest_version, generated_at, capabilities: []}` under `--json`. Copy must not imply `--diff`/`--since`/CSV/HTML/Markdown export modes, reviewer-identity capture, or custom/overridable advisory strings exist in V1.8 — the flag surface is exactly `--manifest`, `--capability`, `--json`, and the frozen nine-entry `PII_REVIEW_ADVISORY_BY_CATEGORY` lookup uses em-dash U+2014 byte-exactly with any character-level drift being a material governance event. The M28 (V1.9) `sensitivity_class` field is similarly a verify-time reviewer aid, not a runtime guarantee: a `sensitivity_class: "restricted"` value means "the manifest evidence — preserve flag, admin/destructive verb, financial route, PII categories, auth marker — placed this capability into the `restricted` bucket under the frozen six-rule decision table," NOT "tusq enforces restricted access at runtime," "tusq certifies GDPR/HIPAA/PCI/SOC2 compliance," or "the runtime is automatically gated by sensitivity"; copy must not describe `sensitivity_class` as runtime data-access control, runtime PII enforcement, automated compliance certification, automated access governance, or as a substitute for reviewer judgment, and must not imply M28 introduces a new top-level CLI noun, new subcommand, environment-variable gate, or any change to `tusq compile`, `tusq approve`, `tusq serve`, `tusq policy verify`, MCP `tools/list`/`tools/call`, or `dry_run_plan` shapes (the only new CLI surface is the optional `--sensitivity <class>` filter on the existing `tusq review` command, the 13-command surface is preserved exactly, `tusq compile` output is byte-identical regardless of `sensitivity_class` value per AC-9). Copy must not imply zero-evidence capabilities are silently `"public"` — they MUST receive `"unknown"` explicitly per the AC-4 zero-evidence guard and SYSTEM_SPEC Constraint 21. Copy must not imply the six-rule decision table is configurable, locale-aware, compliance-mapped, or extensible at runtime — R1–R6 are FROZEN, any change is a material governance event under its own future ROADMAP milestone. Copy must state plainly that first post-M28 manifest regeneration flips the M13 `capability_digest` on every capability whose previous `"unknown"` value resolves to a computed class, requiring fresh `tusq approve` re-approval before `tools/list` re-surfaces those capabilities — the M13 approval gate is the correct migration path, not a regression. Copy must not imply `--sensitivity <class>` changes `tusq review` exit-code semantics or hides unapproved/low-confidence capabilities; values outside the five-element enum `{public, internal, confidential, restricted, unknown}` exit 1 with an actionable message before any output. The M29 (V1.10) `auth_requirements` field is similarly a verify-time reviewer aid, not a runtime guarantee: an `auth_scheme: "bearer"` value means "an entry in `cap.auth_hints` matched the R1 regex `/bearer|jwt|access[_-]?token/i`," NOT "tusq verifies bearer tokens at runtime," "tusq enforces authentication," "tusq certifies OAuth/OIDC/SAML/SOC2/ISO27001 compliance," or "tusq validates token signatures or scope grants"; copy must not describe `auth_requirements` as runtime authentication, runtime authorization enforcement, automated AAA enforcement, automated compliance certification, or as a substitute for reviewer judgment, and must not imply M29 introduces a new top-level CLI noun, new subcommand, environment-variable gate, new dependency in `package.json` (no `passport`, no `jsonwebtoken`, no `oauth2-server`), or any change to `tusq compile`, `tusq approve`, `tusq serve`, `tusq policy verify`, `tusq redaction review`, MCP `tools/list`/`tools/call`/`dry_run_plan` shapes, or compiled `tusq-tools/*.json` output (the only new CLI surface is the optional `--auth-scheme <scheme>` filter on the existing `tusq review` command, the 13-command surface is preserved exactly, `tusq compile` output is byte-identical before and after M29 per AC-7, and `auth_requirements` does NOT appear in compiled tools or any MCP response). Copy must not imply zero-evidence capabilities are silently `"none"` — they MUST receive `"unknown"` explicitly via the AC-4 zero-evidence guard that fires BEFORE R1 (SYSTEM_SPEC Constraint 22); `"none"` is reachable only from explicit R6 evidence. Copy must not imply the six-rule decision table is configurable, locale-aware, compliance-mapped, or extensible at runtime — R1–R6 are FROZEN; any change to regex patterns, scheme assignments, precedence, or default class is a material governance event under its own future ROADMAP milestone. Copy must state plainly that first post-M29 manifest regeneration flips the M13 `capability_digest` on every capability because `auth_requirements` is now part of the digest payload, requiring fresh `tusq approve` re-approval before `tools/list` re-surfaces capabilities — the M13 approval gate is the correct migration path, not a regression. Copy must not imply `--auth-scheme <scheme>` changes `tusq review` exit-code semantics or hides unapproved/low-confidence capabilities; values outside the seven-element enum `{bearer, api_key, session, basic, oauth, none, unknown}` exit 1 with an actionable message before any output, and combining `--auth-scheme` with `--sensitivity` is AND-style intersection (a capability must match both filters to appear), not OR-style union. Copy must not imply `auth_scopes`/`auth_roles` reflect what an OAuth provider actually grants or what an authorization middleware actually enforces at runtime — both arrays come from the frozen middleware-annotation regexes `/scopes?:\s*\[([^\]]+)\]/` and `/role[s]?:\s*\[([^\]]+)\]/`, are always present (NEVER `null`), order-preserving, case-sensitively deduplicated, and return `[]` on no match. Copy must disclose the V1.10 limitation that the M9/M15 scanner does not set `auth_required` during `tusq scan` or `tusq manifest` generation, so R6 (`auth_required === false` → `auth_scheme: "none"`) is reachable in V1.10 only via manually-edited manifests; the implementation is correct for manual edits, this is non-blocking for V1.10 ship, and a future milestone may populate `auth_required` from scanner evidence.
3. **Governance vagueness**
   If "governed" is left as slogan-level copy, buyers may miss that V1 already ships reviewable approval state, optional approval trail, and redaction policy alongside route provenance and classification metadata.
4. **Category abstraction overload**
   If we lead with "capability compiler" without first naming the incumbent-SaaS problem, the launch will sound like internal platform language rather than a concrete product wedge.
5. **Framework expectation drift**
   If the launch message sounds category-wide, teams on unsupported stacks will bounce immediately. Framework support should appear high in every primary asset.
6. **Heuristic scanner perception**
   The scanner is useful but not exhaustive. We should frame the manifest as reviewable output, not perfect autonomous understanding.
7. **Install-path ambiguity**
   Until the package/distribution path is finalized, the launch CTA should be "try it locally from the repo" rather than "install it from npm."

## Channel Guardrails

- Website and announcement: lead with audience + operator problem before the workflow proof and V1 boundary
- Website and announcement: keep the proof sentence short enough to skim before listing metadata fields
- Website and announcement: show at least one concrete example of the review surface, not just the word "governance"
- GitHub README and docs: tell users how to try the product today without implying a public package is already published
- Social posts: frame the release as a governed path from repo to reviewed manifest to describe-only MCP, not as a general AI agent platform
- Community posts: invite supported-stack users with real repos, not broad curiosity traffic from unsupported ecosystems

## Run re-affirmation — run_8fe3b8b418dc589c (turn_5e97659a21a458e2, launch, attempt 1)

Re-verified the M29 (V1.10) launch plan against shipped product behavior on HEAD `a89c4fb`. The "Static Auth Requirements Inference" launch story remains: a non-attestation reviewer aid that infers `auth_requirements` (closed seven-value `auth_scheme` + closed five-value `evidence_source` + frozen six-rule decision table + zero-evidence `unknown` guard + order-preserving `auth_scopes`/`auth_roles` arrays) from the existing manifest middleware-name and route-prefix evidence with no new CLI noun, no new subcommand, no new dependency, no environment-variable gate, byte-identical `tusq compile` output, and byte-for-byte unchanged MCP `tools/list` / `tools/call` / `dry_run_plan` shapes. The only operator-visible launch surface is the optional `--auth-scheme <scheme>` filter on `tusq review` (AND-style intersection with M28's `--sensitivity`). Disclosed V1.10 limitation: scanner does not set `auth_required`, so R6 is reachable only via manually-edited manifests. `npm test` passes 16 scenarios; the 13-command CLI surface (`init, scan, manifest, compile, serve, review, docs, approve, diff, policy, redaction, version, help`) is intact. No launch-plan revision required.

## Run re-affirmation — run_94746c3508844fcb (turn_d2cdf748d6a4b511, launch, attempt 1)

Re-verified the M29 (V1.10) launch plan on HEAD `a3a794a` for run_94746c3508844fcb. Zero source drift through this run's pm/dev/qa chain. The launch story remains: a non-attestation reviewer aid that infers `auth_requirements` from existing manifest middleware-name and route-prefix evidence with no new CLI noun, no new subcommand, no new dependency, byte-identical `tusq compile` output, and byte-for-byte unchanged MCP `tools/list` / `tools/call` / `dry_run_plan` shapes — `auth_requirements` does NOT appear in any MCP response. The only operator-visible launch surface remains the optional `--auth-scheme <scheme>` filter on `tusq review` (AND-style intersection with M28's `--sensitivity`). V1.10 limitation: scanner does not set `auth_required`, so R6 is reachable only via manually-edited manifests. `npm test` passes 16 scenarios; the 13-command CLI surface is intact. No launch-plan revision required.
